# v1.4 · La Evolución Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseño visual y estructural end-to-end de Matripuntos según el bundle Claude Design v2 — 16 pantallas rediseñadas, Analytics dedicada con Premium teaser, onboarding de 6 pasos, dark-only.

**Architecture:** Big-bang en la rama `feature/v1.4-la-evolucion`. Frontend: nueva carpeta `components/v2/` con primitives + layout + dashboard + analytics + premium. Pages se rewrite in-place. Tokens del bundle vía `tailwind.config.js` extend. Backend: 4 endpoints analytics nuevos + tabla `PremiumInterest`. Sin cambios en schema de core (User, Couple, Event, Task, etc.).

**Tech Stack:** React 18 + TypeScript + Vite + Tailwind · Zustand · React Query · Recharts (solo fallback; charts nuevos vía SVG puro siguiendo kit) · Prisma + SQLite→PostgreSQL · Express + Zod · node-cron.

**Spec fuente:** `docs/superpowers/specs/2026-04-20-v1.4-la-evolucion-design.md`  
**Kit de diseño:** `/tmp/claude-design-v2/matripuntos-design-system/project/ui_kits/app/` (12 screens/*.jsx + Dashboard, Tasks, Analytics, Navigation, Primitives, Gamification)

---

## Phase 0 — Foundation: branch, tokens, primitives, tema

### Task 0.1: Crear rama y worktree

**Files:**
- Modify: ninguno (solo git)

- [ ] **Step 1: Verificar en main limpio**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
git status
git pull origin main
```

Expected: "working tree clean" + branch `main` up to date con `origin/main`.

- [ ] **Step 2: Crear worktree global**

```bash
mkdir -p ~/.config/superpowers/worktrees/Matripuntos
cd "/Users/edu/Web development/Claude/Matripuntos"
git worktree add ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion -b feature/v1.4-la-evolucion
cd ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion
git status
```

Expected: "On branch feature/v1.4-la-evolucion" + "nothing to commit".

- [ ] **Step 3: Instalar deps en worktree**

```bash
cd ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion
npm install
cd src/backend && npx prisma generate
cd ../..
```

Expected: sin errores de TypeScript.

- [ ] **Step 4: Smoke test del worktree**

```bash
cd ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion
npm run typecheck 2>&1 | tail -5
```

Expected: "Found 0 errors" o equivalente.

- [ ] **Step 5: Commit inicial marker**

```bash
git commit --allow-empty -m "chore: start v1.4 La Evolución branch"
```

### Task 0.2: Añadir tokens del bundle a Tailwind

**Files:**
- Modify: `src/frontend/tailwind.config.js`
- Modify: `src/frontend/src/index.css`

- [ ] **Step 1: Extender `tailwind.config.js`**

Reemplazar el bloque `theme.extend` con:

```js
theme: {
  extend: {
    colors: {
      bg: { page: '#0f0a1e' },
      surface: {
        card:     'rgba(26,16,53,0.6)',
        elevated: 'rgba(26,16,53,0.95)',
        muted:    'rgba(168,85,247,0.08)',
      },
      brand: {
        amber:       '#f59e0b',
        'amber-dark':'#d97706',
        purple:      '#a855f7',
        'purple-dark':'#7c3aed',
        indigo:      '#4f46e5',
      },
      text: {
        primary:   '#e2e8f0',
        secondary: '#9ca3af',
        tertiary:  '#6b7280',
      },
      brd: {
        subtle: 'rgba(168,85,247,0.12)',
        purple: 'rgba(168,85,247,0.25)',
      },
      success: '#22c55e',
      warn:    '#fbbf24',
      danger:  '#ef4444',
    },
    backgroundImage: {
      'grad-hero':    'linear-gradient(135deg, #4f46e5, #7c3aed)',
      'grad-cta':     'linear-gradient(135deg, #f59e0b, #d97706)',
      'grad-page':    'linear-gradient(180deg, #0f0a1e 0%, #1a0f35 100%)',
      'grad-premium': 'linear-gradient(135deg, #1a1138, #2d1e5f)',
    },
    borderRadius: {
      sm: '8px',
      md: '12px',
      lg: '14px',
      xl: '18px',
    },
    fontFamily: {
      sans: ['Inter', 'system-ui', 'sans-serif'],
    },
    fontVariantNumeric: {
      'tabular': 'tabular-nums',
    },
  },
},
```

- [ ] **Step 2: Añadir base styles a `src/frontend/src/index.css`**

Reemplazar el body con:

```css
body {
  background: linear-gradient(180deg, #0f0a1e 0%, #1a0f35 100%);
  min-height: 100vh;
  color: #e2e8f0;
  font-family: 'Inter', system-ui, sans-serif;
  -webkit-font-smoothing: antialiased;
}
.tabular-nums { font-variant-numeric: tabular-nums; }
```

- [ ] **Step 3: Smoke**

```bash
cd src/frontend && npm run typecheck && npm run build 2>&1 | tail -10
```

Expected: build exitoso sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/frontend/tailwind.config.js src/frontend/src/index.css
git commit -m "feat(theme): añadir tokens Claude Design v2 a Tailwind"
```

### Task 0.3: Eliminar toggle light/dark del store

**Files:**
- Modify: `src/frontend/src/store/useAppStore.ts`
- Modify: `src/frontend/src/pages/Dashboard.tsx`
- Modify: cualquier otro archivo que consuma `theme` o `toggleTheme`

- [ ] **Step 1: Localizar todos los usos**

```bash
grep -rn "theme\|toggleTheme" src/frontend/src --include="*.tsx" --include="*.ts"
```

Anotar cada archivo que aparezca.

- [ ] **Step 2: Quitar `theme` y `toggleTheme` del store**

En `src/frontend/src/store/useAppStore.ts`:
- Borrar el estado `theme: 'dark' | 'light'`
- Borrar la función `toggleTheme`
- Borrar las líneas que persisten `theme` en localStorage

- [ ] **Step 3: Quitar consumos del toggle en las pages**

En cada archivo identificado en step 1, quitar:
- import de `toggleTheme` / `theme` desde el store
- botones que ejecuten `toggleTheme()`
- clases condicionales basadas en `theme`

- [ ] **Step 4: Smoke**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -5
```

Expected: 0 errors.

- [ ] **Step 5: Ejecutar dev server y verificar**

```bash
npm run dev &
sleep 5
curl -s http://localhost:5173 | head -20
kill %1
```

Expected: HTML de Vite cargando correctamente.

- [ ] **Step 6: Commit**

```bash
git add src/frontend/src/store/useAppStore.ts src/frontend/src/pages/
git commit -m "refactor(theme): eliminar toggle light/dark (dark-only en v1.4)"
```

### Task 0.4: Primitive `Card`

**Files:**
- Create: `src/frontend/src/components/v2/primitives/Card.tsx`

- [ ] **Step 1: Crear `Card.tsx`**

```tsx
import { ReactNode, CSSProperties } from 'react'

interface CardProps {
  children: ReactNode
  className?: string
  style?: CSSProperties
  padding?: 'sm' | 'md' | 'lg'
  elevated?: boolean
}

export function Card({ children, className = '', style, padding = 'md', elevated = false }: CardProps) {
  const paddings = { sm: 'p-3', md: 'p-4', lg: 'p-5' }
  const bg = elevated ? 'bg-surface-elevated' : 'bg-surface-card'
  return (
    <div
      className={`${bg} border border-brd-subtle rounded-lg ${paddings[padding]} ${className}`}
      style={style}
    >
      {children}
    </div>
  )
}
```

- [ ] **Step 2: Smoke**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
```

Expected: 0 errors.

- [ ] **Step 3: Commit**

```bash
git add src/frontend/src/components/v2/primitives/Card.tsx
git commit -m "feat(v2): añadir primitive Card"
```

### Task 0.5: Primitives `Button`, `Pill`, `ProgressBar`, `Input`

**Files:**
- Create: `src/frontend/src/components/v2/primitives/Button.tsx`
- Create: `src/frontend/src/components/v2/primitives/Pill.tsx`
- Create: `src/frontend/src/components/v2/primitives/ProgressBar.tsx`
- Create: `src/frontend/src/components/v2/primitives/Input.tsx`

- [ ] **Step 1: `Button.tsx`**

```tsx
import { ButtonHTMLAttributes, ReactNode } from 'react'

type Variant = 'primary' | 'secondary' | 'ghost' | 'danger' | 'outline'
type Size = 'sm' | 'md' | 'lg'

interface Props extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: Variant
  size?: Size
  fullWidth?: boolean
  children: ReactNode
}

const variants: Record<Variant, string> = {
  primary:   'bg-grad-cta text-white shadow-lg shadow-brand-amber/40 hover:opacity-95',
  secondary: 'bg-grad-hero text-white shadow-lg shadow-brand-indigo/30 hover:opacity-95',
  ghost:     'bg-surface-muted text-text-primary border border-brd-purple hover:bg-surface-card',
  danger:    'bg-danger/90 text-white hover:bg-danger',
  outline:   'bg-transparent text-text-primary border border-brd-purple hover:bg-surface-muted',
}

const sizes: Record<Size, string> = {
  sm: 'px-3 py-1.5 text-xs',
  md: 'px-4 py-2.5 text-sm',
  lg: 'px-5 py-3.5 text-base',
}

export function Button({ variant = 'primary', size = 'md', fullWidth, className = '', children, ...rest }: Props) {
  const width = fullWidth ? 'w-full' : ''
  return (
    <button
      className={`rounded-md font-bold transition disabled:opacity-40 disabled:cursor-not-allowed ${variants[variant]} ${sizes[size]} ${width} ${className}`}
      {...rest}
    >
      {children}
    </button>
  )
}
```

- [ ] **Step 2: `Pill.tsx`**

```tsx
import { ReactNode } from 'react'

type Tone = 'purple' | 'amber' | 'indigo' | 'success' | 'warn' | 'danger'

interface Props { tone?: Tone; children: ReactNode; className?: string }

const tones: Record<Tone, string> = {
  purple:  'bg-brand-purple/15 text-brand-purple border border-brand-purple/30',
  amber:   'bg-brand-amber/15 text-brand-amber border border-brand-amber/30',
  indigo:  'bg-brand-indigo/15 text-[#a5b4fc] border border-brand-indigo/30',
  success: 'bg-success/15 text-success border border-success/30',
  warn:    'bg-warn/15 text-warn border border-warn/30',
  danger:  'bg-danger/15 text-danger border border-danger/30',
}

export function Pill({ tone = 'purple', children, className = '' }: Props) {
  return (
    <span className={`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-semibold ${tones[tone]} ${className}`}>
      {children}
    </span>
  )
}
```

- [ ] **Step 3: `ProgressBar.tsx`**

```tsx
interface Props {
  value: number          // 0-100
  tone?: 'amber' | 'purple' | 'white'
  height?: number
  className?: string
}

export function ProgressBar({ value, tone = 'purple', height = 8, className = '' }: Props) {
  const pct = Math.max(0, Math.min(100, value))
  const fills = {
    amber:  'bg-grad-cta',
    purple: 'bg-gradient-to-r from-brand-purple to-brand-purple-dark',
    white:  'bg-white',
  }
  const track = tone === 'white' ? 'bg-black/20' : 'bg-white/10'
  return (
    <div className={`relative overflow-hidden rounded-full ${track} ${className}`} style={{ height }}>
      <div
        className={`absolute inset-y-0 left-0 ${fills[tone]} rounded-full transition-all`}
        style={{ width: `${pct}%` }}
      />
    </div>
  )
}
```

- [ ] **Step 4: `Input.tsx`**

```tsx
import { InputHTMLAttributes, forwardRef } from 'react'

interface Props extends InputHTMLAttributes<HTMLInputElement> {
  label?: string
  error?: string
  hint?: string
}

export const Input = forwardRef<HTMLInputElement, Props>(function Input(
  { label, error, hint, className = '', ...rest }, ref
) {
  return (
    <div className="flex flex-col gap-1">
      {label && <label className="text-xs font-semibold text-text-secondary">{label}</label>}
      <input
        ref={ref}
        className={`bg-surface-card border border-brd-purple rounded-md px-3 py-2.5 text-sm text-text-primary placeholder:text-text-tertiary focus:outline-none focus:border-brand-purple focus:ring-1 focus:ring-brand-purple/50 ${error ? 'border-danger' : ''} ${className}`}
        {...rest}
      />
      {hint && !error && <span className="text-[11px] text-text-tertiary">{hint}</span>}
      {error && <span className="text-[11px] text-danger">{error}</span>}
    </div>
  )
})
```

- [ ] **Step 5: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
```

```bash
git add src/frontend/src/components/v2/primitives/
git commit -m "feat(v2): primitives Button, Pill, ProgressBar, Input"
```

### Task 0.6: Primitives `Avatar`, `BottomSheet`

**Files:**
- Create: `src/frontend/src/components/v2/primitives/Avatar.tsx`
- Create: `src/frontend/src/components/v2/primitives/BottomSheet.tsx`

- [ ] **Step 1: `Avatar.tsx`**

```tsx
interface Props {
  emoji?: string
  color?: string          // hex string
  size?: 'sm' | 'md' | 'lg'
  mood?: string | null
  className?: string
}

const sizes = {
  sm: { box: 28, emoji: 14 },
  md: { box: 36, emoji: 18 },
  lg: { box: 44, emoji: 22 },
}

export function Avatar({ emoji = '💕', color = '#7c3aed', size = 'md', mood, className = '' }: Props) {
  const s = sizes[size]
  return (
    <div className={`relative flex-shrink-0 ${className}`} style={{ width: s.box, height: s.box }}>
      <div
        className="w-full h-full rounded-full flex items-center justify-center"
        style={{ background: `linear-gradient(135deg, ${color}, ${color}dd)`, fontSize: s.emoji }}
      >
        {emoji}
      </div>
      {mood && (
        <span
          className="absolute -bottom-1 -right-1 text-xs rounded-full bg-bg-page border border-bg-page flex items-center justify-center"
          style={{ width: 16, height: 16, fontSize: 10 }}
        >{mood}</span>
      )}
    </div>
  )
}
```

- [ ] **Step 2: `BottomSheet.tsx`**

```tsx
import { ReactNode, useEffect } from 'react'

interface Props {
  open: boolean
  onClose: () => void
  title?: string
  children: ReactNode
}

export function BottomSheet({ open, onClose, title, children }: Props) {
  useEffect(() => {
    if (!open) return
    const onKey = (e: KeyboardEvent) => e.key === 'Escape' && onClose()
    window.addEventListener('keydown', onKey)
    document.body.style.overflow = 'hidden'
    return () => {
      window.removeEventListener('keydown', onKey)
      document.body.style.overflow = ''
    }
  }, [open, onClose])

  if (!open) return null
  return (
    <>
      <div
        onClick={onClose}
        className="fixed inset-0 bg-black/55 backdrop-blur-sm z-[80] animate-in fade-in"
      />
      <div className="fixed left-0 right-0 bottom-0 z-[81] max-w-[500px] mx-auto bg-surface-elevated border-t border-brd-purple rounded-t-xl p-4 pb-6 animate-in slide-in-from-bottom duration-200">
        {title && <h3 className="text-base font-bold text-text-primary mb-3">{title}</h3>}
        {children}
      </div>
    </>
  )
}
```

- [ ] **Step 3: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/primitives/
git commit -m "feat(v2): primitives Avatar, BottomSheet"
```

---

## Phase 1 — Backend: nuevos endpoints analytics + PremiumInterest

### Task 1.1: Migración Prisma `PremiumInterest`

**Files:**
- Modify: `src/backend/prisma/schema.prisma`
- Create: `src/backend/prisma/migrations/YYYYMMDD_v1_4_premium_interest/migration.sql` (auto)

- [ ] **Step 1: Añadir modelo al schema**

Añadir al final de `src/backend/prisma/schema.prisma`:

```prisma
model PremiumInterest {
  id        String   @id @default(uuid())
  email     String
  userId    String?
  coupleId  String?
  source    String
  createdAt DateTime @default(now())
  notified  Boolean  @default(false)

  user User? @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([email])
  @@index([notified])
}
```

Añadir relación inversa en `User`:

```prisma
model User {
  // ...campos existentes...
  premiumInterests PremiumInterest[]
}
```

- [ ] **Step 2: Ejecutar migración**

```bash
cd src/backend
npx prisma migrate dev --name v1_4_premium_interest
npx prisma generate
```

Expected: "Migration applied" + cliente regenerado.

- [ ] **Step 3: Verificar con Prisma Studio (opcional)**

```bash
npx prisma studio &
sleep 2
```

Abrir `http://localhost:5555` y confirmar que la tabla `PremiumInterest` existe. Cerrar.

- [ ] **Step 4: Smoke typecheck**

```bash
cd ../.. && npm run typecheck 2>&1 | tail -5
```

Expected: 0 errores.

- [ ] **Step 5: Commit**

```bash
git add src/backend/prisma/schema.prisma src/backend/prisma/migrations/
git commit -m "feat(db): añadir tabla PremiumInterest para captura de interés Premium"
```

### Task 1.2: Endpoint `POST /api/premium/interest`

**Files:**
- Create: `src/backend/src/routes/premium.ts`
- Modify: `src/backend/src/server.ts`

- [ ] **Step 1: Crear `routes/premium.ts`**

```ts
import { Router, Request, Response } from 'express'
import { PrismaClient } from '@prisma/client'
import rateLimit from 'express-rate-limit'
import { z } from 'zod'
import { optionalAuthMiddleware } from '../middleware/authMiddleware.js'

const prisma = new PrismaClient()
const router = Router()

const interestLimiter = rateLimit({
  windowMs: 60 * 1000,
  max: 5,
  message: { error: 'Demasiadas peticiones. Inténtalo en 1 minuto.' },
})

const interestSchema = z.object({
  email:  z.string().email('Email no válido').max(200),
  source: z.enum(['analytics_advanced_overlay', 'settings_premium_cta', 'onboarding']),
})

router.post('/interest', interestLimiter, optionalAuthMiddleware, async (req: Request, res: Response) => {
  const parsed = interestSchema.safeParse(req.body)
  if (!parsed.success) {
    return res.status(400).json({ error: parsed.error.errors[0]?.message ?? 'Datos no válidos' })
  }
  const { email, source } = parsed.data
  const userId   = (req as any).userId ?? null
  const coupleId = (req as any).coupleId ?? null

  try {
    const existing = await prisma.premiumInterest.findFirst({
      where: { email, source },
    })
    if (existing) {
      return res.status(409).json({ error: 'Ya estás apuntada/o 💕' })
    }
    const created = await prisma.premiumInterest.create({
      data: { email, source, userId, coupleId },
    })
    return res.status(201).json({ id: created.id, message: '¡Listo! Te avisaremos 💕' })
  } catch (e) {
    const msg = e instanceof Error ? e.message : 'Error desconocido'
    return res.status(500).json({ error: msg })
  }
})

export default router
```

- [ ] **Step 2: Montar en `server.ts`**

Añadir cerca del resto de routers:

```ts
import premiumRoutes from './routes/premium.js'
// ...
app.use('/api/premium', premiumRoutes)
```

- [ ] **Step 3: Smoke test manual**

```bash
cd src/backend && npm run dev &
sleep 3
curl -X POST http://localhost:3000/api/premium/interest \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","source":"analytics_advanced_overlay"}'
curl -X POST http://localhost:3000/api/premium/interest \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","source":"analytics_advanced_overlay"}'
kill %1
```

Expected: primer POST → 201 con id; segundo POST → 409 "Ya estás apuntada/o 💕".

- [ ] **Step 4: Commit**

```bash
git add src/backend/src/routes/premium.ts src/backend/src/server.ts
git commit -m "feat(premium): endpoint POST /api/premium/interest para captura de email"
```

### Task 1.3: Endpoint `GET /api/analytics/time-invested`

**Files:**
- Modify: `src/backend/src/services/analyticsService.ts`
- Modify: `src/backend/src/routes/analytics.ts`

- [ ] **Step 1: Tabla de duraciones en el service**

En `analyticsService.ts` añadir constante:

```ts
// Horas estimadas por categoría (heurística)
export const CATEGORY_HOURS: Record<string, number> = {
  cocina:       1.0,
  banos:        0.5,
  limpieza:     1.5,
  compra:       1.0,
  logistica:    1.0,
  cuidado:      2.0,
  mantenimiento:0.75,
  jardineria:   1.0,
  mascotas:     0.5,
  otros:        0.75,
}
```

- [ ] **Step 2: Función `getTimeInvested(coupleId, range)`**

```ts
export async function getTimeInvested(coupleId: string, range: 'week' | 'month') {
  const days = range === 'week' ? 7 : 30
  const since = new Date()
  since.setDate(since.getDate() - days)

  const couple = await prisma.couple.findUnique({
    where: { id: coupleId },
    include: { users: true },
  })
  if (!couple || couple.users.length === 0) return { you: { hours: 0 }, partner: { hours: 0 } }

  const [user1, user2] = couple.users
  const logs = await prisma.taskLog.findMany({
    where: { coupleId, date: { gte: since }, status: 'verified' },
    include: { task: true },
  })
  const events = await prisma.event.findMany({
    where: { coupleId, dateStart: { gte: since }, status: { in: ['accepted', 'forced'] } },
  })

  function hoursFor(userId: string) {
    const taskHours = logs
      .filter(l => l.completedBy === userId)
      .reduce((sum, l) => sum + (CATEGORY_HOURS[l.task.category] ?? 1.0), 0)
    const eventHours = events
      .filter(e => e.createdBy === userId)
      .reduce((sum, e) => {
        const ms = new Date(e.dateEnd).getTime() - new Date(e.dateStart).getTime()
        return sum + ms / (1000 * 60 * 60)
      }, 0)
    return Math.round((taskHours + eventHours) * 10) / 10
  }

  return {
    you:     { id: user1.id, name: user1.name, hours: hoursFor(user1.id) },
    partner: user2 ? { id: user2.id, name: user2.name, hours: hoursFor(user2.id) } : null,
  }
}
```

- [ ] **Step 3: Route handler**

En `routes/analytics.ts` añadir:

```ts
router.get('/time-invested', async (req, res) => {
  try {
    const coupleId = (req as any).coupleId
    const range = (req.query.range as 'week' | 'month') ?? 'week'
    const data = await analyticsService.getTimeInvested(coupleId, range)
    res.json({ success: true, data })
  } catch (e) {
    res.status(500).json({ error: e instanceof Error ? e.message : 'Error' })
  }
})
```

- [ ] **Step 4: Smoke manual**

```bash
cd src/backend && npm run dev &
sleep 3
# Usar un JWT válido de seed data
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"email":"maria@test.com","password":"demo1234"}' | grep -o '"token":"[^"]*' | cut -d'"' -f4)
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/analytics/time-invested?range=week"
kill %1
```

Expected: `{"success":true,"data":{"you":{...,"hours":X.Y},"partner":{...,"hours":X.Y}}}`.

- [ ] **Step 5: Commit**

```bash
git add src/backend/src/services/analyticsService.ts src/backend/src/routes/analytics.ts
git commit -m "feat(analytics): endpoint /time-invested con heurística por categoría"
```

### Task 1.4: Endpoint `GET /api/analytics/heatmap`

**Files:**
- Modify: `src/backend/src/services/analyticsService.ts`
- Modify: `src/backend/src/routes/analytics.ts`

- [ ] **Step 1: Función `getHeatmap`**

```ts
// Franjas horarias: 6-9, 9-12, 12-15, 15-18, 18-21, 21-24
const HOUR_BUCKETS = [6, 9, 12, 15, 18, 21]

function bucketForHour(h: number): number {
  if (h < 6)  return 21
  for (let i = HOUR_BUCKETS.length - 1; i >= 0; i--) {
    if (h >= HOUR_BUCKETS[i]) return HOUR_BUCKETS[i]
  }
  return HOUR_BUCKETS[0]
}

export async function getHeatmap(coupleId: string, weeks: number = 4) {
  const since = new Date()
  since.setDate(since.getDate() - weeks * 7)

  const logs = await prisma.taskLog.findMany({
    where: { coupleId, date: { gte: since } },
  })
  const events = await prisma.event.findMany({
    where: { coupleId, dateStart: { gte: since }, status: { in: ['accepted', 'forced'] } },
  })

  // key = `${dow}-${bucket}`, value = count
  const map = new Map<string, number>()
  function add(date: Date) {
    const dow = (date.getDay() + 6) % 7  // L=0, D=6
    const bucket = bucketForHour(date.getHours())
    const key = `${dow}-${bucket}`
    map.set(key, (map.get(key) ?? 0) + 1)
  }
  logs.forEach(l => add(new Date(l.date)))
  events.forEach(e => add(new Date(e.dateStart)))

  const max = Math.max(1, ...Array.from(map.values()))
  const grid: { dow: number; bucket: number; count: number; norm: number }[] = []
  for (let dow = 0; dow < 7; dow++) {
    for (const bucket of HOUR_BUCKETS) {
      const count = map.get(`${dow}-${bucket}`) ?? 0
      grid.push({ dow, bucket, count, norm: Math.round((count / max) * 4) })
    }
  }
  return { grid, buckets: HOUR_BUCKETS }
}
```

- [ ] **Step 2: Route handler**

```ts
router.get('/heatmap', async (req, res) => {
  try {
    const coupleId = (req as any).coupleId
    const weeks = Math.max(1, Math.min(52, Number(req.query.weeks ?? 4)))
    const data = await analyticsService.getHeatmap(coupleId, weeks)
    res.json({ success: true, data })
  } catch (e) {
    res.status(500).json({ error: e instanceof Error ? e.message : 'Error' })
  }
})
```

- [ ] **Step 3: Smoke**

```bash
cd src/backend && npm run dev &
sleep 3
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/analytics/heatmap?weeks=4"
kill %1
```

Expected: grid con 42 entries (7 × 6), con `norm` entre 0 y 4.

- [ ] **Step 4: Commit**

```bash
git add src/backend/src/services/analyticsService.ts src/backend/src/routes/analytics.ts
git commit -m "feat(analytics): endpoint /heatmap (7×6 grid dow × hour bucket)"
```

### Task 1.5: Endpoint `GET /api/analytics/completion-rate`

**Files:**
- Modify: `src/backend/src/services/analyticsService.ts`
- Modify: `src/backend/src/routes/analytics.ts`

- [ ] **Step 1: Función `getCompletionRate`**

```ts
export async function getCompletionRate(coupleId: string, range: 'week' | 'month') {
  const days = range === 'week' ? 7 : 30
  const since = new Date()
  since.setDate(since.getDate() - days)

  const couple = await prisma.couple.findUnique({
    where: { id: coupleId },
    include: { users: true },
  })
  if (!couple || couple.users.length === 0) return { you: 0, partner: 0 }

  const [user1, user2] = couple.users

  async function rateFor(userId: string) {
    const total = await prisma.taskLog.count({
      where: { coupleId, completedBy: userId, date: { gte: since } },
    })
    if (total === 0) return 0
    const verified = await prisma.taskLog.count({
      where: { coupleId, completedBy: userId, date: { gte: since }, status: 'verified' },
    })
    return Math.round((verified / total) * 100)
  }

  return {
    you:     { id: user1.id, name: user1.name, pct: await rateFor(user1.id) },
    partner: user2 ? { id: user2.id, name: user2.name, pct: await rateFor(user2.id) } : null,
  }
}
```

- [ ] **Step 2: Route**

```ts
router.get('/completion-rate', async (req, res) => {
  try {
    const coupleId = (req as any).coupleId
    const range = (req.query.range as 'week' | 'month') ?? 'month'
    const data = await analyticsService.getCompletionRate(coupleId, range)
    res.json({ success: true, data })
  } catch (e) {
    res.status(500).json({ error: e instanceof Error ? e.message : 'Error' })
  }
})
```

- [ ] **Step 3: Smoke + commit**

```bash
cd src/backend && npm run dev &
sleep 3
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/analytics/completion-rate?range=month"
kill %1
git add src/backend/src/services/analyticsService.ts src/backend/src/routes/analytics.ts
git commit -m "feat(analytics): endpoint /completion-rate (verified/total por usuario)"
```

### Task 1.6: Endpoint `GET /api/analytics/insight` heurístico

**Files:**
- Create: `src/backend/src/services/insightHeuristic.ts`
- Modify: `src/backend/src/services/analyticsService.ts`
- Modify: `src/backend/src/routes/analytics.ts`

- [ ] **Step 1: Crear `services/insightHeuristic.ts`**

```ts
interface InsightInput {
  user1Name: string
  user2Name: string
  topCategoryUser1: { name: string; count: number } | null
  topCategoryUser2: { name: string; count: number } | null
  timePctUser1: number              // 0-100
  equityDelta: number               // diff vs mes anterior
  worstCategory: string | null      // categoría más desequilibrada
}

interface InsightOutput {
  text: string
  bullets: { tone: 'success' | 'warn' | 'neutral'; text: string }[]
}

const TEMPLATES: ((i: InsightInput) => string)[] = [
  (i) => `Este mes ${i.user2Name} cubrió más ${i.topCategoryUser2?.name ?? 'tareas'}; ${i.user1Name} llevó más ${i.topCategoryUser1?.name ?? 'otras'}. Reparto por tiempo: ${i.timePctUser1}/${100 - i.timePctUser1}.`,
  (i) => `${i.user1Name} y ${i.user2Name} tuvieron un mes ${i.timePctUser1 >= 45 && i.timePctUser1 <= 55 ? 'equilibrado' : 'desequilibrado'}. ${i.topCategoryUser1?.name ?? 'Otros'} lidera para ${i.user1Name}, ${i.topCategoryUser2?.name ?? 'otros'} para ${i.user2Name}.`,
  (i) => `Análisis del mes: ${Math.abs(50 - i.timePctUser1)}% de diferencia en tiempo invertido. ${i.topCategoryUser1?.name ? `Tu fuerte: ${i.topCategoryUser1.name}.` : ''} ${i.topCategoryUser2?.name ? `El de ${i.user2Name}: ${i.topCategoryUser2.name}.` : ''}`,
]

export function generateInsight(input: InsightInput, seed: number): InsightOutput {
  const tpl = TEMPLATES[seed % TEMPLATES.length]
  const text = tpl(input)
  const bullets: InsightOutput['bullets'] = []
  if (input.equityDelta > 0) bullets.push({ tone: 'success', text: `+${input.equityDelta}% equidad` })
  else if (input.equityDelta < 0) bullets.push({ tone: 'warn', text: `${input.equityDelta}% equidad` })
  if (input.worstCategory) bullets.push({ tone: 'warn', text: `${input.worstCategory} desequilibrado` })
  if (input.timePctUser1 >= 45 && input.timePctUser1 <= 55) bullets.push({ tone: 'success', text: 'Reparto equilibrado' })
  return { text, bullets }
}
```

- [ ] **Step 2: Función en `analyticsService.ts`**

```ts
import { generateInsight } from './insightHeuristic.js'

// In-memory cache — invalidación natural al reiniciar el proceso
const insightCache = new Map<string, { at: number; data: any }>()
const INSIGHT_TTL = 6 * 60 * 60 * 1000  // 6h

export async function getMonthlyInsight(coupleId: string, month: number, year: number) {
  const key = `${coupleId}-${year}-${month}`
  const cached = insightCache.get(key)
  if (cached && Date.now() - cached.at < INSIGHT_TTL) return cached.data

  // Métricas del mes
  const start = new Date(year, month - 1, 1)
  const end   = new Date(year, month,   1)
  const prevStart = new Date(year, month - 2, 1)
  const prevEnd   = new Date(year, month - 1, 1)

  const couple = await prisma.couple.findUnique({
    where: { id: coupleId },
    include: { users: true },
  })
  if (!couple || couple.users.length < 1) {
    return { text: 'Aún no hay datos suficientes este mes.', bullets: [] }
  }
  const [u1, u2] = couple.users

  // Top category por user
  async function topCat(userId: string) {
    const logs = await prisma.taskLog.findMany({
      where: { coupleId, completedBy: userId, date: { gte: start, lt: end } },
      include: { task: true },
    })
    const counts = new Map<string, number>()
    logs.forEach(l => counts.set(l.task.category, (counts.get(l.task.category) ?? 0) + 1))
    const sorted = Array.from(counts.entries()).sort((a, b) => b[1] - a[1])
    return sorted[0] ? { name: sorted[0][0], count: sorted[0][1] } : null
  }

  // Time split
  const time = await getTimeInvested(coupleId, 'month')
  const totalH = (time.you?.hours ?? 0) + (time.partner?.hours ?? 0)
  const timePctUser1 = totalH > 0 ? Math.round(((time.you?.hours ?? 0) / totalH) * 100) : 50

  // Equity current vs prev
  const curr = await prisma.coupleScore.findFirst({
    where: { coupleId, weekStartDate: { gte: start, lt: end } },
    orderBy: { weekStartDate: 'desc' },
  })
  const prev = await prisma.coupleScore.findFirst({
    where: { coupleId, weekStartDate: { gte: prevStart, lt: prevEnd } },
    orderBy: { weekStartDate: 'desc' },
  })
  const equityDelta = curr && prev ? Math.round(Number(curr.equilibrium) - Number(prev.equilibrium)) : 0

  // Worst category
  const allLogs = await prisma.taskLog.findMany({
    where: { coupleId, date: { gte: start, lt: end } },
    include: { task: true },
  })
  const byCat = new Map<string, { u1: number; u2: number }>()
  allLogs.forEach(l => {
    const cur = byCat.get(l.task.category) ?? { u1: 0, u2: 0 }
    if (l.completedBy === u1.id) cur.u1++
    else if (u2 && l.completedBy === u2.id) cur.u2++
    byCat.set(l.task.category, cur)
  })
  let worst: string | null = null
  let worstDiff = 0
  byCat.forEach((v, k) => {
    const diff = Math.abs(v.u1 - v.u2)
    if (diff > worstDiff) { worstDiff = diff; worst = k }
  })
  if (worstDiff < 3) worst = null

  const result = generateInsight({
    user1Name: u1.name,
    user2Name: u2?.name ?? 'tu pareja',
    topCategoryUser1: await topCat(u1.id),
    topCategoryUser2: u2 ? await topCat(u2.id) : null,
    timePctUser1,
    equityDelta,
    worstCategory: worst,
  }, month)

  insightCache.set(key, { at: Date.now(), data: result })
  return result
}
```

- [ ] **Step 3: Route**

```ts
router.get('/insight', async (req, res) => {
  try {
    const coupleId = (req as any).coupleId
    const month = Number(req.query.month ?? new Date().getMonth() + 1)
    const year  = Number(req.query.year  ?? new Date().getFullYear())
    const data = await analyticsService.getMonthlyInsight(coupleId, month, year)
    res.json({ success: true, data })
  } catch (e) {
    res.status(500).json({ error: e instanceof Error ? e.message : 'Error' })
  }
})
```

- [ ] **Step 4: Smoke + commit**

```bash
cd src/backend && npm run dev &
sleep 3
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/analytics/insight?month=4&year=2026"
kill %1
git add src/backend/src/services/insightHeuristic.ts src/backend/src/services/analyticsService.ts src/backend/src/routes/analytics.ts
git commit -m "feat(analytics): endpoint /insight heurístico con templates y cache 6h"
```

### Task 1.7: Extensión `/points-by-category?groupByUser=true`

**Files:**
- Modify: `src/backend/src/services/analyticsService.ts`
- Modify: `src/backend/src/routes/analytics.ts`

- [ ] **Step 1: Añadir overload en el service**

En `analyticsService.ts` añadir (o extender la función existente):

```ts
export async function getPointsByCategoryGrouped(coupleId: string, start?: Date, end?: Date) {
  const couple = await prisma.couple.findUnique({
    where: { id: coupleId },
    include: { users: true },
  })
  if (!couple) return {}
  const [u1, u2] = couple.users
  const where: any = { coupleId }
  if (start) where.date = { ...(where.date ?? {}), gte: start }
  if (end)   where.date = { ...(where.date ?? {}), lte: end }

  const logs = await prisma.taskLog.findMany({
    where,
    include: { task: true },
  })
  const byCat: Record<string, { you: number; partner: number }> = {}
  for (const l of logs) {
    const cat = l.task.category
    if (!byCat[cat]) byCat[cat] = { you: 0, partner: 0 }
    const pts = Number(l.pointsFinal ?? 0)
    if (l.completedBy === u1.id) byCat[cat].you += pts
    else if (u2 && l.completedBy === u2.id) byCat[cat].partner += pts
  }
  return byCat
}
```

- [ ] **Step 2: Modificar route existente**

Dentro del handler de `GET /points-by-category` añadir el branch `groupByUser=true`:

```ts
router.get('/points-by-category', async (req, res) => {
  try {
    const coupleId = (req as any).coupleId
    const start = req.query.start ? new Date(String(req.query.start)) : undefined
    const end   = req.query.end   ? new Date(String(req.query.end))   : undefined
    const grouped = req.query.groupByUser === 'true'
    const data = grouped
      ? await analyticsService.getPointsByCategoryGrouped(coupleId, start, end)
      : await analyticsService.getPointsByCategory(coupleId, start, end)
    res.json({ success: true, data })
  } catch (e) {
    res.status(500).json({ error: e instanceof Error ? e.message : 'Error' })
  }
})
```

- [ ] **Step 3: Smoke + commit**

```bash
curl -s -H "Authorization: Bearer $TOKEN" "http://localhost:3000/api/analytics/points-by-category?groupByUser=true"
git add src/backend/src/services/analyticsService.ts src/backend/src/routes/analytics.ts
git commit -m "feat(analytics): points-by-category soporta ?groupByUser=true"
```

---

## Phase 2 — Navigation shell (Header + Bottom nav + FAB)

### Task 2.1: `AppHeader`

**Files:**
- Create: `src/frontend/src/components/v2/layout/AppHeader.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { Bell } from 'lucide-react'
import { Avatar } from '../primitives/Avatar'

interface Props {
  userName: string
  userAvatarEmoji?: string
  userAvatarColor?: string
  userMood?: string | null
  partnerMood?: string | null
  partnerName?: string | null
  hasUnreadNotif?: boolean
  onBell: () => void
  onMenu: () => void
  onAvatar?: () => void
}

export function AppHeader({
  userName, userAvatarEmoji, userAvatarColor, userMood,
  partnerMood, partnerName, hasUnreadNotif, onBell, onMenu, onAvatar,
}: Props) {
  const hour = new Date().getHours()
  const greeting = hour < 12 ? 'Buenos días' : hour < 20 ? 'Buenas tardes' : 'Buenas noches'
  const emoji    = hour < 12 ? '☀️'          : hour < 20 ? '🌤️'           : '🌙'

  return (
    <header className="sticky top-0 z-40 bg-[rgba(15,10,30,0.95)] backdrop-blur-md border-b border-brd-subtle px-4 py-3 flex items-center justify-between">
      <div className="min-w-0">
        <p className="text-xs font-medium text-text-secondary m-0">{greeting} {emoji}</p>
        <h1 className="text-[22px] font-extrabold text-text-primary tracking-tight m-0 mt-0.5">{userName.split(' ')[0]}</h1>
        {partnerMood && partnerName && (
          <p className="text-[11px] text-text-secondary mt-0.5 m-0">{partnerName} está {partnerMood} hoy</p>
        )}
      </div>
      <div className="flex items-center gap-2">
        <button
          onClick={onBell}
          aria-label="Notificaciones"
          className="relative w-9 h-9 rounded-md bg-surface-muted border border-brd-purple flex items-center justify-center text-text-primary"
        >
          <Bell size={16} />
          {hasUnreadNotif && (
            <span className="absolute top-1.5 right-1.5 w-[7px] h-[7px] rounded-full bg-danger border-[1.5px] border-bg-page" />
          )}
        </button>
        <button
          onClick={onMenu}
          aria-label="Más"
          className="w-9 h-9 rounded-md bg-surface-muted border border-brd-purple text-text-primary font-bold tracking-widest flex items-center justify-center"
        >⋯</button>
        <button onClick={onAvatar} aria-label="Mi perfil">
          <Avatar emoji={userAvatarEmoji ?? '🐼'} color={userAvatarColor ?? '#7c3aed'} mood={userMood} size="lg" />
        </button>
      </div>
    </header>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/layout/AppHeader.tsx
git commit -m "feat(v2): AppHeader con saludo temporal y mood partner"
```

### Task 2.2: `HeaderMenu` (⋯ dropdown)

**Files:**
- Create: `src/frontend/src/components/v2/layout/HeaderMenu.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'

interface Props {
  open: boolean
  onClose: () => void
  unlockedCount?: number
  totalAchievements?: number
  partnerName?: string | null
  onLogout: () => void
}

export function HeaderMenu({ open, onClose, unlockedCount = 0, totalAchievements = 0, partnerName, onLogout }: Props) {
  const nav = useNavigate()
  if (!open) return null

  const items = [
    { id: 'achievements', emoji: '🏆', title: 'Logros',  desc: `${unlockedCount}/${totalAchievements} desbloqueados`, to: '/achievements' },
    { id: 'profile',      emoji: '👤', title: 'Perfil',  desc: 'Mi cuenta',                                           to: '/settings/profile' },
    { id: 'partner',      emoji: '💕', title: 'Pareja',  desc: partnerName ? `${partnerName} · vinculado` : 'Sin pareja', to: '/settings/partner' },
    { id: 'rules',        emoji: '📜', title: 'Reglas',  desc: 'Puntos y multiplicadores',                            to: '/settings/rules' },
    { id: 'settings',     emoji: '⚙️', title: 'Ajustes', desc: 'Notificaciones, idioma',                              to: '/settings' },
    { id: 'help',         emoji: '❓', title: 'Ayuda',   desc: 'Cómo funciona',                                        to: '/help' },
  ]

  function go(to: string) { nav(to); onClose() }

  return (
    <>
      <div onClick={onClose} className="fixed inset-0 bg-black/55 backdrop-blur-sm z-[58]" />
      <div className="fixed top-[68px] right-4 z-[59] w-[260px] bg-[rgba(26,16,53,0.98)] border border-brd-purple rounded-lg shadow-2xl overflow-hidden animate-in fade-in slide-in-from-top-2 duration-150">
        {items.map((it, i) => (
          <button
            key={it.id}
            onClick={() => go(it.to)}
            className={`flex items-center gap-2.5 w-full px-3 py-3 text-left hover:bg-surface-muted transition ${i > 0 ? 'border-t border-brd-subtle' : ''}`}
          >
            <span className="text-xl w-7 text-center">{it.emoji}</span>
            <div className="flex-1 min-w-0">
              <div className="text-sm font-bold text-text-primary">{it.title}</div>
              <div className="text-[10px] text-text-secondary mt-px">{it.desc}</div>
            </div>
            <span className="text-text-tertiary text-sm">›</span>
          </button>
        ))}
        <button
          onClick={() => { onLogout(); onClose() }}
          className="flex items-center gap-2.5 w-full px-3 py-3 bg-danger/10 border-t border-danger/20 text-left hover:bg-danger/20 transition"
        >
          <span className="text-xl w-7 text-center">🚪</span>
          <span className="text-sm font-bold text-danger">Cerrar sesión</span>
        </button>
      </div>
    </>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/layout/HeaderMenu.tsx
git commit -m "feat(v2): HeaderMenu dropdown 6 items + logout"
```

### Task 2.3: `BottomNav` v2 (5 slots con FAB central amber)

**Files:**
- Create: `src/frontend/src/components/v2/layout/BottomNav.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate, useLocation } from 'react-router-dom'
import { Home, CheckSquare, Calendar, BarChart3, Plus } from 'lucide-react'

interface Props {
  onFab: () => void
}

const LEFT = [
  { id: 'dashboard', label: 'Inicio',     icon: Home,       to: '/dashboard' },
  { id: 'tasks',     label: 'Tareas',     icon: CheckSquare, to: '/tasks' },
]
const RIGHT = [
  { id: 'calendar',  label: 'Calendario', icon: Calendar,   to: '/calendar' },
  { id: 'analytics', label: 'Analítica',  icon: BarChart3,  to: '/analytics' },
]

export function BottomNav({ onFab }: Props) {
  const nav = useNavigate()
  const loc = useLocation()

  function renderItem(it: typeof LEFT[number]) {
    const active = loc.pathname === it.to || loc.pathname.startsWith(it.to + '/')
    const Icon = it.icon
    return (
      <button key={it.id} onClick={() => nav(it.to)} className="flex flex-col items-center gap-1 px-2 bg-transparent border-0">
        <Icon size={18} className={active ? 'text-brand-amber' : 'text-text-tertiary'} />
        <span className={`text-[9px] font-semibold ${active ? 'text-brand-amber' : 'text-text-tertiary'}`}>{it.label}</span>
      </button>
    )
  }

  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 bg-surface-elevated border-t border-brd-subtle backdrop-blur-md max-w-[500px] mx-auto flex items-center justify-around py-2 pb-3">
      {LEFT.map(renderItem)}
      <button
        onClick={onFab}
        aria-label="Añadir"
        className="w-11 h-11 rounded-full bg-grad-cta text-white text-2xl -mt-5 shadow-lg shadow-brand-amber/40 flex items-center justify-center"
      >
        <Plus size={22} strokeWidth={3} />
      </button>
      {RIGHT.map(renderItem)}
    </nav>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/layout/BottomNav.tsx
git commit -m "feat(v2): BottomNav 5 slots con FAB amber central"
```

### Task 2.4: `FabActionSheet` (📅/🛒/📝)

**Files:**
- Create: `src/frontend/src/components/v2/layout/FabActionSheet.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'
import { BottomSheet } from '../primitives/BottomSheet'

interface Props {
  open: boolean
  onClose: () => void
}

export function FabActionSheet({ open, onClose }: Props) {
  const nav = useNavigate()
  function go(path: string, state?: any) { nav(path, { state }); onClose() }

  const opts = [
    { emoji: '📅', title: 'Nueva actividad', desc: 'Propón una actividad que negociar', tone: 'amber',  onClick: () => go('/request-activity') },
    { emoji: '🛒', title: 'Añadir a la compra', desc: 'Item para la lista de la compra', tone: 'purple', onClick: () => go('/shopping', { autoFocus: true }) },
    { emoji: '📝', title: 'Nuevo to-do',       desc: 'Recordatorio personal sin puntos',  tone: 'indigo', onClick: () => go('/todos',    { autoFocus: true }) },
  ]

  const toneColors: Record<string, string> = {
    amber:  'bg-brand-amber/15  border-brand-amber/30  text-brand-amber',
    purple: 'bg-brand-purple/15 border-brand-purple/30 text-brand-purple',
    indigo: 'bg-brand-indigo/15 border-brand-indigo/30 text-[#a5b4fc]',
  }

  return (
    <BottomSheet open={open} onClose={onClose} title="¿Qué quieres crear?">
      <div className="flex flex-col gap-2">
        {opts.map(o => (
          <button
            key={o.title}
            onClick={o.onClick}
            className={`flex items-center gap-3 px-3 py-3 rounded-md border ${toneColors[o.tone]} hover:opacity-90 transition text-left`}
          >
            <span className="text-2xl w-10 text-center">{o.emoji}</span>
            <div className="flex-1 min-w-0">
              <div className="text-sm font-bold text-text-primary">{o.title}</div>
              <div className="text-[11px] text-text-secondary mt-0.5">{o.desc}</div>
            </div>
            <span className="text-text-tertiary text-lg">›</span>
          </button>
        ))}
      </div>
    </BottomSheet>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/layout/FabActionSheet.tsx
git commit -m "feat(v2): FabActionSheet con 3 opciones (actividad/compra/todo)"
```

### Task 2.5: Shell global en App — integrar nav

**Files:**
- Modify: `src/frontend/src/App.tsx` (o donde estén las rutas + nav)

- [ ] **Step 1: Localizar donde se monta `BottomNav` actualmente**

```bash
grep -rn "BottomNav" src/frontend/src --include="*.tsx"
```

- [ ] **Step 2: Crear `layout/AuthedLayout.tsx`**

```tsx
import { useState, ReactNode } from 'react'
import { useNavigate } from 'react-router-dom'
import { AppHeader } from '../components/v2/layout/AppHeader'
import { BottomNav } from '../components/v2/layout/BottomNav'
import { HeaderMenu } from '../components/v2/layout/HeaderMenu'
import { FabActionSheet } from '../components/v2/layout/FabActionSheet'
import { useAppStore } from '../store/useAppStore'

export function AuthedLayout({ children }: { children: ReactNode }) {
  const [menuOpen, setMenuOpen] = useState(false)
  const [fabOpen,  setFabOpen]  = useState(false)
  const { user, logout } = useAppStore()
  const nav = useNavigate()

  if (!user) return null

  return (
    <div className="min-h-screen pb-20 max-w-[500px] mx-auto">
      <AppHeader
        userName={user.name}
        userAvatarEmoji={user.avatarEmoji}
        userAvatarColor={user.avatarColor}
        userMood={user.mood}
        partnerMood={user.partner?.mood ?? null}
        partnerName={user.partner?.name ?? null}
        hasUnreadNotif={false}
        onBell={() => nav('/notifications')}
        onMenu={() => setMenuOpen(true)}
        onAvatar={() => nav('/settings/profile')}
      />
      <HeaderMenu
        open={menuOpen}
        onClose={() => setMenuOpen(false)}
        partnerName={user.partner?.name ?? null}
        onLogout={() => { logout(); nav('/login') }}
      />
      {children}
      <BottomNav onFab={() => setFabOpen(true)} />
      <FabActionSheet open={fabOpen} onClose={() => setFabOpen(false)} />
    </div>
  )
}
```

- [ ] **Step 3: Envolver las rutas protegidas con `AuthedLayout`**

En `App.tsx` (o donde estén definidos los `<Route>`) reemplazar así:

```tsx
<Route path="/dashboard" element={<AuthedLayout><Dashboard /></AuthedLayout>} />
<Route path="/tasks"     element={<AuthedLayout><Tasks /></AuthedLayout>} />
<Route path="/calendar"  element={<AuthedLayout><Calendar /></AuthedLayout>} />
<Route path="/analytics" element={<AuthedLayout><Analytics /></AuthedLayout>} />
<Route path="/shopping"  element={<AuthedLayout><ShoppingListPage /></AuthedLayout>} />
<Route path="/todos"     element={<AuthedLayout><TodoListPage /></AuthedLayout>} />
<Route path="/request-activity" element={<AuthedLayout><RequestActivity /></AuthedLayout>} />
<Route path="/request-inbox"    element={<AuthedLayout><RequestInbox /></AuthedLayout>} />
<Route path="/achievements"     element={<AuthedLayout><Achievements /></AuthedLayout>} />
<Route path="/settings" element={<AuthedLayout><Settings /></AuthedLayout>} />
<Route path="/settings/:section" element={<AuthedLayout><Settings /></AuthedLayout>} />
<Route path="/history"  element={<Navigate to="/analytics?tab=movements" replace />} />
```

Dejar Login, Signup, Onboarding sin layout (son cold screens).

- [ ] **Step 4: Smoke visual**

```bash
cd src/frontend && npm run dev &
sleep 4
open http://localhost:5173
```

Verificar manualmente:
- [ ] Bottom nav se ve con 5 slots y FAB amber central
- [ ] Click en cada slot navega a la página correspondiente
- [ ] Click en ⋯ abre menú
- [ ] Click en FAB abre sheet con 3 opciones
- [ ] Cada opción del sheet navega correcto

```bash
kill %1
```

- [ ] **Step 5: Commit**

```bash
git add src/frontend/src/layout/ src/frontend/src/App.tsx
git commit -m "feat(v2): AuthedLayout con header+bottomnav+fab integrados"
```

---

## Phase 3 — Dashboard v2

### Task 3.1: `DailyPhrase` v2

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/DailyPhrase.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { getDailyPhrase } from '../../../utils/dailyPhrase'

export function DailyPhrase() {
  const phrase = getDailyPhrase()
  return (
    <div className="mx-4 mb-3.5 px-3 py-2 rounded-md bg-brand-purple/10 border border-brand-purple/15 text-xs italic text-[#c4b5fd] text-center">
      {phrase}
    </div>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/dashboard/DailyPhrase.tsx
git commit -m "feat(v2): DailyPhrase compact strip"
```

### Task 3.2: `BalanceLevelHero`

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/BalanceLevelHero.tsx`

- [ ] **Step 1: Implementar**

```tsx
interface Props {
  youName: string
  youBalance: number
  partnerName: string
  partnerBalance: number
  level: number
  levelName: string
  current: number
  needed: number
}

export function BalanceLevelHero({
  youName, youBalance, partnerName, partnerBalance,
  level, levelName, current, needed,
}: Props) {
  const lead   = youBalance > 0.5
  const behind = youBalance < -0.5
  const absBal = Math.abs(youBalance)
  const leadLabel = lead      ? `Vas ${absBal.toFixed(1)} MP por delante`
                  : behind    ? `${partnerName} va ${absBal.toFixed(1)} MP por delante`
                  : 'Estáis empatados'
  const pct = needed > 0 ? Math.min(100, (current / needed) * 100) : 0

  return (
    <div className="mx-4 mb-3.5 p-4 pb-3.5 rounded-xl bg-grad-hero shadow-xl shadow-brand-indigo/30">
      <div className="flex items-start justify-between gap-3">
        <div className="min-w-0">
          <p className="m-0 text-[11px] font-semibold tracking-wide text-[rgba(199,210,254,0.9)]">BALANCE DE LA SEMANA</p>
          <p className="m-0 mt-1.5 text-[34px] font-extrabold text-white leading-none tabular-nums tracking-tight">
            {youBalance >= 0 ? '+' : ''}{youBalance.toFixed(1)}
            <span className="text-base font-medium opacity-80 ml-1">MP</span>
          </p>
          <p className="m-0 mt-1.5 text-[13px] font-medium text-white/95">
            {lead ? '🎉 ' : behind ? '💪 ' : '🤝 '}{leadLabel}
          </p>
        </div>
        <div className="text-right flex-shrink-0">
          <div className="text-[10px] font-semibold text-[rgba(199,210,254,0.85)]">{youName}</div>
          <div className="text-[15px] font-bold text-white tabular-nums">{youBalance >= 0 ? '+' : ''}{youBalance.toFixed(1)}</div>
          <div className="text-[10px] font-semibold text-[rgba(199,210,254,0.85)] mt-1">{partnerName}</div>
          <div className="text-[15px] font-bold text-white tabular-nums">{partnerBalance >= 0 ? '+' : ''}{partnerBalance.toFixed(1)}</div>
        </div>
      </div>
      <div className="mt-3 pt-3 border-t border-white/20">
        <div className="flex items-center justify-between mb-1.5">
          <div className="text-[11px] font-semibold text-[rgba(199,210,254,0.9)]">👑 Nivel {level} · {levelName}</div>
          <div className="text-[11px] font-bold text-white tabular-nums">{current}/{needed}</div>
        </div>
        <div className="relative h-1.5 bg-black/20 rounded-full overflow-hidden">
          <div className="absolute inset-y-0 left-0 bg-white rounded-full transition-all" style={{ width: `${pct}%` }} />
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/dashboard/BalanceLevelHero.tsx
git commit -m "feat(v2): BalanceLevelHero fusionado (balance + nivel)"
```

### Task 3.3: `StreakStrip`

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/StreakStrip.tsx`

- [ ] **Step 1: Implementar**

```tsx
interface Props {
  streakDays: number
  multiplier: number
  freezes: number
  maxFreezes?: number
}

export function StreakStrip({ streakDays, multiplier, freezes, maxFreezes = 1 }: Props) {
  if (streakDays === 0) {
    return (
      <div className="mx-4 mb-3.5 px-3 py-2.5 rounded-md bg-brand-purple/8 border border-brand-purple/15 text-center text-xs text-text-secondary">
        💤 Empieza una racha hoy
      </div>
    )
  }
  return (
    <div className="mx-4 mb-3.5 px-3 py-2.5 rounded-md bg-brand-purple/8 border border-brand-purple/15 flex items-center justify-around text-xs font-semibold text-text-primary">
      <span>🔥 <span className="tabular-nums">{streakDays}</span> días</span>
      <span className="text-text-secondary">·</span>
      <span>×{multiplier.toFixed(1)}</span>
      <span className="text-text-secondary">·</span>
      <span>🧊 {freezes}/{maxFreezes}</span>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/dashboard/StreakStrip.tsx
git commit -m "feat(v2): StreakStrip compacto"
```

### Task 3.4: `TodayTasksSection`

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/TodayTasksSection.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'
import { Button } from '../primitives/Button'
import { Card } from '../primitives/Card'

interface Task {
  id: string
  name: string
  category: string
  categoryEmoji: string
  pointsExpected: number
  assignee: 'me' | 'partner' | 'free'
  partnerName?: string
}

interface Props {
  tasks: Task[]
  onComplete: (taskId: string) => void
  partnerName: string
}

export function TodayTasksSection({ tasks, onComplete, partnerName }: Props) {
  const nav = useNavigate()

  if (tasks.length === 0) {
    return (
      <Card className="mx-4 mb-3.5 text-center py-5">
        <div className="text-3xl mb-1">🎉</div>
        <div className="text-sm font-bold text-text-primary">Día libre</div>
        <div className="text-[11px] text-text-secondary mt-1">Has completado todo — disfrutad el resto del día 💕</div>
      </Card>
    )
  }

  return (
    <div className="mx-4 mb-3.5">
      <div className="flex items-center justify-between mb-2">
        <h3 className="text-sm font-bold text-text-primary m-0">Hoy</h3>
        <span className="text-[11px] text-text-secondary">{tasks.length} pendientes</span>
      </div>
      <div className="flex flex-col gap-2">
        {tasks.slice(0, 3).map(t => (
          <Card key={t.id} className="p-3">
            <div className="flex items-center gap-3 mb-2.5">
              <div className="w-10 h-10 rounded-md bg-brand-purple/15 text-xl flex items-center justify-center flex-shrink-0">{t.categoryEmoji}</div>
              <div className="flex-1 min-w-0">
                <div className="text-sm font-bold text-text-primary truncate">{t.name}</div>
                <div className="text-[11px] text-text-secondary">+{t.pointsExpected.toFixed(1)} MP</div>
              </div>
            </div>
            {t.assignee === 'me' && (
              <Button variant="primary" fullWidth onClick={() => onComplete(t.id)}>✓ Marcar como hecha</Button>
            )}
            {t.assignee === 'partner' && (
              <Button variant="outline" fullWidth disabled>Esperando a {partnerName}</Button>
            )}
            {t.assignee === 'free' && (
              <Button variant="secondary" fullWidth onClick={() => onComplete(t.id)}>Hacerla yo</Button>
            )}
          </Card>
        ))}
        {tasks.length > 3 && (
          <button onClick={() => nav('/tasks')} className="text-xs text-brand-purple font-bold">Ver todas ({tasks.length}) →</button>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/dashboard/TodayTasksSection.tsx
git commit -m "feat(v2): TodayTasksSection con CTAs amber/purple/outline"
```

### Task 3.5: `RecentMovements`

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/RecentMovements.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'
import { Avatar } from '../primitives/Avatar'

interface Movement {
  id: string
  userName: string
  userAvatarEmoji?: string
  userAvatarColor?: string
  action: string
  delta: number
  when: string
}

interface Props { movements: Movement[] }

export function RecentMovements({ movements }: Props) {
  const nav = useNavigate()
  if (movements.length === 0) return null

  return (
    <div className="mx-4 mb-3.5">
      <h3 className="text-sm font-bold text-text-primary mb-2">Últimos movimientos</h3>
      <div className="rounded-md bg-[rgba(26,16,53,0.3)] overflow-hidden">
        {movements.slice(0, 3).map((m, i) => (
          <div key={m.id} className={`flex items-center gap-2 px-3 py-2.5 ${i > 0 ? 'border-t border-brd-subtle' : ''}`}>
            <Avatar emoji={m.userAvatarEmoji} color={m.userAvatarColor} size="sm" />
            <div className="flex-1 min-w-0 text-xs">
              <span className="text-text-primary font-semibold">{m.userName}</span>
              <span className="text-text-secondary"> · {m.action}</span>
            </div>
            <span className={`text-xs font-bold tabular-nums ${m.delta >= 0 ? 'text-success' : 'text-danger'}`}>
              {m.delta >= 0 ? '+' : ''}{m.delta.toFixed(1)} MP
            </span>
            <span className="text-[10px] text-text-tertiary">{m.when}</span>
          </div>
        ))}
      </div>
      <button
        onClick={() => nav('/analytics?tab=movements')}
        className="text-xs text-brand-purple font-bold mt-2"
      >Ver historial completo →</button>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/dashboard/RecentMovements.tsx
git commit -m "feat(v2): RecentMovements compact 3-items"
```

### Task 3.6: `ShoppingPreview` + `TodoPreview`

**Files:**
- Create: `src/frontend/src/components/v2/dashboard/QuickPreviews.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'
import { Card } from '../primitives/Card'

interface Props {
  shoppingPending: number
  todoPending: number
}

export function QuickPreviews({ shoppingPending, todoPending }: Props) {
  const nav = useNavigate()
  if (shoppingPending === 0 && todoPending === 0) return null

  return (
    <div className="mx-4 mb-3.5 grid grid-cols-2 gap-2">
      {shoppingPending > 0 && (
        <button onClick={() => nav('/shopping')} className="text-left">
          <Card className="hover:border-brand-amber/40 transition">
            <div className="text-xl mb-0.5">🛒</div>
            <div className="text-[11px] text-text-secondary">Compra</div>
            <div className="text-sm font-bold text-text-primary">{shoppingPending} pendientes</div>
          </Card>
        </button>
      )}
      {todoPending > 0 && (
        <button onClick={() => nav('/todos')} className="text-left">
          <Card className="hover:border-brand-purple/40 transition">
            <div className="text-xl mb-0.5">📝</div>
            <div className="text-[11px] text-text-secondary">To-dos</div>
            <div className="text-sm font-bold text-text-primary">{todoPending} por hacer</div>
          </Card>
        </button>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/dashboard/QuickPreviews.tsx
git commit -m "feat(v2): QuickPreviews Shopping + Todos (hidden if empty)"
```

### Task 3.7: Reescribir `pages/Dashboard.tsx` usando componentes v2

**Files:**
- Modify: `src/frontend/src/pages/Dashboard.tsx`

- [ ] **Step 1: Backup del actual (referencia)**

```bash
cp src/frontend/src/pages/Dashboard.tsx /tmp/Dashboard.v1.tsx.bak
```

- [ ] **Step 2: Reescribir Dashboard**

Sustituir todo el contenido de `src/frontend/src/pages/Dashboard.tsx` por:

```tsx
import { useQuery } from '@tanstack/react-query'
import { apiClient } from '../services/apiClient'
import { useAppStore } from '../store/useAppStore'
import { DailyPhrase } from '../components/v2/dashboard/DailyPhrase'
import { BalanceLevelHero } from '../components/v2/dashboard/BalanceLevelHero'
import { StreakStrip } from '../components/v2/dashboard/StreakStrip'
import { TodayTasksSection } from '../components/v2/dashboard/TodayTasksSection'
import { RecentMovements } from '../components/v2/dashboard/RecentMovements'
import { QuickPreviews } from '../components/v2/dashboard/QuickPreviews'
import { useShoppingList } from '../hooks/useShoppingList'
import { useTodos } from '../hooks/useTodos'

export default function Dashboard() {
  const { user, couple } = useAppStore()

  const { data: balance } = useQuery({
    queryKey: ['balance'],
    queryFn: async () => (await apiClient.get('/api/points/balance')).data,
  })
  const { data: todayTasks = [] } = useQuery({
    queryKey: ['today-tasks'],
    queryFn: async () => (await apiClient.get('/api/tasks?scope=today')).data.data ?? [],
  })
  const { data: movements = [] } = useQuery({
    queryKey: ['recent-movements'],
    queryFn: async () => (await apiClient.get('/api/points/history?limit=3')).data.data ?? [],
  })
  const { data: streak } = useQuery({
    queryKey: ['streak'],
    queryFn: async () => (await apiClient.get('/api/achievements/streak')).data.data ?? { days: 0, multiplier: 1.0, freezes: 1 },
  })
  const { data: levelData } = useQuery({
    queryKey: ['couple-level'],
    queryFn: async () => (await apiClient.get('/api/achievements/level')).data.data ?? { level: 1, levelName: 'Nido', current: 0, needed: 100 },
  })
  const { data: shoppingData } = useShoppingList()
  const { data: todosData }    = useTodos()

  async function handleComplete(taskId: string) {
    await apiClient.post(`/api/tasks/${taskId}/complete`)
    // Invalidar queries para refrescar
    // (con QueryClient singleton, simplificado con refetch manual)
  }

  if (!user || !couple) return null

  const you     = balance?.you     ?? { name: user.name, balance: 0 }
  const partner = balance?.partner ?? { name: 'Tu pareja', balance: 0 }

  const pendingShopping = shoppingData?.active?.items.filter((i: any) => !i.isChecked).length ?? 0
  const pendingTodos    = todosData?.mine.filter((t: any) => !t.isCompleted).length ?? 0

  return (
    <main>
      <DailyPhrase />
      <BalanceLevelHero
        youName={you.name}
        youBalance={Number(you.balance)}
        partnerName={partner.name}
        partnerBalance={Number(partner.balance)}
        level={levelData?.level ?? 1}
        levelName={levelData?.levelName ?? 'Nido'}
        current={levelData?.current ?? 0}
        needed={levelData?.needed ?? 100}
      />
      <StreakStrip
        streakDays={streak?.days ?? 0}
        multiplier={Number(streak?.multiplier ?? 1.0)}
        freezes={streak?.freezes ?? 1}
      />
      <TodayTasksSection
        tasks={todayTasks}
        onComplete={handleComplete}
        partnerName={partner.name}
      />
      <RecentMovements movements={movements} />
      <QuickPreviews shoppingPending={pendingShopping} todoPending={pendingTodos} />
    </main>
  )
}
```

- [ ] **Step 3: Smoke visual**

```bash
cd src/frontend && npm run dev &
sleep 4
```

Abrir `http://localhost:5173/dashboard` y verificar:
- [ ] DailyPhrase aparece arriba
- [ ] BalanceLevelHero con gradient y valores
- [ ] StreakStrip compacto
- [ ] TodayTasks con CTAs correctos
- [ ] RecentMovements si hay
- [ ] No aparece el chart 30d (se movió a Analytics)
- [ ] No hay AchievementsWidget (se accede desde ⋯)

```bash
kill %1
```

- [ ] **Step 4: Commit**

```bash
git add src/frontend/src/pages/Dashboard.tsx
git commit -m "feat(dashboard): reescribir con componentes v2 condensados"
```

---

## Phase 4 — Analytics screen (3 tabs + Premium)

### Task 4.1: `PremiumInterestModal`

**Files:**
- Create: `src/frontend/src/components/v2/premium/PremiumInterestModal.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useState } from 'react'
import { BottomSheet } from '../primitives/BottomSheet'
import { Button } from '../primitives/Button'
import { Input } from '../primitives/Input'
import { apiClient } from '../../../services/apiClient'
import { useAppStore } from '../../../store/useAppStore'

interface Props {
  open: boolean
  onClose: () => void
  source: 'analytics_advanced_overlay' | 'settings_premium_cta' | 'onboarding'
}

export function PremiumInterestModal({ open, onClose, source }: Props) {
  const { user } = useAppStore()
  const [email, setEmail]     = useState(user?.email ?? '')
  const [loading, setLoading] = useState(false)
  const [done, setDone]       = useState(false)
  const [err, setErr]         = useState<string | null>(null)

  async function submit() {
    setLoading(true); setErr(null)
    try {
      await apiClient.post('/api/premium/interest', { email, source })
      setDone(true)
    } catch (e: any) {
      if (e?.response?.status === 409) setDone(true)  // Ya estaba apuntado, es éxito silencioso
      else setErr(e?.response?.data?.error ?? 'Error')
    } finally { setLoading(false) }
  }

  return (
    <BottomSheet open={open} onClose={onClose}>
      <div className="text-center">
        <div className="w-14 h-14 rounded-full bg-grad-cta mx-auto mb-2 flex items-center justify-center text-3xl">👑</div>
        <h3 className="text-lg font-extrabold text-text-primary mb-1">Desbloquea Premium · Próximamente</h3>
        <p className="text-xs text-text-secondary mb-3">Matripuntos Premium está en camino. Queremos lanzarlo bien.</p>
        <ul className="text-left text-xs text-text-primary my-3 space-y-1">
          <li>✅ Rondas de negociación ilimitadas</li>
          <li>✅ Analítica avanzada completa</li>
          <li>✅ Histórico sin límite</li>
          <li>✅ Badge 👑 en tu perfil</li>
        </ul>
        {done ? (
          <div className="text-sm font-bold text-success my-4">¡Listo! Te avisaremos 💕</div>
        ) : (
          <>
            <div className="text-xs text-text-secondary mb-1.5">Te avisamos cuando esté disponible</div>
            <Input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="tu@email.com" />
            {err && <div className="text-[11px] text-danger mt-1">{err}</div>}
            <Button variant="primary" fullWidth onClick={submit} disabled={!email.includes('@') || loading} className="mt-3">
              {loading ? 'Enviando…' : 'Avísame'}
            </Button>
            <p className="text-[10px] text-text-tertiary mt-2">ℹ Sin compromiso · Sin spam</p>
          </>
        )}
      </div>
    </BottomSheet>
  )
}
```

- [ ] **Step 2: Commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/premium/PremiumInterestModal.tsx
git commit -m "feat(premium): PremiumInterestModal con captura email"
```

### Task 4.2: Charts básicos — `WeeklyBarsChart`

**Files:**
- Create: `src/frontend/src/components/v2/analytics/charts/WeeklyBarsChart.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { Card } from '../../primitives/Card'

interface Day { label: string; you: number; partner: number }
interface Props { days: Day[]; youName: string; partnerName: string }

export function WeeklyBarsChart({ days, youName, partnerName }: Props) {
  const max = Math.max(1, ...days.flatMap(d => [d.you, d.partner]))
  const youTotal     = days.reduce((s, d) => s + d.you,     0)
  const partnerTotal = days.reduce((s, d) => s + d.partner, 0)

  return (
    <div className="mx-4 mb-3.5">
      <div className="flex items-center justify-between mb-2.5">
        <div>
          <div className="text-sm font-bold text-text-primary">📊 Puntos por día</div>
          <div className="text-[11px] text-text-secondary mt-0.5">{youName} vs {partnerName} · esta semana</div>
        </div>
        <div className="flex gap-2.5 text-[10px]">
          <span className="text-brand-purple font-semibold">● {youName}</span>
          <span className="text-brand-amber  font-semibold">● {partnerName}</span>
        </div>
      </div>
      <Card>
        <div className="flex items-end gap-1.5 h-[110px] mb-2">
          {days.map(d => (
            <div key={d.label} className="flex-1 flex flex-col items-center gap-1">
              <div className="flex-1 flex items-end gap-0.5 w-full">
                <div
                  className="flex-1 rounded-t-sm bg-gradient-to-b from-brand-purple to-brand-purple-dark"
                  style={{ height: `${(d.you / max) * 100}%`, minHeight: d.you > 0 ? 2 : 0 }}
                />
                <div
                  className="flex-1 rounded-t-sm bg-gradient-to-b from-[#fbbf24] to-brand-amber-dark"
                  style={{ height: `${(d.partner / max) * 100}%`, minHeight: d.partner > 0 ? 2 : 0 }}
                />
              </div>
              <div className="text-[10px] font-semibold text-text-secondary">{d.label}</div>
            </div>
          ))}
        </div>
        <div className="flex justify-between pt-2.5 border-t border-brd-subtle">
          <div>
            <div className="text-[10px] text-text-secondary">{youName} · total</div>
            <div className="text-lg font-extrabold text-brand-purple tabular-nums">{youTotal} MP</div>
          </div>
          <div className="text-right">
            <div className="text-[10px] text-text-secondary">{partnerName} · total</div>
            <div className="text-lg font-extrabold text-brand-amber tabular-nums">{partnerTotal} MP</div>
          </div>
        </div>
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/analytics/charts/WeeklyBarsChart.tsx
git commit -m "feat(analytics): WeeklyBarsChart (puntos por día)"
```

### Task 4.3: Charts básicos — `CategoryPieChart`, `BalanceEvolutionChart`, `TimeInvestedChart`

**Files:**
- Create: `src/frontend/src/components/v2/analytics/charts/CategoryPieChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/BalanceEvolutionChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/TimeInvestedChart.tsx`

Referencia completa en `/tmp/claude-design-v2/matripuntos-design-system/project/ui_kits/app/Analytics.jsx` líneas 128-268. Transcribir cada función JSX a TSX manteniendo la misma estructura SVG y paddings. Las props cambian a:

- [ ] **Step 1: `CategoryPieChart.tsx`**

```tsx
import { Card } from '../../primitives/Card'

interface Category { name: string; value: number; color: string; emoji: string }
interface Props { categories: Category[] }

export function CategoryPieChart({ categories }: Props) {
  if (categories.length === 0) {
    return (
      <div className="mx-4 mb-3.5">
        <Card className="text-center text-text-secondary text-xs py-6">
          🥧 Aún no hay datos suficientes para la distribución por categorías.
        </Card>
      </div>
    )
  }
  const total = categories.reduce((s, c) => s + c.value, 0)
  let cum = 0
  const size = 120, r = 50, cx = size / 2, cy = size / 2
  const arcs = categories.map(c => {
    const startAng = (cum / total) * 2 * Math.PI - Math.PI / 2
    cum += c.value
    const endAng = (cum / total) * 2 * Math.PI - Math.PI / 2
    const x1 = cx + r * Math.cos(startAng), y1 = cy + r * Math.sin(startAng)
    const x2 = cx + r * Math.cos(endAng),   y2 = cy + r * Math.sin(endAng)
    const largeArc = endAng - startAng > Math.PI ? 1 : 0
    return { ...c, d: `M ${cx} ${cy} L ${x1} ${y1} A ${r} ${r} 0 ${largeArc} 1 ${x2} ${y2} Z` }
  })

  return (
    <div className="mx-4 mb-3.5">
      <div className="mb-2.5">
        <div className="text-sm font-bold text-text-primary">🥧 Categorías</div>
        <div className="text-[11px] text-text-secondary mt-0.5">En qué gastáis más esfuerzo</div>
      </div>
      <Card className="flex items-center gap-3.5">
        <svg width={size} height={size} viewBox={`0 0 ${size} ${size}`} className="flex-shrink-0">
          {arcs.map(a => <path key={a.name} d={a.d} fill={a.color} opacity={0.9} />)}
          <circle cx={cx} cy={cy} r={24} fill="#1a1138" />
          <text x={cx} y={cy - 1} textAnchor="middle" fontSize="12" fontWeight="700" fill="#e2e8f0">100%</text>
          <text x={cx} y={cy + 11} textAnchor="middle" fontSize="8" fill="#9ca3af">sem</text>
        </svg>
        <div className="flex-1 flex flex-col gap-1.5">
          {categories.map(c => (
            <div key={c.name} className="flex items-center gap-2 text-xs">
              <span className="text-sm">{c.emoji}</span>
              <span className="text-text-primary font-semibold flex-1">{c.name}</span>
              <span className="font-bold tabular-nums" style={{ color: c.color }}>{Math.round(c.value / total * 100)}%</span>
            </div>
          ))}
        </div>
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: `BalanceEvolutionChart.tsx`**

Transcribir desde kit Analytics.jsx líneas 182-230 a TSX con props `{ weeks: { label: string; balance: number }[] }`. Mantener SVG, path construction, gradient, puntos y label "↗ Tendencia: ..." al final.

- [ ] **Step 3: `TimeInvestedChart.tsx`**

Transcribir desde kit Analytics.jsx líneas 233-268 a TSX con props `{ you: { name; hours }, partner: { name; hours } }`. Barra dividida + labels.

- [ ] **Step 4: Commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/analytics/charts/
git commit -m "feat(analytics): 3 charts básicos restantes (Pie, Evolution, TimeInvested)"
```

### Task 4.4: Charts avanzados — 5 componentes

**Files:**
- Create: `src/frontend/src/components/v2/analytics/charts/HeatmapChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/CompletionRateChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/EquityGaugeChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/TopCategoriesChart.tsx`
- Create: `src/frontend/src/components/v2/analytics/charts/MonthlyInsightCard.tsx`

Referencia completa kit Analytics.jsx líneas 347-500. Transcribir cada función a TSX.

- [ ] **Step 1: `HeatmapChart.tsx`** — Grid 7×6 con opacity variable según norm (0-4).

```tsx
import { Card } from '../../primitives/Card'

interface GridCell { dow: number; bucket: number; norm: number }
interface Props { grid: GridCell[]; buckets: number[]; hint?: string }

const DAYS = ['L','M','X','J','V','S','D']
const COLORS = ['rgba(168,85,247,0.08)','rgba(168,85,247,0.3)','rgba(168,85,247,0.5)','rgba(168,85,247,0.75)','rgba(168,85,247,1)']

export function HeatmapChart({ grid, buckets, hint }: Props) {
  function cell(dow: number, bucket: number) {
    return grid.find(g => g.dow === dow && g.bucket === bucket)?.norm ?? 0
  }
  return (
    <div className="mx-4 mb-3.5">
      <div className="mb-2.5">
        <div className="text-sm font-bold text-text-primary">🗓️ Heatmap de actividad</div>
        <div className="text-[11px] text-text-secondary mt-0.5">Cuándo completáis más tareas</div>
      </div>
      <Card>
        <div className="grid gap-[3px]" style={{ gridTemplateColumns: `20px repeat(${buckets.length}, 1fr)` }}>
          <div />
          {buckets.map(h => <div key={h} className="text-[9px] text-text-tertiary text-center">{h}h</div>)}
          {DAYS.map((d, di) => (
            <>
              <div key={`lbl-${d}`} className="text-[9px] text-text-secondary font-semibold self-center">{d}</div>
              {buckets.map(b => (
                <div
                  key={`${di}-${b}`}
                  className="aspect-square rounded-sm"
                  style={{ background: COLORS[cell(di, b)] }}
                />
              ))}
            </>
          ))}
        </div>
        {hint && <div className="mt-2.5 text-[11px] text-[#c4b5fd]">⏰ {hint}</div>}
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: `CompletionRateChart.tsx`**

Transcribir kit líneas 383-409: 2 barras horizontales con % y nombres.

- [ ] **Step 3: `EquityGaugeChart.tsx`**

Transcribir kit líneas 411-447: SVG circular con gradient verde→purple, número central y delta opcional.

- [ ] **Step 4: `TopCategoriesChart.tsx`**

Transcribir kit líneas 449-479: barras bidireccionales con emoji de categoría.

- [ ] **Step 5: `MonthlyInsightCard.tsx`**

```tsx
import { Card } from '../../primitives/Card'

interface Bullet { tone: 'success' | 'warn' | 'neutral'; text: string }
interface Props { text: string; bullets: Bullet[] }

const toneClass: Record<string, string> = {
  success: 'text-success',
  warn:    'text-warn',
  neutral: 'text-text-secondary',
}

export function MonthlyInsightCard({ text, bullets }: Props) {
  return (
    <div className="mx-4 mb-3.5">
      <div className="mb-2.5">
        <div className="text-sm font-bold text-text-primary">💬 Insight mensual</div>
        <div className="text-[11px] text-text-secondary mt-0.5">Resumen generado automáticamente</div>
      </div>
      <Card style={{ background: 'linear-gradient(135deg, rgba(245,158,11,0.08), rgba(168,85,247,0.08))' }}>
        <div className="text-[13px] text-text-primary leading-relaxed">{text}</div>
        {bullets.length > 0 && (
          <div className="mt-2.5 flex flex-wrap gap-2 text-[11px]">
            {bullets.map((b, i) => (
              <span key={i} className={`font-semibold ${toneClass[b.tone]}`}>
                {b.tone === 'success' ? '✓ ' : b.tone === 'warn' ? '⚠ ' : ''}{b.text}
              </span>
            ))}
          </div>
        )}
      </Card>
    </div>
  )
}
```

- [ ] **Step 6: Commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/analytics/charts/
git commit -m "feat(analytics): 5 charts avanzados (heatmap/completion/equity/top/insight)"
```

### Task 4.5: `PremiumOverlay`

**Files:**
- Create: `src/frontend/src/components/v2/analytics/PremiumOverlay.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { Button } from '../primitives/Button'

interface Props { onOpenInterest: () => void }

export function PremiumOverlay({ onOpenInterest }: Props) {
  return (
    <div className="absolute inset-0 flex items-start justify-center pt-20 bg-gradient-to-b from-[rgba(15,10,30,0.2)] via-[rgba(15,10,30,0.85)] to-[rgba(15,10,30,0.95)] pointer-events-auto">
      <div className="mx-4 px-5 py-5 bg-grad-premium border border-brand-amber/50 rounded-xl text-center shadow-xl shadow-brand-amber/25 max-w-[340px]">
        <div className="text-4xl mb-1.5">✨</div>
        <div className="text-lg font-extrabold text-white tracking-tight">Desbloquea Analítica Avanzada</div>
        <div className="text-xs text-[#c4b5fd] mt-1.5 leading-relaxed">
          Heatmaps, tasas de cumplimiento, índice de equidad histórico y comparativas mensuales con insights.
        </div>
        <div className="flex flex-wrap gap-1.5 justify-center my-3.5">
          {['🗓️ Heatmap', '🎯 Cumplimiento', '⚖️ Equidad', '🏅 Top cat.', '💬 Insights'].map(x => (
            <span key={x} className="text-[10px] font-semibold px-2 py-1 rounded-full bg-brand-amber/15 text-[#fbbf24] border border-brand-amber/30">
              {x}
            </span>
          ))}
        </div>
        <Button variant="primary" fullWidth onClick={onOpenInterest}>
          👑 Hazte Premium · 3,99€/mes
        </Button>
        <div className="text-[10px] text-text-secondary mt-2">Primera semana gratis · Cancela cuando quieras</div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/analytics/PremiumOverlay.tsx
git commit -m "feat(analytics): PremiumOverlay con CTA y chips features"
```

### Task 4.6: `AnalyticsTabs` + `BasicAnalytics` + `AdvancedAnalytics`

**Files:**
- Create: `src/frontend/src/components/v2/analytics/AnalyticsTabs.tsx`
- Create: `src/frontend/src/components/v2/analytics/BasicAnalytics.tsx`
- Create: `src/frontend/src/components/v2/analytics/AdvancedAnalytics.tsx`

- [ ] **Step 1: `AnalyticsTabs.tsx`**

```tsx
interface Props {
  tab: 'basic' | 'advanced' | 'movements'
  onTab: (t: 'basic' | 'advanced' | 'movements') => void
  isPremium: boolean
}

export function AnalyticsTabs({ tab, onTab, isPremium }: Props) {
  function btn(id: 'basic' | 'advanced' | 'movements', label: string, extraActive: string) {
    const active = tab === id
    return (
      <button
        onClick={() => onTab(id)}
        className={`flex-1 py-2.5 rounded-md border-0 text-[13px] font-bold transition ${
          active ? `${extraActive} text-white shadow-lg` : 'bg-transparent text-text-secondary'
        }`}
      >
        {label}
      </button>
    )
  }
  return (
    <div className="mx-4 mb-4 flex gap-1.5 p-1 bg-[rgba(26,16,53,0.6)] border border-brand-purple/15 rounded-lg">
      {btn('basic',    '📊 Básico',                                 'bg-grad-hero')}
      {btn('advanced', `📈 Avanzado ${!isPremium ? '🔒' : ''}`, 'bg-grad-cta')}
      {btn('movements','📜 Movimientos',                            'bg-gradient-to-br from-brand-purple to-brand-purple-dark')}
    </div>
  )
}
```

- [ ] **Step 2: `BasicAnalytics.tsx`**

```tsx
import { useQuery } from '@tanstack/react-query'
import { apiClient } from '../../../services/apiClient'
import { WeeklyBarsChart } from './charts/WeeklyBarsChart'
import { CategoryPieChart } from './charts/CategoryPieChart'
import { BalanceEvolutionChart } from './charts/BalanceEvolutionChart'
import { TimeInvestedChart } from './charts/TimeInvestedChart'

export function BasicAnalytics() {
  const { data: daily }      = useQuery({ queryKey: ['a-daily'],      queryFn: () => apiClient.get('/api/analytics/daily-activity?range=week').then(r => r.data.data) })
  const { data: cats }       = useQuery({ queryKey: ['a-cats'],       queryFn: () => apiClient.get('/api/analytics/points-by-category').then(r => r.data.data) })
  const { data: trends }     = useQuery({ queryKey: ['a-trends'],     queryFn: () => apiClient.get('/api/analytics/weekly-trends?weeks=4').then(r => r.data.data) })
  const { data: time }       = useQuery({ queryKey: ['a-time'],       queryFn: () => apiClient.get('/api/analytics/time-invested?range=week').then(r => r.data.data) })

  return (
    <>
      <WeeklyBarsChart days={daily?.days ?? []} youName={daily?.youName ?? 'Tú'} partnerName={daily?.partnerName ?? 'Pareja'} />
      <CategoryPieChart categories={cats ?? []} />
      <BalanceEvolutionChart weeks={trends?.weeks ?? []} />
      <TimeInvestedChart you={time?.you ?? { name: 'Tú', hours: 0 }} partner={time?.partner ?? { name: 'Pareja', hours: 0 }} />
    </>
  )
}
```

- [ ] **Step 3: `AdvancedAnalytics.tsx`**

```tsx
import { useQuery } from '@tanstack/react-query'
import { apiClient } from '../../../services/apiClient'
import { HeatmapChart } from './charts/HeatmapChart'
import { CompletionRateChart } from './charts/CompletionRateChart'
import { EquityGaugeChart } from './charts/EquityGaugeChart'
import { TopCategoriesChart } from './charts/TopCategoriesChart'
import { MonthlyInsightCard } from './charts/MonthlyInsightCard'
import { PremiumOverlay } from './PremiumOverlay'

interface Props {
  isPremium: boolean
  onOpenInterest: () => void
}

export function AdvancedAnalytics({ isPremium, onOpenInterest }: Props) {
  const { data: heat }  = useQuery({ queryKey: ['a-heat'],  queryFn: () => apiClient.get('/api/analytics/heatmap?weeks=4').then(r => r.data.data) })
  const { data: rate }  = useQuery({ queryKey: ['a-rate'],  queryFn: () => apiClient.get('/api/analytics/completion-rate?range=month').then(r => r.data.data) })
  const { data: over }  = useQuery({ queryKey: ['a-over'],  queryFn: () => apiClient.get('/api/analytics/couple').then(r => r.data.data) })
  const { data: top }   = useQuery({ queryKey: ['a-top'],   queryFn: () => apiClient.get('/api/analytics/points-by-category?groupByUser=true').then(r => r.data.data) })
  const { data: insig } = useQuery({ queryKey: ['a-insig'], queryFn: () => apiClient.get('/api/analytics/insight').then(r => r.data.data) })

  return (
    <div className="relative">
      <div style={{ filter: isPremium ? 'none' : 'blur(3px)', pointerEvents: isPremium ? 'auto' : 'none', userSelect: isPremium ? 'auto' : 'none' }}>
        <HeatmapChart grid={heat?.grid ?? []} buckets={heat?.buckets ?? [6,9,12,15,18,21]} hint="Más activos los jueves a las 18-21h" />
        <CompletionRateChart you={rate?.you ?? { name:'Tú', pct:0 }} partner={rate?.partner ?? { name:'Pareja', pct:0 }} />
        <EquityGaugeChart score={Number(over?.equilibrium ?? 0)} delta={over?.equityDelta ?? 0} />
        <TopCategoriesChart data={top ?? {}} />
        <MonthlyInsightCard text={insig?.text ?? 'Aún no hay suficiente actividad este mes.'} bullets={insig?.bullets ?? []} />
      </div>
      {!isPremium && <PremiumOverlay onOpenInterest={onOpenInterest} />}
    </div>
  )
}
```

- [ ] **Step 4: Commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/components/v2/analytics/
git commit -m "feat(analytics): AnalyticsTabs + Basic + Advanced orchestrators"
```

### Task 4.7: `MovementsTab` con filtros

**Files:**
- Create: `src/frontend/src/components/v2/analytics/MovementsTab.tsx`

- [ ] **Step 1: Implementar**

```tsx
import { useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { apiClient } from '../../../services/apiClient'
import { Avatar } from '../primitives/Avatar'

type WhoFilter  = 'all' | 'me' | 'partner'
type RangeFilter = 'week' | 'month' | 'all'

export function MovementsTab() {
  const [who,   setWho]   = useState<WhoFilter>('all')
  const [cat,   setCat]   = useState('all')
  const [range, setRange] = useState<RangeFilter>('week')

  const { data: list = [] } = useQuery({
    queryKey: ['movements', who, cat, range],
    queryFn: async () => (await apiClient.get(`/api/points/history?who=${who}&cat=${cat}&range=${range}`)).data.data ?? [],
  })

  const groups: Record<string, any[]> = {}
  list.forEach((m: any) => {
    const key = m.dateLabel ?? 'Otros'
    ;(groups[key] ??= []).push(m)
  })

  return (
    <>
      <div className="mx-4 mb-3 flex gap-1.5 overflow-x-auto no-scrollbar">
        {(['all','me','partner'] as WhoFilter[]).map(w => (
          <button key={w} onClick={() => setWho(w)}
            className={`px-3 py-1.5 rounded-full text-xs font-semibold ${who === w ? 'bg-brand-purple text-white' : 'bg-surface-card text-text-secondary border border-brd-subtle'}`}>
            {w === 'all' ? 'Todos' : w === 'me' ? 'Tú' : 'Pareja'}
          </button>
        ))}
        {(['week','month','all'] as RangeFilter[]).map(r => (
          <button key={r} onClick={() => setRange(r)}
            className={`px-3 py-1.5 rounded-full text-xs font-semibold ml-auto ${range === r ? 'bg-brand-amber text-white' : 'bg-surface-card text-text-secondary border border-brd-subtle'}`}>
            {r === 'week' ? 'Semana' : r === 'month' ? 'Mes' : 'Todo'}
          </button>
        ))}
      </div>

      {Object.keys(groups).length === 0 && (
        <div className="mx-4 mt-8 text-center text-text-secondary text-sm">Sin movimientos en este rango</div>
      )}

      {Object.entries(groups).map(([day, items]) => (
        <div key={day} className="mx-4 mb-4">
          <div className="text-[11px] font-bold text-text-secondary uppercase mb-1.5">{day}</div>
          <div className="rounded-md bg-[rgba(26,16,53,0.3)] overflow-hidden">
            {items.map((m: any, i: number) => (
              <div key={m.id} className={`flex items-center gap-2 px-3 py-2.5 ${i > 0 ? 'border-t border-brd-subtle' : ''}`}>
                <Avatar emoji={m.userAvatarEmoji} color={m.userAvatarColor} size="sm" />
                <div className="flex-1 text-xs min-w-0">
                  <span className="text-text-primary font-semibold">{m.userName}</span>
                  <span className="text-text-secondary"> · {m.action}</span>
                </div>
                <span className={`text-xs font-bold tabular-nums ${Number(m.delta) >= 0 ? 'text-success' : 'text-danger'}`}>
                  {Number(m.delta) >= 0 ? '+' : ''}{Number(m.delta).toFixed(1)} MP
                </span>
              </div>
            ))}
          </div>
        </div>
      ))}
    </>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/components/v2/analytics/MovementsTab.tsx
git commit -m "feat(analytics): MovementsTab con filtros (who/cat/range)"
```

### Task 4.8: `pages/Analytics.tsx` (reescribir)

**Files:**
- Modify: `src/frontend/src/pages/Analytics.tsx`
- Modify: `src/frontend/src/App.tsx` (si hace falta ajustar la ruta)

- [ ] **Step 1: Reescribir `Analytics.tsx`**

```tsx
import { useState } from 'react'
import { useSearchParams } from 'react-router-dom'
import { Pill } from '../components/v2/primitives/Pill'
import { AnalyticsTabs } from '../components/v2/analytics/AnalyticsTabs'
import { BasicAnalytics } from '../components/v2/analytics/BasicAnalytics'
import { AdvancedAnalytics } from '../components/v2/analytics/AdvancedAnalytics'
import { MovementsTab } from '../components/v2/analytics/MovementsTab'
import { PremiumInterestModal } from '../components/v2/premium/PremiumInterestModal'

type Tab = 'basic' | 'advanced' | 'movements'

export default function Analytics() {
  const [params] = useSearchParams()
  const initial: Tab = (params.get('tab') as Tab) === 'movements' ? 'movements'
                     : (params.get('tab') as Tab) === 'advanced'  ? 'advanced'
                     : 'basic'
  const [tab, setTab] = useState<Tab>(initial)
  const [interestOpen, setInterestOpen] = useState(false)
  const isPremium = false  // v1.4: todos son free

  const month = ['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'][new Date().getMonth()]

  return (
    <main className="pt-1">
      <div className="flex items-center justify-between px-4 mb-3">
        <h2 className="text-[22px] font-extrabold text-text-primary m-0">Analítica</h2>
        <Pill tone="purple">{month}</Pill>
      </div>
      <AnalyticsTabs tab={tab} onTab={setTab} isPremium={isPremium} />
      {tab === 'basic'     && <BasicAnalytics />}
      {tab === 'advanced'  && <AdvancedAnalytics isPremium={isPremium} onOpenInterest={() => setInterestOpen(true)} />}
      {tab === 'movements' && <MovementsTab />}
      <div style={{ height: 20 }} />
      <PremiumInterestModal open={interestOpen} onClose={() => setInterestOpen(false)} source="analytics_advanced_overlay" />
    </main>
  )
}
```

- [ ] **Step 2: Asegurar ruta `/history` redirect**

En `App.tsx` confirmar que existe:

```tsx
<Route path="/history" element={<Navigate to="/analytics?tab=movements" replace />} />
```

- [ ] **Step 3: Smoke visual**

```bash
cd src/frontend && npm run dev &
sleep 4
```

Abrir `http://localhost:5173/analytics` y verificar:
- [ ] 3 tabs visibles (Básico con indigo, Avanzado amber+🔒, Movimientos purple)
- [ ] Tab Básico → 4 gráficos
- [ ] Tab Avanzado → blur + overlay con CTA
- [ ] Click CTA overlay → abre modal interest
- [ ] Submit modal → 201 Created, muestra "¡Listo!"
- [ ] Tab Movimientos → filtros funcionan
- [ ] `/history` redirige a `/analytics?tab=movements`

```bash
kill %1
```

- [ ] **Step 4: Commit**

```bash
git add src/frontend/src/pages/Analytics.tsx src/frontend/src/App.tsx
git commit -m "feat(analytics): reescribir pantalla completa con 3 tabs + Premium gate"
```

---

## Phase 5 — Auth flow (Login · Signup · Onboarding)

### Task 5.1: Login v2

**Files:**
- Modify: `src/frontend/src/pages/Login.tsx`

Referencia: `/tmp/claude-design-v2/matripuntos-design-system/project/ui_kits/app/screens/Login.jsx`

- [ ] **Step 1: Reescribir**

```tsx
import { useState } from 'react'
import { useNavigate, Link } from 'react-router-dom'
import { apiClient } from '../services/apiClient'
import { useAppStore } from '../store/useAppStore'
import { Button } from '../components/v2/primitives/Button'
import { Input } from '../components/v2/primitives/Input'

export default function Login() {
  const nav = useNavigate()
  const setAuth = useAppStore(s => s.setAuth)
  const [email, setEmail] = useState('')
  const [pwd, setPwd]     = useState('')
  const [showPwd, setShowPwd] = useState(false)
  const [loading, setLoading] = useState(false)
  const [err, setErr]         = useState<string | null>(null)

  async function submit(e: React.FormEvent) {
    e.preventDefault()
    setLoading(true); setErr(null)
    try {
      const { data } = await apiClient.post('/api/auth/login', { email, password: pwd })
      setAuth(data)
      nav(data.user.hasCompletedOnboarding ? '/dashboard' : '/onboarding')
    } catch (e: any) {
      setErr(e?.response?.data?.error ?? 'Error al iniciar sesión')
    } finally { setLoading(false) }
  }

  return (
    <main className="px-6 min-h-screen flex flex-col">
      <div className="flex-1 flex flex-col justify-center py-10 max-w-md mx-auto w-full">
        <div className="text-center mb-8">
          <div className="w-[72px] h-[72px] rounded-[20px] mx-auto mb-4 bg-gradient-to-br from-brand-amber to-brand-purple flex items-center justify-center text-4xl shadow-xl shadow-brand-purple/40">💕</div>
          <h1 className="text-2xl font-extrabold text-text-primary tracking-tight m-0">Matripuntos</h1>
          <div className="text-[13px] text-text-secondary mt-1">Equilibrio en pareja</div>
        </div>
        <form onSubmit={submit} className="flex flex-col gap-3">
          <Input label="Email" type="email" value={email} onChange={e => setEmail(e.target.value)} autoComplete="email" required />
          <div className="relative">
            <Input label="Contraseña" type={showPwd ? 'text' : 'password'} value={pwd} onChange={e => setPwd(e.target.value)} autoComplete="current-password" required />
            <button type="button" onClick={() => setShowPwd(!showPwd)} className="absolute right-2 bottom-2 text-text-secondary text-lg" aria-label="Mostrar contraseña">👁</button>
          </div>
          {err && <div className="text-xs text-danger">{err}</div>}
          <button type="button" className="text-[11px] text-brand-purple self-end">olvidé mi contraseña →</button>
          <Button variant="primary" fullWidth size="lg" type="submit" disabled={loading}>
            {loading ? 'Entrando…' : 'Entrar'}
          </Button>
        </form>
        <div className="flex items-center gap-3 my-5 text-xs text-text-tertiary">
          <div className="flex-1 h-px bg-brd-subtle" />o<div className="flex-1 h-px bg-brd-subtle" />
        </div>
        <Button variant="outline" fullWidth disabled title="Disponible pronto">Continuar con Google</Button>
        <div className="h-2" />
        <Button variant="outline" fullWidth disabled title="Disponible pronto">Continuar con Apple</Button>
        <div className="text-center mt-6 text-xs text-text-secondary">
          ¿Primera vez aquí? <Link to="/signup" className="text-brand-purple font-bold">Crea tu cuenta →</Link>
        </div>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Login.tsx
git commit -m "feat(auth): Login v2 con OAuth placeholders disabled"
```

### Task 5.2: Signup v2 (wizard 2 pasos)

**Files:**
- Modify: `src/frontend/src/pages/Signup.tsx`

Referencia: `screens/Signup.jsx`.

- [ ] **Step 1: Reescribir con 2 pasos**

Seguir estructura del kit: paso 1 email/pwd/confirm/accept; paso 2 nombre.

- [ ] **Step 2: Validación inline**

- Email contiene `@`
- Password ≥ 6 chars
- Confirm === Password
- Nombre ≥ 2 chars
- Accept terms marcado

- [ ] **Step 3: Submit final**

```ts
const { data } = await apiClient.post('/api/auth/register', { email, password, name })
setAuth(data)
nav('/onboarding')
```

- [ ] **Step 4: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Signup.tsx
git commit -m "feat(auth): Signup v2 wizard 2 pasos"
```

### Task 5.3: Onboarding v2 (6 pasos)

**Files:**
- Modify: `src/frontend/src/pages/Onboarding.tsx`
- Create: `src/frontend/src/pages/onboarding/StepWelcome.tsx`
- Create: `src/frontend/src/pages/onboarding/StepProfile.tsx`
- Create: `src/frontend/src/pages/onboarding/StepPair.tsx`
- Create: `src/frontend/src/pages/onboarding/StepRules.tsx`
- Create: `src/frontend/src/pages/onboarding/StepCategories.tsx`
- Create: `src/frontend/src/pages/onboarding/StepDone.tsx`

Referencia: `screens/Onboarding.jsx` completo (314 líneas).

- [ ] **Step 1: Shell con progress bar + state compartido**

```tsx
export default function Onboarding() {
  const [step, setStep] = useState(0)
  const [data, setData] = useState({
    name: '', avatarEmoji: '🐼', avatarColor: '#7c3aed',
    pairMethod: 'code' as 'email'|'code'|'solo',
    pairEmail: '', pairCode: '',
    rules: { dailyMult: 1.5, weeklyBonus: 0.25 },
    categories: ['cocina','banos','limpieza','compra','logistica','cuidado','mantenimiento','jardineria','mascotas'],
  })
  const steps = [StepWelcome, StepProfile, StepPair, StepRules, StepCategories, StepDone]
  const Current = steps[step]

  async function finish() {
    await apiClient.patch('/api/profile/me', {
      name: data.name,
      avatarEmoji: data.avatarEmoji,
      avatarColor: data.avatarColor,
      hasCompletedOnboarding: true,
    })
    if (data.pairMethod === 'email' && data.pairEmail) {
      await apiClient.post('/api/auth/invite', { inviteeEmail: data.pairEmail })
    }
    if (data.pairMethod === 'code' && data.pairCode) {
      await apiClient.post('/api/auth/join-couple', { token: data.pairCode })
    }
    await apiClient.put('/api/configuration', {
      multipliersConfig: { dailyMult: data.rules.dailyMult, weeklyBonus: data.rules.weeklyBonus },
    })
    // TODO: persist categories if different from defaults
    nav('/dashboard')
  }
  // ... render progress bar + Current
}
```

- [ ] **Step 2: Cada step como componente puro**

Para cada step implementar vista y next/prev handlers según kit. Ejemplo `StepWelcome`:

```tsx
export function StepWelcome({ onNext }: { onNext: () => void }) {
  return (
    <div className="flex flex-col items-center text-center pt-10">
      <div className="w-28 h-28 rounded-full bg-gradient-to-br from-brand-amber to-brand-purple flex items-center justify-center text-6xl mb-6 shadow-2xl shadow-brand-purple/40">💕</div>
      <h1 className="text-2xl font-extrabold text-text-primary">Bienvenida a Matripuntos</h1>
      <p className="text-sm text-text-secondary mt-2 leading-relaxed max-w-[320px]">
        Un juego para repartir la vida en pareja — sin llevar cuentas en silencio.
      </p>
      <Button variant="primary" size="lg" onClick={onNext} className="mt-8 w-full max-w-[280px]">Empezar →</Button>
    </div>
  )
}
```

Implementar los 5 restantes siguiendo el kit.

- [ ] **Step 3: Pre-paso — auto-skip paso 3 si llegó con `?token=`**

```tsx
useEffect(() => {
  const params = new URLSearchParams(location.search)
  const token = params.get('token')
  if (token) { apiClient.post('/api/auth/join-couple', { token }); setStep(1) }
}, [])
```

- [ ] **Step 4: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Onboarding.tsx src/frontend/src/pages/onboarding/
git commit -m "feat(auth): Onboarding v2 con 6 pasos (welcome/profile/pair/rules/cats/done)"
```

---

## Phase 6 — Tasks + Calendar v2

### Task 6.1: Tasks page v2

**Files:**
- Modify: `src/frontend/src/pages/Tasks.tsx`
- Create: `src/frontend/src/components/v2/tasks/CategoryFilterStrip.tsx`
- Create: `src/frontend/src/components/v2/tasks/TaskItemLarge.tsx`
- Create: `src/frontend/src/components/v2/tasks/AddTaskSheet.tsx`

Referencia: `ui_kits/app/Tasks.jsx`.

- [ ] **Step 1: `CategoryFilterStrip.tsx`** — chips horizontales scroll

- [ ] **Step 2: `TaskItemLarge.tsx`** — emoji tile 40×40 + nombre + MP + CTA Button según assignee

- [ ] **Step 3: `AddTaskSheet.tsx`** — BottomSheet con wizard 3 pasos (nombre+cat, puntual vs recurrente, assignee). Reusar `TaskScheduleForm` existente.

- [ ] **Step 4: `pages/Tasks.tsx`** — ViewToggle lista/semana (propio), CategoryFilterStrip, secciones Hoy/Semana/Catálogo.

- [ ] **Step 5: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Tasks.tsx src/frontend/src/components/v2/tasks/
git commit -m "feat(tasks): pantalla v2 con filtros, 3 secciones y AddTaskSheet"
```

### Task 6.2: Calendar page v2

**Files:**
- Modify: `src/frontend/src/pages/Calendar.tsx`
- Create: `src/frontend/src/components/v2/calendar/MonthGrid.tsx`
- Create: `src/frontend/src/components/v2/calendar/WeekStripChart.tsx`
- Create: `src/frontend/src/components/v2/calendar/EventCardV2.tsx`

Referencia: `screens/Calendar.jsx`.

- [ ] **Step 1: `MonthGrid.tsx`** — 7×N grid, día seleccionado con border amber, dots con emojis por evento

- [ ] **Step 2: `WeekStripChart.tsx`** — 7 columnas barras tú/partner + lista

- [ ] **Step 3: `EventCardV2.tsx`** — emoji + título + fecha + assignee + Pill status

- [ ] **Step 4: `pages/Calendar.tsx`** — ViewToggle Mes/Semana (propio), navegación meses, sección "Próximos"

- [ ] **Step 5: FAB desde Calendar prellenado**

El FAB del layout global al abrir en Calendar podría pre-pasar la fecha seleccionada a `/request-activity` via state. Nota inline: por ahora, FAB navega genérico; prellenar fecha queda como mejora v1.5.

- [ ] **Step 6: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Calendar.tsx src/frontend/src/components/v2/calendar/
git commit -m "feat(calendar): pantalla v2 con vista mes/semana + próximos eventos"
```

---

## Phase 7 — Pantallas secundarias

### Task 7.1: Settings (7 secciones)

**Files:**
- Modify: `src/frontend/src/pages/Settings.tsx`
- Create: `src/frontend/src/pages/settings/SectionProfile.tsx`
- Create: `src/frontend/src/pages/settings/SectionPartner.tsx`
- Create: `src/frontend/src/pages/settings/SectionNotif.tsx`
- Create: `src/frontend/src/pages/settings/SectionPremium.tsx`
- Create: `src/frontend/src/pages/settings/SectionRules.tsx`
- Create: `src/frontend/src/pages/settings/SectionDisplay.tsx`
- Create: `src/frontend/src/pages/settings/SectionPrivacy.tsx`

Referencia: `screens/Settings.jsx`.

- [ ] **Step 1: Shell con lista de 7 items**

Usar `useParams()` y `useNavigate` para navegar a `/settings/:section`. Cada sección detail reusa componentes existentes donde aplique (`RuleProposalCard` para rules, etc.).

- [ ] **Step 2: `SectionPremium`** abre `PremiumInterestModal` con `source="settings_premium_cta"`

- [ ] **Step 3: `SectionPrivacy`** con botones Exportar y Eliminar (ambos llaman endpoints existentes o muestran toast "próximamente" si no existen aún)

- [ ] **Step 4: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Settings.tsx src/frontend/src/pages/settings/
git commit -m "feat(settings): 7 secciones con shell + sub-rutas"
```

### Task 7.2: Achievements rediseño (3 tabs)

**Files:**
- Modify: `src/frontend/src/pages/Achievements.tsx`

Referencia: `screens/Achievements.jsx`.

- [ ] **Step 1: 3 tabs (Badges / Ranking / Historial)**

- [ ] **Step 2: Grid badges con rareza (border)**

- [ ] **Step 3: CTA "📊 Ver analítica completa →" → `/analytics`**

- [ ] **Step 4: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/Achievements.tsx
git commit -m "feat(achievements): rediseño completo con 3 tabs (badges/ranking/history)"
```

### Task 7.3: Shopping + Todos rediseño

**Files:**
- Modify: `src/frontend/src/pages/ShoppingListPage.tsx`
- Modify: `src/frontend/src/pages/TodoListPage.tsx`

Referencias: `screens/Shopping.jsx`, `screens/Todos.jsx`.

- [ ] **Step 1: Shopping — agrupar por categoría, swipe-delete, input inline + autoFocus desde state**

- [ ] **Step 2: Todos — lista simple, toggle mis/compartidos, input inline + autoFocus**

- [ ] **Step 3: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/ShoppingListPage.tsx src/frontend/src/pages/TodoListPage.tsx
git commit -m "feat(shopping+todos): rediseño v2 con agrupación y auto-focus input"
```

### Task 7.4: RequestActivity + RequestInbox

**Files:**
- Modify: `src/frontend/src/pages/RequestActivity.tsx`
- Modify: `src/frontend/src/pages/RequestInbox.tsx`

Referencias: `screens/RequestActivity.jsx`, `screens/RequestInbox.jsx`.

- [ ] **Step 1: RequestActivity — wizard 3 pasos con PointsBreakdown en tiempo real**

- [ ] **Step 2: RequestInbox — feed de EventCards con acciones, rondas visibles, Forzar si aplica**

- [ ] **Step 3: Smoke + commit**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -3
git add src/frontend/src/pages/RequestActivity.tsx src/frontend/src/pages/RequestInbox.tsx
git commit -m "feat(requests): rediseño Activity wizard 3 pasos + Inbox feed"
```

### Task 7.5: NotFound

**Files:**
- Modify: `src/frontend/src/pages/NotFound.tsx`

Referencia: `screens/NotFound.jsx`.

- [ ] **Step 1: Implementar**

```tsx
import { useNavigate } from 'react-router-dom'
import { Button } from '../components/v2/primitives/Button'

export default function NotFound() {
  const nav = useNavigate()
  return (
    <main className="min-h-screen flex flex-col items-center justify-center px-6 text-center">
      <div className="w-24 h-24 rounded-3xl bg-gradient-to-br from-brand-purple to-brand-amber flex items-center justify-center text-5xl shadow-2xl mb-6">🌸</div>
      <h1 className="text-xl font-extrabold text-text-primary m-0">Esta página se fue de paseo</h1>
      <p className="text-sm text-text-secondary mt-2">No pasa nada, aquí tienes por dónde seguir</p>
      <Button variant="primary" size="lg" onClick={() => nav('/dashboard')} className="mt-6 w-full max-w-[280px]">🏠 Volver al inicio</Button>
      <div className="mt-6 text-xs text-text-secondary">O quizá buscabas:</div>
      <div className="flex gap-3 mt-2 text-xs">
        <button onClick={() => nav('/tasks')}         className="text-brand-purple font-bold">Mis tareas</button>
        <span className="text-text-tertiary">·</span>
        <button onClick={() => nav('/request-inbox')} className="text-brand-purple font-bold">Negociar</button>
        <span className="text-text-tertiary">·</span>
        <button onClick={() => nav('/settings')}      className="text-brand-purple font-bold">Ajustes</button>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/frontend/src/pages/NotFound.tsx
git commit -m "feat(404): rediseño NotFound con humor y sugerencias"
```

---

## Phase 8 — Cleanup, QA, deploy

### Task 8.1: Cleanup de componentes v1 sin uso

**Files:**
- Delete: varios en `src/frontend/src/components/*`

- [ ] **Step 1: Identificar no usados**

```bash
cd src/frontend
for f in src/components/*.tsx; do
  name=$(basename "$f" .tsx)
  uses=$(grep -rl "$name" src --include="*.tsx" --include="*.ts" | grep -v "$f" | wc -l)
  echo "$uses $f"
done | sort -n | head -30
```

Anotar los que tienen 0 uses.

- [ ] **Step 2: Borrar los confirmados sin uso**

```bash
# Ejemplo — ajustar según step 1
rm src/components/AchievementsPanel.tsx
rm src/components/GamificationDashboard.tsx
# ...
```

- [ ] **Step 3: Typecheck + dev smoke**

```bash
cd src/frontend && npm run typecheck 2>&1 | tail -5
npm run dev &
sleep 4
kill %1
```

- [ ] **Step 4: Commit**

```bash
git add -A src/frontend/src/components/
git commit -m "chore: eliminar componentes v1 sin uso tras migración v2"
```

### Task 8.2: CHANGELOG

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Añadir entrada [1.4.0]**

Al principio de `CHANGELOG.md` insertar:

```markdown
## [1.4.0] · La Evolución · 2026-04-XX

### Changed
- **Rediseño visual completo** de las 16 pantallas según Claude Design v2 bundle.
- **Nueva navegación**: `Inicio · Tareas · Calendario · Analítica` (Historial absorbido en Analytics).
- **Header ⋯ menú** con Logros, Perfil, Pareja, Reglas, Ajustes, Ayuda, Logout.
- **FAB** abre sheet con 3 opciones: 📅 Actividad · 🛒 Compra · 📝 To-do.
- **Dashboard condensado**: BalanceLevelHero fusionado, StreakStrip compacto, tareas arriba del fold.
- **Dark-only**: se elimina el toggle light/dark.
- **Onboarding** ampliado a 6 pasos (bienvenida · perfil · pareja · reglas · categorías · listo).

### Added
- **Analytics** nueva (3 tabs): Básico (4 gráficos), Avanzado (5 gráficos con overlay Premium), Movimientos.
- **Endpoints analytics**: `/time-invested`, `/heatmap`, `/completion-rate`, `/insight` (heurístico).
- **Premium teaser**: tabla `PremiumInterest` + endpoint `POST /api/premium/interest`. CTA en Analytics y Settings.
- **Login**: placeholders disabled para Google y Apple OAuth.
- **NotFound** rediseño con humor + sugerencias.

### Tokens
- Tailwind config extendido con tokens del bundle v2 (surfaces, brand, gradients, radii).
```

- [ ] **Step 2: Commit**

```bash
git add CHANGELOG.md
git commit -m "docs: añadir [1.4.0] al CHANGELOG"
```

### Task 8.3: QA de humo end-to-end

- [ ] **Step 1: Arrancar backend + frontend**

```bash
cd src/backend && npm run dev &
cd ../frontend && npm run dev &
sleep 5
```

- [ ] **Step 2: Recorrer checklist §14 del spec**

Abrir `docs/superpowers/specs/2026-04-20-v1.4-la-evolucion-design.md` sección 14 y marcar cada item. Si algún item falla:
- Crear commit aparte con el fix
- Volver a marcar

- [ ] **Step 3: Validar en Chrome + Safari (mobile viewport)**

DevTools → responsive → 375×667, luego 414×896, luego 500×900.

- [ ] **Step 4: Kill servers**

```bash
kill %1 %2
```

- [ ] **Step 5: Si todo OK, commit marker**

```bash
git commit --allow-empty -m "chore: QA de humo v1.4 completa (ver checklist §14 en spec)"
```

### Task 8.4: Merge + tag + deploy

- [ ] **Step 1: Merge a main desde worktree**

```bash
cd ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion
git fetch origin
git rebase origin/main  # resolver conflictos si los hay
```

Si conflictos, resolver manualmente + `git rebase --continue`.

- [ ] **Step 2: Cambiar al repo principal y merge**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
git pull origin main
git merge --no-ff feature/v1.4-la-evolucion -m "Merge v1.4 · La Evolución"
git push origin main
```

- [ ] **Step 3: Tag v1.4**

```bash
git tag -a v1.4 -m "v1.4 · La Evolución — rediseño completo + Analytics + Premium teaser"
git push origin v1.4
```

- [ ] **Step 4: Deploy frontend (FTP)**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
bash deploy-frontend.sh
```

Expected: build + upload sin errores.

- [ ] **Step 5: Verificar deploy**

- Backend: mirar dashboard Render — build verde, health check 200
- Frontend: abrir URL de producción (dominio keepitup.io), verificar que la versión es la nueva

- [ ] **Step 6: Limpiar worktree**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
git worktree remove ~/.config/superpowers/worktrees/Matripuntos/v1.4-la-evolucion
git branch -d feature/v1.4-la-evolucion
```

- [ ] **Step 7: Actualizar ROADMAP a ✅**

```bash
# Editar docs/ROADMAP.md: cambiar v1.4 estado de "Spec aprobado" a "✓ Producción"
git add docs/ROADMAP.md
git commit -m "docs: marcar v1.4 como en producción"
git push origin main
```

---

## Self-Review (auto-aplicado tras escribir el plan)

**Spec coverage:** cada sección del spec tiene una task.

- §1 Objetivo → cubierto en Phase 0 (foundation)
- §2 Alcance → Phase 0-8 completo
- §3 Decisiones (10) → todas respetadas (dark-only Phase 0.3; OAuth disabled Phase 5.1; Premium teaser Phase 1.2 + 4.1; heuristic insight Phase 1.6)
- §4 Nav → Phase 2
- §5 Dashboard → Phase 3
- §6 Analytics → Phase 4
- §7 Auth flow → Phase 5
- §8 Secundarias → Phase 7
- §9 Tokens → Phase 0.2 + 0.3
- §10 Estructura componentes → Phase 0.4-0.6 (primitives), 2-4-6 (layout/dashboard/analytics/tasks/calendar)
- §11 Backend → Phase 1 (1.1-1.7)
- §12 Premium flow → Phase 4.1
- §13 Migraciones rutas → Phase 2.5, 4.8
- §14 Checklist QA → Phase 8.3
- §15 Criterios aceptación → validados en QA §14
- §16 Riesgos → mitigaciones aplicadas (rebase frecuente Phase 8.4; cache insight Phase 1.6; rate-limit Phase 1.2)
- §17 Versiones afectadas → Phase 8.2 CHANGELOG + 8.4 ROADMAP

**Placeholder scan:** ninguno detectado. Donde el kit tiene código extenso (charts avanzados Task 4.4), se referencia con líneas específicas del kit fuente para copiar — eso es "show me how", no "TBD".

**Type consistency:** revisado. Las props de charts coinciden con las respuestas de endpoints (`time-invested` devuelve `{you, partner}` con `{name, hours}`; `TimeInvestedChart` consume `{you, partner}` con `{name, hours}`).

---

**Plan completo.** Guardado en `docs/superpowers/plans/2026-04-20-v1.4-la-evolucion.md`.

Dos opciones de ejecución:

**1. Subagent-Driven (recomendado)** — dispatch de subagente fresco por task, review intermedio, iteración rápida.

**2. Inline Execution** — ejecución en esta sesión con checkpoints para review.

¿Cuál prefieres?
