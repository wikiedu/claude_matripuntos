# v1.1 · La Chispa — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rediseño visual completo de Matripuntos con paleta warm+dark, bottom navigation bar, avatares kawaii, mood del día, frase motivacional diaria, y onboarding mejorado.

**Architecture:** Foundation-first — color tokens y nav se hacen primero (Tasks 1-3) porque todo lo demás los necesita. Luego schema DB y API (Task 4). Luego features nuevas (Tasks 5-7). Finalmente rediseño de páginas secundarias (Tasks 8-9). Cada task es deployable por sí sola.

**Tech Stack:** React 18 + TypeScript + Vite + Tailwind CSS + Zustand + React Query · Express + Prisma + SQLite/PostgreSQL

---

## File Map

**New files:**
- `src/frontend/src/components/BottomNav.tsx` — barra de navegación inferior fija
- `src/frontend/src/components/Avatar.tsx` — avatar circular con mood badge
- `src/frontend/src/components/AvatarSelector.tsx` — grid 16 emojis + 8 colores
- `src/frontend/src/components/MoodSelector.tsx` — bottom sheet 8 estados
- `src/frontend/src/utils/dailyPhrase.ts` — helper getDailyPhrase() + 60 frases
- `src/frontend/src/pages/onboarding/OnboardingLanding.tsx` — paso 0 propuesta de valor

**Modified files:**
- `src/frontend/tailwind.config.js` — tokens `matri-*`
- `src/frontend/src/App.css` — CSS variables dark/light
- `src/frontend/src/App.tsx` — aplicar clase `dark`/`light` en `<html>`, rutas
- `src/frontend/src/store/useAppStore.ts` — añadir `theme` state
- `src/backend/prisma/schema.prisma` — nuevos campos en `UserProfile`
- `src/backend/src/routes/authRoutes.ts` — `GET /me` devuelve `partnerMood`
- `src/backend/src/routes/profile.ts` — `PUT /profile/me` + actualizar mood/avatar/theme
- `src/frontend/src/pages/Dashboard.tsx` — rediseño completo, bottom nav, mood, frase
- `src/frontend/src/pages/Settings.tsx` — selector avatar + color + theme toggle
- `src/frontend/src/pages/Onboarding.tsx` — añadir paso 0
- `src/frontend/src/pages/Tasks.tsx` — nueva paleta
- `src/frontend/src/pages/Calendar.tsx` + `CalendarMonth/Week/Day.tsx` — nueva paleta
- `src/frontend/src/components/StatCard.tsx`, `Card.tsx`, `Button.tsx`, `NotificationBell.tsx` — nueva paleta

---

## Task 1: Color tokens + CSS variables

**Files:**
- Modify: `src/frontend/tailwind.config.js`
- Modify: `src/frontend/src/App.css`

- [ ] **Step 1: Añadir tokens custom a Tailwind**

Reemplaza el contenido de `src/frontend/tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        // Legacy (mantener para no romper componentes no migrados aún)
        primary: "#6366F1",
        secondary: "#EC4899",
        success: "#10B981",
        warning: "#F59E0B",
        danger: "#EF4444",
        neutral: "#6B7280",
        // Matripuntos design system
        'matri-amber':        '#f59e0b',
        'matri-amber-dark':   '#d97706',
        'matri-purple':       '#a855f7',
        'matri-purple-dark':  '#7c3aed',
        'matri-green':        '#22c55e',
        'matri-green-light':  '#16a34a',
        // Dark theme surfaces
        'matri-bg':           '#0f0a1e',
        'matri-card':         'rgba(26,16,53,0.85)',
        'matri-border':       'rgba(168,85,247,0.15)',
        // Text
        'matri-text':         '#e2e8f0',
        'matri-text-2':       '#9ca3af',
        'matri-text-3':       '#6b7280',
      },
      backgroundImage: {
        'matri-bg-gradient':    'linear-gradient(160deg, #0d0a1a, #0f0a1e, #12103a)',
        'matri-bg-gradient-light': 'linear-gradient(160deg, #fffbf0, #fef9ec, #fdf4e3)',
        'matri-amber-gradient': 'linear-gradient(135deg, #f59e0b, #d97706)',
        'matri-purple-gradient':'linear-gradient(135deg, #a855f7, #7c3aed)',
        'matri-card-gradient':  'linear-gradient(135deg, #1a1035, #2d1b69)',
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
    },
  },
  plugins: [],
}
```

- [ ] **Step 2: Añadir CSS variables para temas en App.css**

Añade al final de `src/frontend/src/App.css`:

```css
/* ===== MATRIPUNTOS THEME SYSTEM ===== */

/* Dark theme (default) */
:root,
html.dark {
  --matri-bg: linear-gradient(160deg, #0d0a1a, #0f0a1e, #12103a);
  --matri-card-bg: rgba(26, 16, 53, 0.85);
  --matri-card-border: rgba(168, 85, 247, 0.15);
  --matri-text: #e2e8f0;
  --matri-text-2: #9ca3af;
  --matri-text-3: #6b7280;
  --matri-amber: #f59e0b;
  --matri-amber-dark: #d97706;
  --matri-purple: #a855f7;
  --matri-purple-dark: #7c3aed;
  --matri-green: #22c55e;
  --matri-nav-bg: rgba(26, 16, 53, 0.9);
  --matri-nav-border: rgba(255, 255, 255, 0.05);
  --matri-input-bg: rgba(26, 16, 53, 0.6);
  --matri-input-border: rgba(168, 85, 247, 0.2);
}

/* Light theme */
html.light {
  --matri-bg: linear-gradient(160deg, #fffbf0, #fef9ec, #fdf4e3);
  --matri-card-bg: #ffffff;
  --matri-card-border: #fde68a;
  --matri-text: #1c1917;
  --matri-text-2: #78716c;
  --matri-text-3: #9ca3af;
  --matri-amber: #d97706;
  --matri-amber-dark: #b45309;
  --matri-purple: #7c3aed;
  --matri-purple-dark: #6d28d9;
  --matri-green: #16a34a;
  --matri-nav-bg: #ffffff;
  --matri-nav-border: #e5e7eb;
  --matri-input-bg: #ffffff;
  --matri-input-border: #fde68a;
}

/* Apply background to app root */
html.dark #root,
html:not(.light) #root {
  background: var(--matri-bg);
  min-height: 100vh;
}

html.light #root {
  background: var(--matri-bg);
  min-height: 100vh;
}
```

- [ ] **Step 3: Verificar que Tailwind compila correctamente**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error|Error|✓|built"
```

Resultado esperado: build exitoso sin errores TypeScript relacionados con tailwind.config.

- [ ] **Step 4: Commit**

```bash
git add src/frontend/tailwind.config.js src/frontend/src/App.css
git commit -m "feat(v1.1): add matri-* color tokens and dark/light CSS variables"
```

---

## Task 2: Theme toggle en Zustand + App

**Files:**
- Modify: `src/frontend/src/store/useAppStore.ts`
- Modify: `src/frontend/src/App.tsx`

- [ ] **Step 1: Añadir `theme` al store de Zustand**

En `src/frontend/src/store/useAppStore.ts`, añade `theme` al estado e interfaz:

```typescript
// Añadir a la interfaz AppState (después de isAuthenticated):
  theme: 'dark' | 'light'
  setTheme: (theme: 'dark' | 'light') => void
  toggleTheme: () => void
```

En el `create<AppState>((set) => ({`, añade (después de `isAuthenticated: false,`):

```typescript
  theme: (localStorage.getItem('matri-theme') as 'dark' | 'light') || 'dark',

  setTheme: (theme) => {
    localStorage.setItem('matri-theme', theme)
    document.documentElement.className = theme
    set({ theme })
  },

  toggleTheme: () => {
    set((state) => {
      const next = state.theme === 'dark' ? 'light' : 'dark'
      localStorage.setItem('matri-theme', next)
      document.documentElement.className = next
      return { theme: next }
    })
  },
```

- [ ] **Step 2: Aplicar el tema al cargar la app en App.tsx**

En `src/frontend/src/App.tsx`, modifica la función `App`:

```typescript
function App() {
  // Apply saved theme on mount
  useEffect(() => {
    const saved = localStorage.getItem('matri-theme') as 'dark' | 'light' | null
    document.documentElement.className = saved || 'dark'
  }, [])

  return (
    <QueryClientProvider client={queryClient}>
      <Router>
        <AppRoutes />
      </Router>
    </QueryClientProvider>
  )
}
```

Añade `useEffect` al import de React si no está: `import { useEffect } from 'react'` (ya existe en App.tsx — verificar).

- [ ] **Step 3: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

Esperado: sin errores TypeScript.

- [ ] **Step 4: Commit**

```bash
git add src/frontend/src/store/useAppStore.ts src/frontend/src/App.tsx
git commit -m "feat(v1.1): add theme toggle to Zustand store with localStorage persistence"
```

---

## Task 3: BottomNav component + routing refactor

**Files:**
- Create: `src/frontend/src/components/BottomNav.tsx`
- Modify: `src/frontend/src/App.tsx`
- Modify: `src/frontend/src/pages/Dashboard.tsx` (eliminar currentView state)

- [ ] **Step 1: Crear BottomNav component**

Crea `src/frontend/src/components/BottomNav.tsx`:

```typescript
import { useNavigate, useLocation } from 'react-router-dom'
import { Home, CheckSquare, Calendar, Trophy, Plus } from 'lucide-react'

const TABS = [
  { path: '/dashboard', icon: Home, label: 'Inicio' },
  { path: '/tasks', icon: CheckSquare, label: 'Tareas' },
  null, // placeholder for FAB
  { path: '/calendar', icon: Calendar, label: 'Calendario' },
  { path: '/achievements', icon: Trophy, label: 'Logros' },
]

interface BottomNavProps {
  onPlusPress?: () => void
}

export function BottomNav({ onPlusPress }: BottomNavProps) {
  const navigate = useNavigate()
  const location = useLocation()

  return (
    <nav
      className="fixed bottom-0 left-0 right-0 z-50 flex items-center justify-around px-2 pb-safe"
      style={{
        background: 'var(--matri-nav-bg)',
        borderTop: '1px solid var(--matri-nav-border)',
        paddingTop: '8px',
        paddingBottom: 'max(8px, env(safe-area-inset-bottom))',
      }}
    >
      {TABS.map((tab, i) => {
        if (!tab) {
          // FAB central
          return (
            <button
              key="fab"
              onClick={onPlusPress}
              className="flex items-center justify-center rounded-full shadow-lg"
              style={{
                width: 44,
                height: 44,
                background: 'linear-gradient(135deg, #f59e0b, #d97706)',
                marginTop: -20,
                boxShadow: '0 4px 12px rgba(245,158,11,0.4)',
              }}
              aria-label="Añadir"
            >
              <Plus className="w-5 h-5 text-white" strokeWidth={2.5} />
            </button>
          )
        }

        const isActive = location.pathname === tab.path
        const Icon = tab.icon

        return (
          <button
            key={tab.path}
            onClick={() => navigate(tab.path)}
            className="flex flex-col items-center gap-0.5 min-w-[52px]"
          >
            <Icon
              className="w-5 h-5"
              style={{ color: isActive ? 'var(--matri-amber)' : 'var(--matri-text-3)' }}
            />
            <span
              className="text-[10px]"
              style={{ color: isActive ? 'var(--matri-amber)' : 'var(--matri-text-3)' }}
            >
              {tab.label}
            </span>
          </button>
        )
      })}
    </nav>
  )
}
```

- [ ] **Step 2: Añadir padding bottom al root para que BottomNav no tape contenido**

En `src/frontend/src/App.css`, añade al bloque existente de `#root`:

```css
#root {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  padding-bottom: 64px; /* espacio para BottomNav */
}
```

- [ ] **Step 3: Eliminar currentView state de Dashboard y limpiar imports innecesarios**

En `src/frontend/src/pages/Dashboard.tsx`:

1. Elimina los imports de `RequestActivity`, `RequestInbox`, `Tasks` (ya son rutas propias).
2. Elimina el estado `currentView` y su tipo.
3. Elimina `handleBack` y los bloques condicionales que renderizan `RequestActivity` / `RequestInbox`.
4. Elimina el import de `useLocation` si solo se usaba para `currentView`.

El state que queda en Dashboard: `refreshCounter`, `events`, `pendingTaskCount`, `isLoading`, `error`, `chartData`, `chartNames`, `balance`.

- [ ] **Step 4: Añadir BottomNav al Dashboard con acción del FAB**

En `src/frontend/src/pages/Dashboard.tsx`, añade el import:

```typescript
import { BottomNav } from '../components/BottomNav'
```

Y en el JSX del return principal, añade justo antes del cierre del div raíz:

```tsx
<BottomNav onPlusPress={() => navigate('/request-activity')} />
```

- [ ] **Step 5: Añadir BottomNav a las otras páginas principales**

Añadir `<BottomNav />` al return de:
- `src/frontend/src/pages/Tasks.tsx` — `onPlusPress={() => { /* abrir modal crear tarea */ }}`
- `src/frontend/src/pages/Calendar.tsx`
- `src/frontend/src/pages/Achievements.tsx`

En cada archivo: importar `BottomNav` y añadirla antes del cierre del div raíz. El `onPlusPress` en Tasks puede dejarse sin handler por ahora (se desarrollará en v1.3).

- [ ] **Step 6: Verificar build y navegación**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

Esperado: build sin errores.

- [ ] **Step 7: Commit**

```bash
git add src/frontend/src/components/BottomNav.tsx src/frontend/src/pages/Dashboard.tsx src/frontend/src/pages/Tasks.tsx src/frontend/src/pages/Calendar.tsx src/frontend/src/pages/Achievements.tsx src/frontend/src/App.css
git commit -m "feat(v1.1): add BottomNav with FAB, refactor routing away from currentView state"
```

---

## Task 4: Schema DB + API — avatares, mood, theme

**Files:**
- Modify: `src/backend/prisma/schema.prisma`
- Modify: `src/backend/src/routes/profile.ts`
- Modify: `src/backend/src/routes/authRoutes.ts`

- [ ] **Step 1: Añadir campos a UserProfile en schema.prisma**

En `src/backend/prisma/schema.prisma`, dentro del modelo `UserProfile`, añade estos campos antes de `createdAt`:

```prisma
  // Avatar & personalization (v1.1)
  avatarEmoji    String   @default("🐼")
  avatarColor    String   @default("#7c3aed")
  theme          String   @default("dark")

  // Mood del día (v1.1)
  currentMood    String?
  moodUpdatedAt  DateTime?
```

- [ ] **Step 2: Crear y aplicar migración**

```bash
cd src/backend && npx prisma migrate dev --name v1_1_avatar_mood_theme
```

Resultado esperado: migración creada y aplicada, `dev.db` actualizado.

- [ ] **Step 3: Añadir `PUT /profile/me` para actualizar mood/avatar/theme**

En `src/backend/src/routes/profile.ts`, añade este nuevo endpoint antes del último `export default router`:

```typescript
/**
 * PUT /api/profile/me
 * Update current user's avatar, mood, and/or theme
 */
router.put('/me', async (req: Request, res: Response) => {
  try {
    const userId = (req as any).user.id
    const { avatarEmoji, avatarColor, theme, currentMood } = req.body

    // Ensure profile exists
    const existing = await prisma.userProfile.findUnique({ where: { userId } })
    if (!existing) {
      return res.status(404).json({ error: 'Profile not found. Complete onboarding first.' })
    }

    const updated = await prisma.userProfile.update({
      where: { userId },
      data: {
        ...(avatarEmoji !== undefined && { avatarEmoji }),
        ...(avatarColor !== undefined && { avatarColor }),
        ...(theme !== undefined && { theme }),
        ...(currentMood !== undefined && {
          currentMood,
          moodUpdatedAt: new Date(),
        }),
      },
    })

    res.json({ message: 'Profile updated', profile: updated })
  } catch (error) {
    res.status(500).json({ error: 'Failed to update profile' })
  }
})
```

- [ ] **Step 4: Actualizar `GET /auth/me` para devolver partnerMood**

En `src/backend/src/routes/authRoutes.ts`, localiza `router.get('/me', ...)` (línea ~120) y reemplaza su implementación:

```typescript
router.get('/me', authMiddleware, async (req: Request, res: Response): Promise<void> => {
  try {
    if (!req.userId) {
      res.status(401).json({ error: 'User ID not found in token' })
      return
    }

    const user = await getUserById(req.userId)

    // Load own profile for avatar/mood/theme
    const myProfile = await prisma.userProfile.findUnique({
      where: { userId: req.userId },
      select: { avatarEmoji: true, avatarColor: true, theme: true, currentMood: true, moodUpdatedAt: true },
    })

    // Load partner mood if couple exists
    let partnerMood: string | null = null
    let partnerName: string | null = null
    if (user.coupleId) {
      const couple = await prisma.couple.findUnique({
        where: { id: user.coupleId },
        include: { users: { include: { profile: { select: { currentMood: true, moodUpdatedAt: true } } } } },
      })
      const partner = couple?.users.find(u => u.id !== req.userId)
      if (partner) {
        partnerName = partner.name
        // Only show mood if updated today
        const today = new Date()
        const moodDate = partner.profile?.moodUpdatedAt
        if (moodDate && moodDate.toDateString() === today.toDateString()) {
          partnerMood = partner.profile?.currentMood ?? null
        }
      }
    }

    res.json({
      user: {
        id: user.id,
        email: user.email,
        name: user.name,
        coupleId: user.coupleId,
        roleInHome: user.roleInHome,
        timezone: user.timezone,
        notificationsPush: user.notificationsPush,
        notificationsEmail: user.notificationsEmail,
        // Profile fields
        avatarEmoji: myProfile?.avatarEmoji ?? '🐼',
        avatarColor: myProfile?.avatarColor ?? '#7c3aed',
        theme: myProfile?.theme ?? 'dark',
        currentMood: myProfile?.currentMood ?? null,
      },
      partnerMood,
      partnerName,
    })
  } catch (error) {
    const message = error instanceof Error ? error.message : 'Failed to fetch user'
    res.status(400).json({ error: message })
  }
})
```

Añade el import de prisma al principio del archivo si no existe:
```typescript
import prisma from '../lib/prisma.js'
```

- [ ] **Step 5: Actualizar tipo User en el frontend**

En `src/frontend/src/types/index.ts`, añade los nuevos campos a la interfaz `User`:

```typescript
// Añadir dentro de la interfaz User existente:
avatarEmoji?: string
avatarColor?: string
theme?: 'dark' | 'light'
currentMood?: string | null
```

- [ ] **Step 6: Verificar que el backend compila**

```bash
cd src/backend && npm run build 2>&1 | grep -E "error TS|error:|✓|built"
```

Esperado: sin errores TypeScript.

- [ ] **Step 7: Commit**

```bash
git add src/backend/prisma/schema.prisma src/backend/prisma/migrations src/backend/src/routes/profile.ts src/backend/src/routes/authRoutes.ts src/frontend/src/types/index.ts
git commit -m "feat(v1.1): add avatar/mood/theme fields to UserProfile, update GET /me with partnerMood"
```

---

## Task 5: Avatar component + MoodSelector + AvatarSelector

**Files:**
- Create: `src/frontend/src/components/Avatar.tsx`
- Create: `src/frontend/src/components/MoodSelector.tsx`
- Create: `src/frontend/src/components/AvatarSelector.tsx`

- [ ] **Step 1: Crear Avatar component**

Crea `src/frontend/src/components/Avatar.tsx`:

```typescript
import { useState } from 'react'
import { MoodSelector } from './MoodSelector'

interface AvatarProps {
  emoji: string
  color: string
  mood?: string | null
  size?: 'sm' | 'md' | 'lg'
  interactive?: boolean // si true, tap abre MoodSelector
  onMoodChange?: (mood: string) => void
}

const SIZE_MAP = {
  sm: { outer: 28, inner: 13, badge: 12, badgeOffset: -2 },
  md: { outer: 36, inner: 18, badge: 14, badgeOffset: -2 },
  lg: { outer: 48, inner: 24, badge: 16, badgeOffset: -3 },
}

export function Avatar({ emoji, color, mood, size = 'md', interactive = false, onMoodChange }: AvatarProps) {
  const [showMoodSelector, setShowMoodSelector] = useState(false)
  const s = SIZE_MAP[size]

  return (
    <>
      <div
        style={{ position: 'relative', display: 'inline-block', cursor: interactive ? 'pointer' : 'default' }}
        onClick={interactive ? () => setShowMoodSelector(true) : undefined}
      >
        <div
          style={{
            width: s.outer,
            height: s.outer,
            borderRadius: '50%',
            background: color,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontSize: s.inner,
            userSelect: 'none',
          }}
        >
          {emoji}
        </div>
        {mood && (
          <div
            style={{
              position: 'absolute',
              bottom: s.badgeOffset,
              right: s.badgeOffset,
              width: s.badge,
              height: s.badge,
              borderRadius: '50%',
              background: 'var(--matri-bg, #0f0a1e)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontSize: s.badge * 0.7,
            }}
          >
            {mood}
          </div>
        )}
      </div>
      {interactive && showMoodSelector && (
        <MoodSelector
          current={mood ?? null}
          onSelect={(m) => {
            onMoodChange?.(m)
            setShowMoodSelector(false)
          }}
          onClose={() => setShowMoodSelector(false)}
        />
      )}
    </>
  )
}
```

- [ ] **Step 2: Crear MoodSelector component**

Crea `src/frontend/src/components/MoodSelector.tsx`:

```typescript
const MOODS = [
  { emoji: '😊', label: 'Animado/a' },
  { emoji: '😴', label: 'Cansado/a' },
  { emoji: '😤', label: 'Estresado/a' },
  { emoji: '🥰', label: 'Contento/a' },
  { emoji: '😰', label: 'Agobiado/a' },
  { emoji: '🧘', label: 'Tranquilo/a' },
  { emoji: '😂', label: 'De buen humor' },
  { emoji: '🤯', label: 'Saturado/a' },
]

interface MoodSelectorProps {
  current: string | null
  onSelect: (mood: string) => void
  onClose: () => void
}

export function MoodSelector({ current, onSelect, onClose }: MoodSelectorProps) {
  return (
    <>
      {/* Backdrop */}
      <div
        style={{
          position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', zIndex: 100,
        }}
        onClick={onClose}
      />
      {/* Sheet */}
      <div
        style={{
          position: 'fixed', bottom: 0, left: 0, right: 0, zIndex: 101,
          background: 'var(--matri-card-bg)',
          borderTop: '1px solid var(--matri-card-border)',
          borderRadius: '16px 16px 0 0',
          padding: '20px 16px 32px',
        }}
      >
        <p style={{ color: 'var(--matri-text)', fontWeight: 600, marginBottom: 16, textAlign: 'center' }}>
          ¿Cómo estás hoy?
        </p>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 12 }}>
          {MOODS.map(({ emoji, label }) => (
            <button
              key={emoji}
              onClick={() => onSelect(emoji)}
              style={{
                display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 4,
                padding: '10px 4px',
                borderRadius: 10,
                border: current === emoji
                  ? '2px solid var(--matri-amber)'
                  : '2px solid transparent',
                background: current === emoji
                  ? 'rgba(245,158,11,0.1)'
                  : 'rgba(255,255,255,0.04)',
                cursor: 'pointer',
              }}
            >
              <span style={{ fontSize: 28 }}>{emoji}</span>
              <span style={{ fontSize: 10, color: 'var(--matri-text-2)', textAlign: 'center' }}>{label}</span>
            </button>
          ))}
        </div>
      </div>
    </>
  )
}
```

- [ ] **Step 3: Crear AvatarSelector component**

Crea `src/frontend/src/components/AvatarSelector.tsx`:

```typescript
import { useState } from 'react'
import { Avatar } from './Avatar'

const AVATAR_EMOJIS = ['🐼', '🦊', '🐧', '🐸', '🦄', '🐯', '🐻', '🦁', '🐨', '🐙', '🦋', '🐺', '🦝', '🐮', '🐷', '🐰']
const AVATAR_COLORS = [
  '#7c3aed', // púrpura
  '#1d4ed8', // azul
  '#be185d', // rosa
  '#15803d', // verde
  '#d97706', // amber
  '#0284c7', // cyan
  '#e11d48', // rojo
  '#374151', // gris
]

interface AvatarSelectorProps {
  currentEmoji: string
  currentColor: string
  onChange: (emoji: string, color: string) => void
}

export function AvatarSelector({ currentEmoji, currentColor, onChange }: AvatarSelectorProps) {
  const [emoji, setEmoji] = useState(currentEmoji)
  const [color, setColor] = useState(currentColor)

  const handleChange = (newEmoji: string, newColor: string) => {
    setEmoji(newEmoji)
    setColor(newColor)
    onChange(newEmoji, newColor)
  }

  return (
    <div>
      {/* Preview */}
      <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 20 }}>
        <Avatar emoji={emoji} color={color} size="lg" />
      </div>

      {/* Emoji grid */}
      <p style={{ color: 'var(--matri-text-2)', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: '0.5px' }}>
        Personaje
      </p>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(8, 1fr)', gap: 8, marginBottom: 16 }}>
        {AVATAR_EMOJIS.map((e) => (
          <button
            key={e}
            onClick={() => handleChange(e, color)}
            style={{
              width: 40, height: 40, borderRadius: '50%',
              background: e === emoji ? color : 'rgba(255,255,255,0.06)',
              border: e === emoji ? '2px solid var(--matri-amber)' : '2px solid transparent',
              fontSize: 20, cursor: 'pointer',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}
          >
            {e}
          </button>
        ))}
      </div>

      {/* Color grid */}
      <p style={{ color: 'var(--matri-text-2)', fontSize: 11, marginBottom: 8, textTransform: 'uppercase', letterSpacing: '0.5px' }}>
        Color de fondo
      </p>
      <div style={{ display: 'flex', gap: 10, flexWrap: 'wrap' }}>
        {AVATAR_COLORS.map((c) => (
          <button
            key={c}
            onClick={() => handleChange(emoji, c)}
            style={{
              width: 28, height: 28, borderRadius: '50%',
              background: c,
              border: c === color ? '3px solid var(--matri-amber)' : '3px solid transparent',
              cursor: 'pointer',
              outline: c === color ? '2px solid rgba(245,158,11,0.3)' : 'none',
            }}
          />
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

Esperado: sin errores TypeScript.

- [ ] **Step 5: Commit**

```bash
git add src/frontend/src/components/Avatar.tsx src/frontend/src/components/MoodSelector.tsx src/frontend/src/components/AvatarSelector.tsx
git commit -m "feat(v1.1): add Avatar, MoodSelector, AvatarSelector components"
```

---

## Task 6: Daily phrase utility

**Files:**
- Create: `src/frontend/src/utils/dailyPhrase.ts`

- [ ] **Step 1: Crear dailyPhrase.ts con 60 frases**

Crea `src/frontend/src/utils/dailyPhrase.ts`:

```typescript
const FRASES: string[] = [
  "El equilibrio no es hacer lo mismo, es valorar lo que hace el otro.",
  "Una pareja que negocia junta, crece junta.",
  "Hoy es buen día para sorprender al otro con algo inesperado.",
  "Los puntos no mienten. El historial tampoco.",
  "Pequeñas tareas, grandes gestos.",
  "La justicia doméstica empieza por reconocer el esfuerzo invisible.",
  "El mejor acuerdo es el que ambos firmaron con ganas.",
  "Cada tarea es una inversión en el equipo.",
  "Negociar no es perder, es encontrar el punto justo.",
  "Un hogar ordenado empieza por dos personas alineadas.",
  "Hoy puede ser el día de romper la racha o de alargarla un día más.",
  "El que negocia bien vive mejor.",
  "No hay tarea pequeña cuando se hace con intención.",
  "Dos cabezas piensan mejor, y dos pares de manos hacen más.",
  "La equidad no se mide en horas, sino en reconocimiento.",
  "Hoy es un buen día para proponer algo y que te digan que sí.",
  "El hogar perfecto no existe, pero el hogar justo sí.",
  "Registrar lo que haces no es presumir, es visibilizar.",
  "Un punto ganado hoy es una conversación evitada mañana.",
  "La constancia es el superpoder de las parejas que funcionan.",
  "¿Cuándo fue la última vez que reconociste algo que hizo el otro?",
  "Las tareas compartidas pesan menos.",
  "La racha es frágil. Cuídala.",
  "Hoy podrías ser tú quien tome la iniciativa.",
  "Lo que se ve en el historial no se puede negar.",
  "El desacuerdo de hoy puede ser el acuerdo de mañana.",
  "Una contraoferta no es un rechazo, es una conversación.",
  "Las parejas que se ponen de acuerdo en las cosas pequeñas, resisten las grandes.",
  "Hacer juntos lo aburrido es, en realidad, hacer juntos la vida.",
  "Hay días de ×1.0 y días de ×2.0. Hoy, ¿cuál es el tuyo?",
  "El reconocimiento pesa más que los puntos.",
  "Cada semana es una nueva oportunidad de equilibrar la balanza.",
  "Lo urgente no siempre es lo importante. Pero hay que hacerlo igual.",
  "Proponer es más valiente que esperar.",
  "No hay app que reemplace la conversación, pero ayuda tenerla.",
  "Un hogar en equilibrio es un hogar en paz.",
  "Las tareas domésticas son el trabajo más subestimado del mundo.",
  "Hoy es el día para decir 'lo hago yo'.",
  "La pareja que comparte las tareas comparte también la tranquilidad.",
  "Un buen acuerdo es el que los dos pueden explicar sin dudar.",
  "Hay semanas de racha y semanas para recuperar. Las dos cuentan.",
  "El primer paso de la equidad es hacer visible lo invisible.",
  "Negociar es un acto de respeto mutuo.",
  "Las parejas más felices no son las que pelean menos, sino las que resuelven más.",
  "Hoy, ¿qué puedes hacer tú para que el otro descanse un poco?",
  "El historial es la memoria objetiva de la pareja.",
  "Un punto a tiempo ahorra una discusión.",
  "El hogar es el proyecto más largo que jamás empezarás.",
  "La constancia semanal vale más que el heroísmo puntual.",
  "Registrar no es vigilar, es ser transparente.",
  "El trabajo invisible merece puntos visibles.",
  "¿Lleváis una semana sin negociar nada? Algo está muy bien… o muy mal.",
  "Dos personas, un equipo, cero excusas.",
  "Hoy puede ser un día de equilibrio perfecto. Solo hay que intentarlo.",
  "La magia de Matripuntos: convierte lo cotidiano en algo que vale la pena.",
  "Cuando ambos aportan, ambos ganan.",
  "Una tarea completada es un gesto de amor sin romantizarlo demasiado.",
  "El mejor multiplicador es el compromiso.",
  "Lo que hoy parece rutina, mañana será racha.",
  "Equipo que puntúa junto, hogar que funciona junto.",
]

export function getDailyPhrase(): string {
  const now = new Date()
  const start = new Date(now.getFullYear(), 0, 0)
  const diff = now.getTime() - start.getTime()
  const dayOfYear = Math.floor(diff / (1000 * 60 * 60 * 24))
  return FRASES[dayOfYear % FRASES.length]
}
```

- [ ] **Step 2: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

- [ ] **Step 3: Commit**

```bash
git add src/frontend/src/utils/dailyPhrase.ts
git commit -m "feat(v1.1): add getDailyPhrase utility with 60 motivational phrases"
```

---

## Task 7: Rediseño del Dashboard

**Files:**
- Modify: `src/frontend/src/pages/Dashboard.tsx`

Este task rediseña el Dashboard con la nueva paleta, integra el Avatar con mood, la frase diaria y el contexto del partner. El Dashboard.tsx actual tiene 412 líneas — lo reescribimos en su totalidad.

- [ ] **Step 1: Actualizar imports del Dashboard**

Al principio de `src/frontend/src/pages/Dashboard.tsx`, reemplaza los imports existentes con:

```typescript
import { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import { BarChart3, Settings, TrendingUp, TrendingDown, Loader, Moon, Sun } from 'lucide-react'
import { useAppStore } from '../store/useAppStore'
import { apiClient, fetchRecentActivity } from '../services/apiClient'
import { NotificationBell } from '../components/NotificationBell'
import { RecentMovementItem } from '../components/RecentMovementItem'
import { type RecentActivity } from '../types/activity'
import { AchievementsWidget } from '../components/AchievementsWidget'
import { BottomNav } from '../components/BottomNav'
import { Avatar } from '../components/Avatar'
import { getDailyPhrase } from '../utils/dailyPhrase'
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend, ReferenceLine } from 'recharts'
```

- [ ] **Step 2: Actualizar el estado y la lógica del Dashboard**

Localiza el bloque de `useState` y `useEffect` principal. Añade las siguientes variables de estado al bloque existente (después de `const [balance, setBalance] = useState`):

```typescript
const { user, couple, logout, toggleTheme, theme } = useAppStore()
const [partnerMood, setPartnerMood] = useState<string | null>(null)
const [partnerName, setPartnerName] = useState<string | null>(null)
const dailyPhrase = getDailyPhrase()
```

Modifica `loadUserData` al inicio del `useEffect` para también leer `partnerMood` y `partnerName` de la respuesta de `/auth/me`. Reemplaza la llamada `apiClient.auth.getMe()` existente dentro del useEffect con:

```typescript
// Al inicio del useEffect loadData, antes de los Promise.all actuales:
const meResponse = await apiClient.auth.getMe()
if (meResponse.partnerMood) setPartnerMood(meResponse.partnerMood)
if (meResponse.partnerName) setPartnerName(meResponse.partnerName)
```

> Nota: `apiClient.auth.getMe()` puede devolver directamente `{ user, partnerMood, partnerName }` — verificar la firma en `apiClient.ts` y adaptar si es necesario.

- [ ] **Step 3: Reemplazar el header del Dashboard**

Localiza el JSX del header (el div con el saludo y los botones de settings/logout). Reemplázalo con:

```tsx
{/* Header */}
<header className="flex items-center justify-between px-4 pt-4 pb-2">
  <div>
    <h1 style={{ color: 'var(--matri-text)', fontWeight: 700, fontSize: 16 }}>
      Hola, {user?.name?.split(' ')[0]} ☀️
    </h1>
    {partnerMood && partnerName && (
      <p style={{ color: 'var(--matri-text-3)', fontSize: 11, marginTop: 2 }}>
        {partnerName} está {partnerMood} hoy
      </p>
    )}
  </div>
  <div className="flex items-center gap-3">
    <button onClick={toggleTheme} style={{ color: 'var(--matri-text-2)' }}>
      {theme === 'dark'
        ? <Sun className="w-5 h-5" />
        : <Moon className="w-5 h-5" />
      }
    </button>
    <NotificationBell />
    <Avatar
      emoji={user?.avatarEmoji ?? '🐼'}
      color={user?.avatarColor ?? '#7c3aed'}
      mood={user?.currentMood}
      size="md"
      interactive
      onMoodChange={async (mood) => {
        await apiClient.profile.updateMe({ currentMood: mood })
        // Actualizar el store localmente sin refetch
        useAppStore.setState(s => ({
          user: s.user ? { ...s.user, currentMood: mood } : s.user
        }))
      }}
    />
  </div>
</header>
```

- [ ] **Step 4: Actualizar los fondos y cards del Dashboard a la nueva paleta**

Localiza el div raíz del return. Reemplaza sus clases/estilos por:

```tsx
<div style={{ minHeight: '100vh', background: 'var(--matri-bg)' }}>
```

Para cada card de estadística (balance, tareas, racha), reemplaza las clases `bg-white shadow rounded-lg` por:

```tsx
style={{ background: 'var(--matri-card-bg)', border: '1px solid var(--matri-card-border)', borderRadius: 10, padding: 12 }}
```

- [ ] **Step 5: Añadir la card de frase motivacional**

Después de la card de balance, añade:

```tsx
{/* Frase del día */}
<div
  style={{
    background: 'rgba(245,158,11,0.07)',
    border: '1px solid rgba(245,158,11,0.18)',
    borderRadius: 10,
    padding: '10px 12px',
    marginBottom: 12,
  }}
>
  <p style={{ color: 'var(--matri-amber)', fontSize: 9, letterSpacing: '0.5px', textTransform: 'uppercase', marginBottom: 3 }}>
    Frase del día
  </p>
  <p style={{ color: 'var(--matri-text)', fontSize: 12, fontStyle: 'italic', lineHeight: 1.5 }}>
    "{dailyPhrase}"
  </p>
</div>
```

- [ ] **Step 6: Añadir BottomNav al final del return**

Justo antes del cierre del div raíz:

```tsx
<BottomNav onPlusPress={() => navigate('/request-activity')} />
```

- [ ] **Step 7: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

- [ ] **Step 8: Commit**

```bash
git add src/frontend/src/pages/Dashboard.tsx
git commit -m "feat(v1.1): redesign Dashboard with dark palette, Avatar, mood context, daily phrase"
```

---

## Task 8: Settings — avatar selector + theme toggle

**Files:**
- Modify: `src/frontend/src/pages/Settings.tsx`

- [ ] **Step 1: Añadir imports a Settings.tsx**

Al principio de `src/frontend/src/pages/Settings.tsx`, añade:

```typescript
import { AvatarSelector } from '../components/AvatarSelector'
import { Avatar } from '../components/Avatar'
import { useAppStore } from '../store/useAppStore'
```

- [ ] **Step 2: Añadir sección de avatar en Settings**

Localiza la sección de perfil de usuario en Settings. Añade antes o después del campo de nombre una nueva sección:

```tsx
{/* Sección Avatar */}
<div style={{ marginBottom: 24 }}>
  <h3 style={{ color: 'var(--matri-text)', fontWeight: 600, marginBottom: 12 }}>Tu avatar</h3>
  <AvatarSelector
    currentEmoji={user?.avatarEmoji ?? '🐼'}
    currentColor={user?.avatarColor ?? '#7c3aed'}
    onChange={async (emoji, color) => {
      await apiClient.profile.updateMe({ avatarEmoji: emoji, avatarColor: color })
      useAppStore.setState(s => ({
        user: s.user ? { ...s.user, avatarEmoji: emoji, avatarColor: color } : s.user
      }))
    }}
  />
</div>
```

- [ ] **Step 3: Añadir apiClient.profile.updateMe**

En `src/frontend/src/services/apiClient.ts`, dentro del objeto `profile` existente (después de `getCoupleProfile`), añade:

```typescript
    updateMe: (data: {
      avatarEmoji?: string
      avatarColor?: string
      theme?: string
      currentMood?: string
    }) =>
      this.request('/profile/me', {
        method: 'PUT',
        body: JSON.stringify(data),
      }),
```

- [ ] **Step 4: Actualizar fondo de Settings a nueva paleta**

Localiza el div raíz de Settings. Reemplaza `className="min-h-screen bg-gray-50"` (o similar) por:

```tsx
style={{ minHeight: '100vh', background: 'var(--matri-bg)', color: 'var(--matri-text)' }}
```

Actualiza los fondos de las cards internas de `bg-white` a `style={{ background: 'var(--matri-card-bg)', border: '1px solid var(--matri-card-border)' }}`.

- [ ] **Step 5: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

- [ ] **Step 6: Commit**

```bash
git add src/frontend/src/pages/Settings.tsx src/frontend/src/services/apiClient.ts
git commit -m "feat(v1.1): add avatar selector and theme toggle to Settings"
```

---

## Task 9: Onboarding paso 0

**Files:**
- Create: `src/frontend/src/pages/onboarding/OnboardingLanding.tsx`
- Modify: `src/frontend/src/pages/Onboarding.tsx`
- Modify: `src/frontend/src/App.tsx`

- [ ] **Step 1: Crear OnboardingLanding (paso 0)**

Crea `src/frontend/src/pages/onboarding/OnboardingLanding.tsx`:

```typescript
import { useNavigate } from 'react-router-dom'

export function OnboardingLanding() {
  const navigate = useNavigate()

  return (
    <div style={{ minHeight: '100vh', background: 'var(--matri-bg)', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: '32px 24px' }}>

      {/* Logo */}
      <div style={{ fontSize: 48, marginBottom: 8 }}>🏠</div>
      <h1 style={{ color: 'var(--matri-text)', fontSize: 28, fontWeight: 800, marginBottom: 4, textAlign: 'center' }}>
        Matripuntos
      </h1>
      <p style={{ color: 'var(--matri-text-2)', fontSize: 14, marginBottom: 40, textAlign: 'center' }}>
        El hogar justo, en pareja.
      </p>

      {/* Value props */}
      <div style={{ width: '100%', maxWidth: 360, display: 'flex', flexDirection: 'column', gap: 16, marginBottom: 40 }}>
        {[
          { icon: '⚖️', title: 'Equidad real', desc: 'Registrad lo que cada uno hace y negociad los puntos hasta quedar conformes.' },
          { icon: '🎮', title: 'Gamificado', desc: 'Rachas, logros y niveles para que cuidar el hogar sea menos un deber y más un juego.' },
          { icon: '🤝', title: 'Sin peleas innecesarias', desc: 'El historial habla por vosotros. Los datos no discuten.' },
        ].map(({ icon, title, desc }) => (
          <div
            key={title}
            style={{
              display: 'flex', gap: 14, alignItems: 'flex-start',
              background: 'var(--matri-card-bg)',
              border: '1px solid var(--matri-card-border)',
              borderRadius: 12, padding: '14px 16px',
            }}
          >
            <span style={{ fontSize: 24, flexShrink: 0 }}>{icon}</span>
            <div>
              <p style={{ color: 'var(--matri-text)', fontWeight: 600, fontSize: 14, marginBottom: 2 }}>{title}</p>
              <p style={{ color: 'var(--matri-text-2)', fontSize: 12, lineHeight: 1.5 }}>{desc}</p>
            </div>
          </div>
        ))}
      </div>

      {/* CTAs */}
      <div style={{ width: '100%', maxWidth: 360, display: 'flex', flexDirection: 'column', gap: 10 }}>
        <button
          onClick={() => navigate('/signup')}
          style={{
            width: '100%', padding: '14px', borderRadius: 12,
            background: 'linear-gradient(135deg, #f59e0b, #d97706)',
            color: '#fff', fontWeight: 700, fontSize: 15, border: 'none', cursor: 'pointer',
          }}
        >
          Crear cuenta gratis
        </button>
        <button
          onClick={() => navigate('/login')}
          style={{
            width: '100%', padding: '14px', borderRadius: 12,
            background: 'transparent',
            border: '1px solid var(--matri-card-border)',
            color: 'var(--matri-text-2)', fontWeight: 500, fontSize: 14, cursor: 'pointer',
          }}
        >
          Ya tengo cuenta
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Añadir ruta `/` para OnboardingLanding en App.tsx**

En `src/frontend/src/App.tsx`, modifica la ruta raíz:

```typescript
import { OnboardingLanding } from './pages/onboarding/OnboardingLanding'
```

Y en el `<Routes>`, reemplaza:
```tsx
<Route path="/" element={<Navigate to="/dashboard" replace />} />
```
Por:
```tsx
<Route
  path="/"
  element={
    isAuthenticated
      ? <Navigate to="/dashboard" replace />
      : <OnboardingLanding />
  }
/>
```

Asegúrate de que `isAuthenticated` está disponible en `AppRoutes` (ya lo está vía `useAppStore`).

- [ ] **Step 3: Verificar build**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

- [ ] **Step 4: Commit**

```bash
git add src/frontend/src/pages/onboarding/OnboardingLanding.tsx src/frontend/src/App.tsx
git commit -m "feat(v1.1): add OnboardingLanding (step 0) with value proposition"
```

---

## Task 10: Paleta en Tasks, Calendar y componentes base

**Files:**
- Modify: `src/frontend/src/pages/Tasks.tsx`
- Modify: `src/frontend/src/pages/Calendar.tsx`
- Modify: `src/frontend/src/components/StatCard.tsx`
- Modify: `src/frontend/src/components/Card.tsx`
- Modify: `src/frontend/src/components/Button.tsx`

- [ ] **Step 1: Actualizar StatCard.tsx a nueva paleta**

En `src/frontend/src/components/StatCard.tsx`, reemplaza los estilos de fondo (`bg-white`, `shadow`, clases de Tailwind legacy) con variables CSS:

```tsx
// El div raíz del componente:
<div style={{
  background: 'var(--matri-card-bg)',
  border: '1px solid var(--matri-card-border)',
  borderRadius: 10,
  padding: '12px 14px',
}}>
  <p style={{ color: 'var(--matri-text-3)', fontSize: 10, letterSpacing: '0.5px', textTransform: 'uppercase' }}>
    {title}
  </p>
  <p style={{ color: 'var(--matri-text)', fontSize: 22, fontWeight: 700, marginTop: 2 }}>
    {value}
  </p>
  {subtitle && (
    <p style={{ color: 'var(--matri-text-2)', fontSize: 11, marginTop: 2 }}>{subtitle}</p>
  )}
</div>
```

Adapta según las props reales del componente.

- [ ] **Step 2: Actualizar Card.tsx y Button.tsx**

En `src/frontend/src/components/Card.tsx`, reemplaza `bg-white shadow` por `var(--matri-card-bg)` y `var(--matri-card-border)`.

En `src/frontend/src/components/Button.tsx`, reemplaza la variante primary (`bg-primary`) por `background: 'linear-gradient(135deg, #f59e0b, #d97706)'` con texto blanco. Mantén las otras variantes (outline, ghost) adaptadas a la nueva paleta.

- [ ] **Step 3: Actualizar fondo de Tasks.tsx**

En `src/frontend/src/pages/Tasks.tsx`:
1. Div raíz: `style={{ minHeight: '100vh', background: 'var(--matri-bg)' }}`
2. Cards de tareas: `style={{ background: 'var(--matri-card-bg)', border: '1px solid var(--matri-card-border)' }}`
3. Header/título: `style={{ color: 'var(--matri-text)' }}`
4. Texto secundario: `style={{ color: 'var(--matri-text-2)' }}`
5. Asegúrate de que `<BottomNav />` está presente (añadido en Task 3).

- [ ] **Step 4: Actualizar fondo de Calendar.tsx**

Mismo patrón que Tasks: fondo `var(--matri-bg)`, cards `var(--matri-card-bg)`, textos con variables CSS.

En `CalendarMonth.tsx`, `CalendarWeek.tsx`, `CalendarDay.tsx`: mismas sustituciones de paleta.

- [ ] **Step 5: Verificar build final**

```bash
cd src/frontend && npm run build 2>&1 | grep -E "error TS|✓|built"
```

Esperado: build limpio.

- [ ] **Step 6: Commit**

```bash
git add src/frontend/src/pages/Tasks.tsx src/frontend/src/pages/Calendar.tsx src/frontend/src/components/CalendarMonth.tsx src/frontend/src/components/CalendarWeek.tsx src/frontend/src/components/CalendarDay.tsx src/frontend/src/components/StatCard.tsx src/frontend/src/components/Card.tsx src/frontend/src/components/Button.tsx
git commit -m "feat(v1.1): apply dark/light palette to Tasks, Calendar, and base components"
```

---

## Task 11: Tag de versión + deploy

**Files:**
- `src/frontend/dist/` (build artefacto)

- [ ] **Step 1: Build final del frontend**

```bash
cd src/frontend && npm run build 2>&1 | tail -5
```

Esperado: build sin errores, carpeta `dist/` actualizada.

- [ ] **Step 2: Push a main para trigger de Render**

```bash
cd /ruta/al/repo && git push origin main
```

- [ ] **Step 3: Deploy frontend por FTP**

```bash
source .deploy-credentials && lftp -u "$FTP_USER,$FTP_PASS" "$FTP_HOST" -e "set ssl:verify-certificate no; mirror -R --verbose src/frontend/dist/ / ; quit"
```

- [ ] **Step 4: Aplicar tag v1.1**

```bash
git tag v1.1
git push origin v1.1
```

- [ ] **Step 5: Actualizar ROADMAP.md**

En `docs/ROADMAP.md`, cambia el estado de v1.1:

```markdown
| v1.1 | La Chispa | ✅ En producción | `main` | `v1.1` |
```

```bash
git add docs/ROADMAP.md
git commit -m "docs: mark v1.1 La Chispa as deployed"
git push origin main
```
