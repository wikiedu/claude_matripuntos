# v3.2 PWA Shell Instalable — Implementation Plan

> Spec: `docs/superpowers/specs/2026-05-12-mobile-pwa-strategy-design.md` (Fase 2)
> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` o `superpowers:executing-plans`. Plan secuencial — un solo executor preferido para no fragmentar SW.

**Goal:** Convertir la webapp mobile-first (v3.1) en PWA instalable. Manifest + icons + splash + Service Worker Workbox + install prompt UX (Android nativo, iOS instrucciones). Adapter pattern en `src/frontend/src/platform/` deja arquitectura Capacitor-ready. Lighthouse PWA score > 90.

**Architecture:** Sustituir el SW custom actual (`public/push-sw.js`) por SW unificado generado con `vite-plugin-pwa` en modo `injectManifest`. Cache: app shell precache + NetworkFirst para `/api/*` (offline lectura, mutations fallan). Lógica de Web Push migrada al nuevo SW. UI adapters en `platform/` para que Fase 4 Capacitor swappee implementación sin tocar UI.

**Tech Stack:** vite-plugin-pwa, Workbox, Web App Manifest, `pwa-asset-generator` (build-time), Web Push (existente), Vibration API, Web Share API, View Transitions API (pre-trabajo para Fase 3).

**Tag final:** `v3.2` tras merge.

**Branch:** `feature/v3.2-pwa-shell` (creada de `main` post-v3.1).

**Pre-requisito:** Fase 1 completada (mobile-first responsive en producción).

---

## Bloque A · Setup vite-plugin-pwa + manifest (4h)

### Tarea A.1 · Instalar dependencias

- [ ] **Paso 1:** Branch.

```bash
git checkout main && git pull --ff-only origin main && git checkout -b feature/v3.2-pwa-shell
```

- [ ] **Paso 2:** Instalar dependencias en `src/frontend/`.

```bash
cd src/frontend
npm install -D vite-plugin-pwa workbox-window
npm install -D pwa-asset-generator
```

- [ ] **Paso 3:** Verificar versiones quedan en `package.json` (vite-plugin-pwa ≥ 0.20).

- [ ] **Paso 4:** Commit.

```bash
git add src/frontend/package.json src/frontend/package-lock.json
git commit -m "chore(deps): add vite-plugin-pwa + workbox-window + pwa-asset-generator

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea A.2 · Crear manifest.webmanifest

- [ ] **Paso 1:** Crear `src/frontend/public/manifest.webmanifest`.

```json
{
  "name": "Matripuntos - Equilibrio en pareja",
  "short_name": "Matripuntos",
  "description": "Gamifica la equidad en tu pareja con un sistema de puntos negociables",
  "start_url": "/?source=pwa",
  "scope": "/",
  "display": "standalone",
  "orientation": "portrait",
  "theme_color": "#0f172a",
  "background_color": "#0f172a",
  "lang": "es",
  "dir": "ltr",
  "categories": ["productivity", "lifestyle"],
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any" },
    { "src": "/icons/icon-192-maskable.png", "sizes": "192x192", "type": "image/png", "purpose": "maskable" },
    { "src": "/icons/icon-512-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

- [ ] **Paso 2:** Linkear en `src/frontend/index.html` justo debajo del viewport meta.

```html
<link rel="manifest" href="/manifest.webmanifest">
<meta name="theme-color" content="#0f172a">
<link rel="apple-touch-icon" sizes="180x180" href="/icons/apple-touch-icon-180.png">
<link rel="apple-touch-icon" sizes="167x167" href="/icons/apple-touch-icon-167.png">
<link rel="apple-touch-icon" sizes="152x152" href="/icons/apple-touch-icon-152.png">
```

- [ ] **Paso 3:** Verificar parse válido.

```bash
cat src/frontend/public/manifest.webmanifest | python3 -m json.tool > /dev/null && echo OK
```

Esperado: `OK`.

### Tarea A.3 · Generar set completo de icons + splash

- [ ] **Paso 1:** Localizar el logo fuente. Debe ser un SVG limpio ≥ 1024×1024 sin bordes/margin. Si no existe, crear desde el favicon actual o solicitar al usuario.

```bash
ls src/frontend/public/*.svg src/frontend/src/assets/*.svg 2>/dev/null | head
```

Si no hay logo SVG adecuado: **PAUSAR** y pedir al usuario que provea uno en `src/frontend/public/logo-source.svg`. Mientras tanto se puede usar el favicon actual como placeholder con un script ImageMagick.

- [ ] **Paso 2:** Crear script `scripts/generate-pwa-assets.sh`.

```bash
#!/bin/bash
set -euo pipefail

SRC="${1:-src/frontend/public/logo-source.svg}"
OUT="src/frontend/public/icons"
mkdir -p "$OUT"

# Iconos PWA estándar
for size in 192 512; do
  npx -y pwa-asset-generator "$SRC" "$OUT" \
    --icon-only --opaque false --padding "0%" \
    --background "#0f172a" --type png \
    --manifest src/frontend/public/manifest.webmanifest --index ""
done

# Apple touch icons
for size in 152 167 180; do
  magick "$SRC" -resize ${size}x${size} -background "#0f172a" -gravity center -extent ${size}x${size} \
    "$OUT/apple-touch-icon-${size}.png"
done

# Splash screens iOS (8 dispositivos principales)
npx -y pwa-asset-generator "$SRC" "$OUT" \
  --splash-only --background "#0f172a" --padding "calc(50vh - 25%) calc(50vw - 25%)" \
  --opaque true --type png

echo "Done. Generated assets in $OUT"
ls -la "$OUT" | head -30
```

- [ ] **Paso 3:** Hacer ejecutable y correr.

```bash
chmod +x scripts/generate-pwa-assets.sh
bash scripts/generate-pwa-assets.sh
```

- [ ] **Paso 4:** Verificar que se generaron todos.

```bash
ls src/frontend/public/icons/
```

Esperado: al menos `icon-192.png`, `icon-512.png`, variantes maskable, 3 apple-touch-icon-*.png, y archivos `apple-splash-*.png`.

- [ ] **Paso 5:** Linkear splashes en `index.html` (uno por dispositivo, con `media` queries específicos). Pegar el bloque que `pwa-asset-generator` imprime al ejecutar, dentro del `<head>`.

- [ ] **Paso 6:** Commit.

```bash
git add scripts/generate-pwa-assets.sh src/frontend/public/icons/ src/frontend/public/manifest.webmanifest src/frontend/index.html
git commit -m "feat(pwa): manifest + icons + splash screens iOS

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea A.4 · Configurar vite-plugin-pwa en vite.config.ts

- [ ] **Paso 1:** Leer `src/frontend/vite.config.ts` actual.

```bash
cat src/frontend/vite.config.ts
```

- [ ] **Paso 2:** Añadir el plugin con `injectManifest` (modo que permite código custom en el SW, necesario para preservar lógica push).

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      strategies: 'injectManifest',
      srcDir: 'src',
      filename: 'sw.ts',
      injectRegister: 'auto',
      manifest: false,                                  // Servimos manifest.webmanifest manual
      injectManifest: {
        globPatterns: ['**/*.{js,css,html,png,svg,webmanifest,woff2}'],
        maximumFileSizeToCacheInBytes: 5 * 1024 * 1024  // 5MB para precache
      },
      devOptions: { enabled: false }                    // SW solo en build, no en dev
    })
  ],
  build: { /* ... existente ... */ }
});
```

- [ ] **Paso 3:** Commit.

```bash
git add src/frontend/vite.config.ts
git commit -m "feat(pwa): configurar vite-plugin-pwa con injectManifest

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

---

## Bloque B · Service Worker unificado (1 día)

### Tarea B.1 · Leer el SW custom existente

- [ ] **Paso 1:** Leer toda la lógica actual.

```bash
cat src/frontend/public/push-sw.js
```

Apuntar los handlers de `push` y `notificationclick` — los reescribiremos en TypeScript en el nuevo SW.

### Tarea B.2 · Crear src/frontend/src/sw.ts (SW unificado)

- [ ] **Paso 1:** Crear `src/frontend/src/sw.ts`.

```ts
/// <reference lib="webworker" />
/// <reference types="vite-plugin-pwa/client" />
import { precacheAndRoute, cleanupOutdatedCaches } from 'workbox-precaching';
import { registerRoute, NavigationRoute } from 'workbox-routing';
import { NetworkFirst, StaleWhileRevalidate } from 'workbox-strategies';
import { ExpirationPlugin } from 'workbox-expiration';

declare const self: ServiceWorkerGlobalScope;

// Precache: app shell — vite-plugin-pwa inyecta el manifest aquí
precacheAndRoute(self.__WB_MANIFEST);
cleanupOutdatedCaches();

// Runtime: GET /api/* → NetworkFirst (3s timeout)
registerRoute(
  ({ url, request }) =>
    request.method === 'GET' &&
    url.pathname.startsWith('/api/'),
  new NetworkFirst({
    cacheName: 'api-cache',
    networkTimeoutSeconds: 3,
    plugins: [
      new ExpirationPlugin({
        maxEntries: 100,
        maxAgeSeconds: 60 * 60 * 24  // 1 día
      })
    ]
  })
);

// Runtime: imágenes/fonts CDN → StaleWhileRevalidate
registerRoute(
  ({ request }) => ['image', 'font'].includes(request.destination),
  new StaleWhileRevalidate({
    cacheName: 'assets-cache',
    plugins: [new ExpirationPlugin({ maxEntries: 60, maxAgeSeconds: 60 * 60 * 24 * 30 })]
  })
);

// Navigation: serve index.html para todas las navigations (SPA fallback)
registerRoute(new NavigationRoute(
  async () => {
    const cache = await caches.open('workbox-precache-v2');
    const response = await cache.match('/index.html');
    return response ?? Response.error();
  }
));

// ============== Push Notifications (portado del SW custom) ==============

interface PushPayload {
  title: string;
  body: string;
  icon?: string;
  badge?: string;
  tag?: string;
  data?: Record<string, unknown>;
  url?: string;
}

self.addEventListener('push', (event) => {
  let payload: PushPayload;
  try {
    payload = event.data?.json() ?? { title: 'Matripuntos', body: 'Nueva notificación' };
  } catch {
    payload = { title: 'Matripuntos', body: event.data?.text() ?? 'Nueva notificación' };
  }

  const options: NotificationOptions = {
    body: payload.body,
    icon: payload.icon ?? '/icons/icon-192.png',
    badge: payload.badge ?? '/icons/icon-192.png',
    tag: payload.tag,
    data: { url: payload.url ?? '/', ...(payload.data ?? {}) }
  };

  event.waitUntil(self.registration.showNotification(payload.title, options));
});

self.addEventListener('notificationclick', (event) => {
  event.notification.close();
  const url = (event.notification.data?.url as string) ?? '/';

  event.waitUntil((async () => {
    const allClients = await self.clients.matchAll({ type: 'window', includeUncontrolled: true });
    const focused = allClients.find(c => c.url.includes(self.location.origin));
    if (focused) {
      await focused.focus();
      focused.postMessage({ type: 'NAVIGATE', url });
      return;
    }
    await self.clients.openWindow(url);
  })());
});

// ============== Skip waiting + claim ==============

self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', (event) => event.waitUntil(self.clients.claim()));

// Cross-tab message para "update ready"
self.addEventListener('message', (event) => {
  if (event.data?.type === 'SKIP_WAITING') self.skipWaiting();
});
```

- [ ] **Paso 2:** Verificar TS no se queja.

```bash
cd src/frontend && npx tsc --noEmit
```

- [ ] **Paso 3:** Commit.

```bash
git add src/frontend/src/sw.ts
git commit -m "feat(pwa): SW unificado en TS con Workbox + push handlers

Reemplaza public/push-sw.js. Push payload format compatible.

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea B.3 · Registrar SW en main.tsx

- [ ] **Paso 1:** Leer `src/frontend/src/main.tsx`.

```bash
cat src/frontend/src/main.tsx
```

- [ ] **Paso 2:** Añadir registro automático y manejador de update.

```ts
// src/frontend/src/main.tsx — añadir tras el render
import { registerSW } from 'virtual:pwa-register';

const updateSW = registerSW({
  onNeedRefresh() {
    window.dispatchEvent(new CustomEvent('pwa:update-ready'));
  },
  onOfflineReady() {
    window.dispatchEvent(new CustomEvent('pwa:offline-ready'));
  }
});

// Expose para que un componente UI pueda llamar updateSW() al hacer click en "Recargar"
(window as any).__pwaUpdateSW = updateSW;
```

- [ ] **Paso 3:** Verificar el build genera SW.

```bash
cd src/frontend && npm run build && ls dist/ | grep -E "sw\.js|manifest|workbox" | head
```

Esperado: `sw.js`, `manifest.webmanifest`, posibles archivos `workbox-*.js`.

- [ ] **Paso 4:** Commit.

```bash
git add src/frontend/src/main.tsx
git commit -m "feat(pwa): registrar SW vía vite-plugin-pwa con update hooks

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea B.4 · Eliminar SW antiguo y migrar registro

- [ ] **Paso 1:** Buscar dónde se registra `push-sw.js` actualmente.

```bash
grep -rn "push-sw" src/frontend/src/ 2>/dev/null
```

- [ ] **Paso 2:** En `useWebPush.ts` (líneas 43, 81 según el `grep` inicial del audit), sustituir registro manual del SW por uso del SW unificado.

```ts
// src/frontend/src/hooks/useWebPush.ts — patch
// ANTES: const reg = await navigator.serviceWorker.register('/push-sw.js');
// DESPUÉS: const reg = await navigator.serviceWorker.ready;
```

- [ ] **Paso 3:** Verificar que `getRegistration()` ahora devuelve el SW unificado en testing.

- [ ] **Paso 4:** Marcar `public/push-sw.js` como deprecated añadiendo cabecera de warning:

```js
// public/push-sw.js
// DEPRECATED v3.2 — Reemplazado por src/sw.ts vía vite-plugin-pwa.
// Se mantiene este archivo SOLO para que navegadores con cache del SW antiguo
// auto-actualicen al nuevo. Borrar tras 30 días en producción (v3.2.1).
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', async () => {
  await self.registration.unregister();
  const clients = await self.clients.matchAll();
  clients.forEach(c => c.navigate(c.url));
});
```

- [ ] **Paso 5:** Commit.

```bash
git add src/frontend/public/push-sw.js src/frontend/src/hooks/useWebPush.ts
git commit -m "refactor(pwa): migrar registro SW al unificado, deprecar push-sw.js

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

---

## Bloque C · Adapter pattern `src/platform/*` (1 día)

Este bloque deja la arquitectura Capacitor-ready. Cada adapter exporta una interfaz estable; la implementación detecta runtime (`window.Capacitor` futuro).

### Tarea C.1 · Estructura base

- [ ] **Paso 1:** Crear directorio.

```bash
mkdir -p src/frontend/src/platform
```

- [ ] **Paso 2:** Crear `src/frontend/src/platform/index.ts`.

```ts
// src/frontend/src/platform/index.ts
export * as install from './install';
export * as notifications from './notifications';
export * as haptics from './haptics';
export * as share from './share';
export * as storage from './storage';
export * as transitions from './transitions';

export const isNative = (): boolean =>
  typeof window !== 'undefined' && Boolean((window as any).Capacitor);
```

- [ ] **Paso 3:** Crear `src/frontend/src/platform/install.ts`.

```ts
// src/frontend/src/platform/install.ts
let deferredPrompt: BeforeInstallPromptEvent | null = null;

interface BeforeInstallPromptEvent extends Event {
  prompt(): Promise<void>;
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
}

if (typeof window !== 'undefined') {
  window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault();
    deferredPrompt = e as BeforeInstallPromptEvent;
    window.dispatchEvent(new CustomEvent('pwa:installable'));
  });

  window.addEventListener('appinstalled', () => {
    deferredPrompt = null;
    window.dispatchEvent(new CustomEvent('pwa:installed'));
  });
}

export const canPromptInstall = (): boolean => deferredPrompt !== null;

export const promptInstall = async (): Promise<'accepted' | 'dismissed' | 'unavailable'> => {
  if (!deferredPrompt) return 'unavailable';
  await deferredPrompt.prompt();
  const { outcome } = await deferredPrompt.userChoice;
  deferredPrompt = null;
  return outcome;
};

export const isIOSDevice = (): boolean => {
  if (typeof navigator === 'undefined') return false;
  return /iPad|iPhone|iPod/.test(navigator.userAgent) && !(window as any).MSStream;
};

export const isInStandaloneMode = (): boolean => {
  if (typeof window === 'undefined') return false;
  return (
    window.matchMedia('(display-mode: standalone)').matches ||
    (window.navigator as any).standalone === true
  );
};
```

- [ ] **Paso 4:** Crear `src/frontend/src/platform/notifications.ts` (migra de `useWebPush`).

```ts
// src/frontend/src/platform/notifications.ts
import { apiClient } from '../services/apiClient';

const VAPID_PUBLIC_KEY = import.meta.env.VITE_VAPID_PUBLIC_KEY || '';

function urlBase64ToUint8Array(base64String: string): Uint8Array {
  const padding = '='.repeat((4 - base64String.length % 4) % 4);
  const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
  const raw = atob(base64);
  return new Uint8Array([...raw].map(c => c.charCodeAt(0)));
}

export const isSupported = (): boolean => {
  if (typeof window === 'undefined') return false;
  return 'serviceWorker' in navigator && 'PushManager' in window && 'Notification' in window;
};

export const getPermission = (): NotificationPermission =>
  isSupported() ? Notification.permission : 'denied';

export const requestPermission = async (): Promise<NotificationPermission> => {
  if (!isSupported()) return 'denied';
  return Notification.requestPermission();
};

export const subscribe = async (): Promise<PushSubscription | null> => {
  if (!isSupported()) return null;
  const reg = await navigator.serviceWorker.ready;
  let sub = await reg.pushManager.getSubscription();
  if (!sub) {
    sub = await reg.pushManager.subscribe({
      userVisibleOnly: true,
      applicationServerKey: urlBase64ToUint8Array(VAPID_PUBLIC_KEY)
    });
  }
  await apiClient.post('/api/notifications/subscribe', sub.toJSON());
  return sub;
};

export const unsubscribe = async (): Promise<boolean> => {
  if (!isSupported()) return false;
  const reg = await navigator.serviceWorker.ready;
  const sub = await reg.pushManager.getSubscription();
  if (!sub) return true;
  await apiClient.post('/api/notifications/unsubscribe', { endpoint: sub.endpoint });
  return sub.unsubscribe();
};

export const getSubscription = async (): Promise<PushSubscription | null> => {
  if (!isSupported()) return null;
  const reg = await navigator.serviceWorker.ready;
  return reg.pushManager.getSubscription();
};
```

- [ ] **Paso 5:** Crear `src/frontend/src/platform/haptics.ts`.

```ts
// src/frontend/src/platform/haptics.ts
type HapticPattern = 'light' | 'medium' | 'heavy' | 'success' | 'warning' | 'error';

const PATTERNS: Record<HapticPattern, number | number[]> = {
  light: 10,
  medium: 20,
  heavy: 30,
  success: [10, 30, 10],
  warning: [20, 40, 20],
  error: [30, 60, 30]
};

export const isSupported = (): boolean =>
  typeof navigator !== 'undefined' && 'vibrate' in navigator;

export const trigger = (pattern: HapticPattern): void => {
  if (!isSupported()) return;
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
  navigator.vibrate(PATTERNS[pattern]);
};
```

- [ ] **Paso 6:** Crear `src/frontend/src/platform/share.ts`.

```ts
// src/frontend/src/platform/share.ts
export interface ShareData {
  title?: string;
  text?: string;
  url?: string;
}

export const isSupported = (): boolean =>
  typeof navigator !== 'undefined' && 'share' in navigator;

export const share = async (data: ShareData): Promise<'shared' | 'cancelled' | 'unsupported'> => {
  if (!isSupported()) return 'unsupported';
  try {
    await navigator.share(data);
    return 'shared';
  } catch (err) {
    if ((err as Error).name === 'AbortError') return 'cancelled';
    throw err;
  }
};
```

- [ ] **Paso 7:** Crear `src/frontend/src/platform/storage.ts` (wrapper para localStorage que mañana puede ir a Capacitor Preferences).

```ts
// src/frontend/src/platform/storage.ts
export const get = async (key: string): Promise<string | null> => {
  if (typeof window === 'undefined') return null;
  return window.localStorage.getItem(key);
};

export const set = async (key: string, value: string): Promise<void> => {
  if (typeof window === 'undefined') return;
  window.localStorage.setItem(key, value);
};

export const remove = async (key: string): Promise<void> => {
  if (typeof window === 'undefined') return;
  window.localStorage.removeItem(key);
};
```

- [ ] **Paso 8:** Crear `src/frontend/src/platform/transitions.ts` (View Transitions API).

```ts
// src/frontend/src/platform/transitions.ts
export const isSupported = (): boolean =>
  typeof document !== 'undefined' && 'startViewTransition' in document;

export const start = (cb: () => void | Promise<void>): void => {
  if (!isSupported()) {
    void cb();
    return;
  }
  (document as any).startViewTransition(cb);
};
```

- [ ] **Paso 9:** Commit.

```bash
git add src/frontend/src/platform/
git commit -m "feat(platform): adapter pattern para APIs browser-specific

install / notifications / haptics / share / storage / transitions
Cada adapter exporta interfaz estable. Fase 4 Capacitor sustituye implementación sin tocar UI.

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea C.2 · Refactor useWebPush a usar platform/notifications

- [ ] **Paso 1:** Leer `useWebPush.ts` actual.

```bash
cat src/frontend/src/hooks/useWebPush.ts
```

- [ ] **Paso 2:** Sustituir todo el cuerpo por wrapper sobre `platform.notifications`.

```ts
// src/frontend/src/hooks/useWebPush.ts
import { useEffect, useState } from 'react';
import * as notifications from '../platform/notifications';

interface UseWebPushReturn {
  isSupported: boolean;
  permission: NotificationPermission;
  isSubscribed: boolean;
  subscribe: () => Promise<void>;
  unsubscribe: () => Promise<void>;
}

export function useWebPush(): UseWebPushReturn {
  const [permission, setPermission] = useState<NotificationPermission>('default');
  const [isSubscribed, setIsSubscribed] = useState(false);

  useEffect(() => {
    if (!notifications.isSupported()) return;
    setPermission(notifications.getPermission());
    notifications.getSubscription().then(sub => setIsSubscribed(Boolean(sub)));
  }, []);

  return {
    isSupported: notifications.isSupported(),
    permission,
    isSubscribed,
    subscribe: async () => {
      const perm = await notifications.requestPermission();
      setPermission(perm);
      if (perm === 'granted') {
        const sub = await notifications.subscribe();
        setIsSubscribed(Boolean(sub));
      }
    },
    unsubscribe: async () => {
      const ok = await notifications.unsubscribe();
      if (ok) setIsSubscribed(false);
    }
  };
}
```

- [ ] **Paso 3:** Verificar TS y tests.

```bash
cd src/frontend && npx tsc --noEmit && npm run test
```

- [ ] **Paso 4:** Commit.

```bash
git add src/frontend/src/hooks/useWebPush.ts
git commit -m "refactor(hooks): useWebPush delega a platform/notifications

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

---

## Bloque D · Hooks PWA + Componentes UI (4-6h)

### Tarea D.1 · Hook `usePWAInstall`

- [ ] **Paso 1:** Crear `src/frontend/src/hooks/usePWAInstall.ts`.

```ts
// src/frontend/src/hooks/usePWAInstall.ts
import { useEffect, useState } from 'react';
import * as install from '../platform/install';
import * as storage from '../platform/storage';

const SESSION_COUNT_KEY = 'matripuntos:session-count';
const INSTALL_DISMISSED_KEY = 'matripuntos:install-dismissed-at';
const DISMISSED_TTL_MS = 7 * 24 * 60 * 60 * 1000;  // 7 días
const SESSIONS_BEFORE_PROMPT = 2;

interface UsePWAInstallReturn {
  canInstall: boolean;
  isInstalled: boolean;
  isIOS: boolean;
  shouldShowBanner: boolean;
  promptInstall: () => Promise<'accepted' | 'dismissed' | 'unavailable'>;
  dismissBanner: () => Promise<void>;
}

export function usePWAInstall(): UsePWAInstallReturn {
  const [canInstall, setCanInstall] = useState(install.canPromptInstall());
  const [isInstalled, setIsInstalled] = useState(install.isInStandaloneMode());
  const [shouldShowBanner, setShouldShowBanner] = useState(false);

  useEffect(() => {
    const onInstallable = () => setCanInstall(true);
    const onInstalled = () => { setIsInstalled(true); setCanInstall(false); };
    window.addEventListener('pwa:installable', onInstallable);
    window.addEventListener('pwa:installed', onInstalled);
    return () => {
      window.removeEventListener('pwa:installable', onInstallable);
      window.removeEventListener('pwa:installed', onInstalled);
    };
  }, []);

  useEffect(() => {
    (async () => {
      if (isInstalled) return setShouldShowBanner(false);

      // Increment session count
      const countStr = await storage.get(SESSION_COUNT_KEY);
      const newCount = (parseInt(countStr ?? '0') || 0) + 1;
      await storage.set(SESSION_COUNT_KEY, String(newCount));

      // Check dismissal
      const dismissedStr = await storage.get(INSTALL_DISMISSED_KEY);
      const dismissedAt = dismissedStr ? parseInt(dismissedStr) : 0;
      const dismissedRecent = dismissedAt && Date.now() - dismissedAt < DISMISSED_TTL_MS;

      // Show only if: not installed + (canInstall OR iOS) + ≥ 2 sessions + not recently dismissed
      const eligible = newCount >= SESSIONS_BEFORE_PROMPT && !dismissedRecent;
      const showable = canInstall || install.isIOSDevice();
      setShouldShowBanner(eligible && showable);
    })();
  }, [canInstall, isInstalled]);

  return {
    canInstall,
    isInstalled,
    isIOS: install.isIOSDevice(),
    shouldShowBanner,
    promptInstall: install.promptInstall,
    dismissBanner: async () => {
      await storage.set(INSTALL_DISMISSED_KEY, String(Date.now()));
      setShouldShowBanner(false);
    }
  };
}
```

- [ ] **Paso 2:** Crear `src/frontend/src/hooks/useOnlineStatus.ts`.

```ts
// src/frontend/src/hooks/useOnlineStatus.ts
import { useEffect, useState } from 'react';

export function useOnlineStatus(): boolean {
  const [online, setOnline] = useState(typeof navigator === 'undefined' ? true : navigator.onLine);

  useEffect(() => {
    const on = () => setOnline(true);
    const off = () => setOnline(false);
    window.addEventListener('online', on);
    window.addEventListener('offline', off);
    return () => {
      window.removeEventListener('online', on);
      window.removeEventListener('offline', off);
    };
  }, []);

  return online;
}
```

- [ ] **Paso 3:** Commit.

```bash
git add src/frontend/src/hooks/usePWAInstall.ts src/frontend/src/hooks/useOnlineStatus.ts
git commit -m "feat(hooks): usePWAInstall (engagement-gated) + useOnlineStatus

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea D.2 · Componente `InstallBanner` (Android Chrome)

- [ ] **Paso 1:** Crear `src/frontend/src/components/pwa/InstallBanner.tsx`.

```tsx
// src/frontend/src/components/pwa/InstallBanner.tsx
import { X, Download } from 'lucide-react';
import { usePWAInstall } from '../../hooks/usePWAInstall';

export function InstallBanner() {
  const { shouldShowBanner, canInstall, isIOS, promptInstall, dismissBanner } = usePWAInstall();

  // Solo render si banner debe mostrarse Y es Android (Chrome con beforeinstallprompt).
  // iOS lleva su propio componente con instrucciones.
  if (!shouldShowBanner || isIOS || !canInstall) return null;

  return (
    <div
      role="dialog"
      aria-label="Instalar Matripuntos"
      className="fixed bottom-0 left-0 right-0 z-50 pb-safe-bottom bg-surface-card border-t border-brd-subtle p-4 md:hidden"
    >
      <div className="flex items-center gap-3 max-w-md mx-auto">
        <div className="w-12 h-12 rounded-xl bg-brand-purple/20 flex items-center justify-center flex-shrink-0">
          <Download className="w-6 h-6 text-brand-purple" />
        </div>
        <div className="flex-1 min-w-0">
          <p className="text-sm font-semibold text-text-primary">Instalar Matripuntos</p>
          <p className="text-xs text-text-tertiary">Como app, sin abrir el navegador</p>
        </div>
        <button
          type="button"
          onClick={() => promptInstall()}
          className="min-h-touch px-4 py-2 rounded-lg bg-brand-purple text-white text-sm font-semibold"
        >
          Instalar
        </button>
        <button
          type="button"
          aria-label="Cerrar"
          onClick={() => dismissBanner()}
          className="min-h-touch min-w-touch flex items-center justify-center text-text-tertiary"
        >
          <X className="w-5 h-5" />
        </button>
      </div>
    </div>
  );
}
```

### Tarea D.3 · Componente `InstallIOSInstructions`

- [ ] **Paso 1:** Crear `src/frontend/src/components/pwa/InstallIOSInstructions.tsx`.

```tsx
// src/frontend/src/components/pwa/InstallIOSInstructions.tsx
import { X, Share } from 'lucide-react';
import { usePWAInstall } from '../../hooks/usePWAInstall';

export function InstallIOSInstructions() {
  const { shouldShowBanner, isIOS, isInstalled, dismissBanner } = usePWAInstall();

  if (!shouldShowBanner || !isIOS || isInstalled) return null;

  return (
    <div
      role="dialog"
      aria-label="Instalar Matripuntos en iOS"
      className="fixed bottom-0 left-0 right-0 z-50 pb-safe-bottom bg-surface-card border-t border-brd-subtle p-4 md:hidden"
    >
      <div className="max-w-md mx-auto">
        <div className="flex items-start gap-3">
          <div className="flex-1">
            <p className="text-sm font-semibold text-text-primary mb-2">Añade Matripuntos a inicio</p>
            <ol className="text-xs text-text-secondary space-y-1.5">
              <li>1. Toca <Share className="inline w-3.5 h-3.5 mx-1" /> abajo en Safari</li>
              <li>2. Desliza y toca "Añadir a pantalla de inicio"</li>
              <li>3. Toca "Añadir" arriba a la derecha</li>
            </ol>
          </div>
          <button
            type="button"
            aria-label="Cerrar"
            onClick={() => dismissBanner()}
            className="min-h-touch min-w-touch flex items-center justify-center text-text-tertiary -mt-1 -mr-1"
          >
            <X className="w-5 h-5" />
          </button>
        </div>
      </div>
    </div>
  );
}
```

### Tarea D.4 · Componente `UpdateAvailableToast`

- [ ] **Paso 1:** Crear `src/frontend/src/components/pwa/UpdateAvailableToast.tsx`.

```tsx
// src/frontend/src/components/pwa/UpdateAvailableToast.tsx
import { useEffect, useState } from 'react';
import { RefreshCw } from 'lucide-react';

export function UpdateAvailableToast() {
  const [show, setShow] = useState(false);

  useEffect(() => {
    const onUpdate = () => setShow(true);
    window.addEventListener('pwa:update-ready', onUpdate);
    return () => window.removeEventListener('pwa:update-ready', onUpdate);
  }, []);

  if (!show) return null;

  return (
    <div
      role="status"
      className="fixed top-0 left-0 right-0 z-50 pt-safe-top bg-brand-purple text-white px-4 py-3 flex items-center justify-between"
    >
      <span className="text-sm font-semibold">Nueva versión disponible</span>
      <button
        type="button"
        onClick={() => (window as any).__pwaUpdateSW?.(true)}
        className="min-h-touch px-3 py-1.5 rounded-md bg-white/20 text-sm font-semibold flex items-center gap-1.5"
      >
        <RefreshCw className="w-4 h-4" /> Recargar
      </button>
    </div>
  );
}
```

### Tarea D.5 · Componente `OfflineBanner`

- [ ] **Paso 1:** Crear `src/frontend/src/components/pwa/OfflineBanner.tsx`.

```tsx
// src/frontend/src/components/pwa/OfflineBanner.tsx
import { WifiOff } from 'lucide-react';
import { useOnlineStatus } from '../../hooks/useOnlineStatus';

export function OfflineBanner() {
  const online = useOnlineStatus();
  if (online) return null;

  return (
    <div
      role="status"
      aria-live="polite"
      className="fixed top-0 left-0 right-0 z-40 pt-safe-top bg-warning text-warning-foreground px-4 py-2"
    >
      <p className="text-center text-sm font-medium flex items-center justify-center gap-2">
        <WifiOff className="w-4 h-4" /> Sin conexión — mostrando datos guardados
      </p>
    </div>
  );
}
```

### Tarea D.6 · Montar componentes en App.tsx

- [ ] **Paso 1:** Leer `src/frontend/src/App.tsx`.

- [ ] **Paso 2:** Añadir los 4 componentes PWA al árbol root (típicamente debajo del Router):

```tsx
// src/frontend/src/App.tsx — extracto
import { InstallBanner } from './components/pwa/InstallBanner';
import { InstallIOSInstructions } from './components/pwa/InstallIOSInstructions';
import { UpdateAvailableToast } from './components/pwa/UpdateAvailableToast';
import { OfflineBanner } from './components/pwa/OfflineBanner';

// En el JSX root:
<>
  <UpdateAvailableToast />
  <OfflineBanner />
  {/* ... resto de la app ... */}
  <InstallBanner />
  <InstallIOSInstructions />
</>
```

- [ ] **Paso 3:** Commit.

```bash
git add src/frontend/src/components/pwa/ src/frontend/src/App.tsx
git commit -m "feat(pwa): UI install banner + iOS instructions + update + offline

Montados en root. Banner respeta engagement gate (2 sesiones + 7d dismissal).

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea D.7 · Settings: botón "Instalar app" explícito

- [ ] **Paso 1:** En la pantalla de Settings (creada en v3.1), añadir item "Instalar app" si `!isInstalled`. Llama a `promptInstall()` o muestra las instrucciones iOS según device.

```tsx
// Pseudocódigo — adapta al patrón Settings actual
const { isInstalled, isIOS, canInstall, promptInstall } = usePWAInstall();

{!isInstalled && (
  <SettingsItem
    icon={Download}
    label="Instalar Matripuntos como app"
    onClick={() => {
      if (canInstall) return promptInstall();
      if (isIOS) {
        // open a sheet with InstallIOSInstructions content
        setIOSInstructionsSheet(true);
      }
    }}
  />
)}
```

- [ ] **Paso 2:** Commit.

```bash
git add src/frontend/src/pages/Settings.tsx src/frontend/src/components/v2/settings/
git commit -m "feat(pwa): item 'Instalar app' en Settings con flow per-platform

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

---

## Bloque E · Tests (1 día)

### Tarea E.1 · Tests unit de adapters

- [ ] **Paso 1:** Crear `src/frontend/src/platform/__tests__/install.test.ts` con mocks de `window.matchMedia` y `BeforeInstallPromptEvent`.

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as install from '../install';

describe('platform/install', () => {
  beforeEach(() => vi.clearAllMocks());

  it('detects iOS via UA', () => {
    Object.defineProperty(navigator, 'userAgent', {
      value: 'Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)',
      configurable: true
    });
    expect(install.isIOSDevice()).toBe(true);
  });

  it('returns standalone when display-mode matches', () => {
    vi.spyOn(window, 'matchMedia').mockReturnValue({ matches: true } as MediaQueryList);
    expect(install.isInStandaloneMode()).toBe(true);
  });

  it('canPromptInstall is false until beforeinstallprompt fires', () => {
    expect(install.canPromptInstall()).toBe(false);
  });
});
```

- [ ] **Paso 2:** Equivalentes para `notifications.test.ts`, `haptics.test.ts`, `share.test.ts`, `storage.test.ts`.

- [ ] **Paso 3:** Run.

```bash
cd src/frontend && npm run test -- src/platform/__tests__
```

- [ ] **Paso 4:** Commit.

```bash
git add src/frontend/src/platform/__tests__/
git commit -m "test(platform): unit tests para adapters

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea E.2 · Lighthouse CI gate

- [ ] **Paso 1:** Crear `.github/workflows/lighthouse.yml`.

```yaml
name: Lighthouse PWA
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: cd src/frontend && npm ci && npm run build
      - name: Run Lighthouse
        run: |
          npx -y @lhci/cli@latest autorun \
            --collect.staticDistDir=src/frontend/dist \
            --collect.numberOfRuns=1 \
            --assert.preset=lighthouse:recommended \
            --assert.assertions.installable-manifest=error \
            --assert.assertions.service-worker=error \
            --assert.assertions.maskable-icon=error \
            --upload.target=temporary-public-storage
```

- [ ] **Paso 2:** Crear `lighthouserc.json` en raíz.

```json
{
  "ci": {
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.80 }],
        "categories:pwa": ["error", { "minScore": 0.90 }],
        "categories:accessibility": ["error", { "minScore": 0.90 }],
        "categories:best-practices": ["warn", { "minScore": 0.90 }]
      }
    }
  }
}
```

- [ ] **Paso 3:** Commit.

```bash
git add .github/workflows/lighthouse.yml lighthouserc.json
git commit -m "ci(lighthouse): gate Lighthouse PWA ≥ 90 en PRs

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea E.3 · Test Playwright install flow

- [ ] **Paso 1:** Crear `e2e/pwa-install.spec.ts`.

```ts
import { test, expect } from '@playwright/test';

test.describe('PWA install', () => {
  test('manifest is valid JSON and links correctly', async ({ page, request }) => {
    await page.goto('/');
    const manifestHref = await page.locator('link[rel="manifest"]').getAttribute('href');
    expect(manifestHref).toBeTruthy();
    const res = await request.get(manifestHref!);
    expect(res.ok()).toBe(true);
    const json = await res.json();
    expect(json.name).toContain('Matripuntos');
    expect(json.display).toBe('standalone');
    expect(json.icons.length).toBeGreaterThanOrEqual(2);
  });

  test('service worker registers', async ({ page }) => {
    await page.goto('/');
    const swRegistered = await page.evaluate(async () => {
      const reg = await navigator.serviceWorker.ready;
      return Boolean(reg.active);
    });
    expect(swRegistered).toBe(true);
  });

  test('install banner appears after 2 sessions on Android UA', async ({ browser }) => {
    const ctx = await browser.newContext({
      userAgent: 'Mozilla/5.0 (Linux; Android 13; Pixel 7) AppleWebKit/537.36 Chrome/120.0.0.0 Mobile Safari/537.36'
    });
    const page = await ctx.newPage();
    // simular 2 sesiones
    await page.goto('/');
    await page.evaluate(() => localStorage.setItem('matripuntos:session-count', '2'));
    await page.reload();
    // disparar beforeinstallprompt artificialmente (en headless real Chrome puede no llegar)
    await page.evaluate(() => {
      const ev = new Event('beforeinstallprompt') as any;
      ev.prompt = () => Promise.resolve();
      ev.userChoice = Promise.resolve({ outcome: 'accepted' });
      window.dispatchEvent(ev);
    });
    await expect(page.getByRole('dialog', { name: /instalar/i })).toBeVisible({ timeout: 3000 });
  });
});
```

- [ ] **Paso 2:** Run.

```bash
npx playwright test e2e/pwa-install.spec.ts
```

- [ ] **Paso 3:** Commit.

```bash
git add e2e/pwa-install.spec.ts
git commit -m "test(e2e): PWA install flow + manifest + SW registration

Co-Authored-By: claude-flow <ruv@ruv.net>"
```

### Tarea E.4 · QA two-account físico

Verificación manual obligatoria antes de PR a main.

- [ ] **Paso 1:** En un iPhone físico (Safari iOS 16.4+):
  - Abrir https://<deploy-preview>.pages.dev
  - Hacer 2 sesiones (cerrar Safari y reabrir)
  - Verificar que aparece banner "Añade Matripuntos a inicio"
  - Seguir instrucciones, añadir a home
  - Abrir desde icono home → splash visible → app sin barra navegador
  - Login → push notification recibida desde push test endpoint
- [ ] **Paso 2:** En un Android físico (Chrome):
  - Mismo flow
  - Banner debe aparecer con botón "Instalar"
  - Tras instalar, icono en home, app abre standalone
  - Push notification funciona
- [ ] **Paso 3:** Test de update: hacer un cambio mínimo en `main`, deploy, verificar que `UpdateAvailableToast` aparece a los pocos segundos al reabrir.
- [ ] **Paso 4:** Test offline: en DevTools mobile, toggle "Offline", recargar la app. App shell debe cargar, datos GET cacheados deben verse, intento de mutación debe mostrar mensaje de error claro.
- [ ] **Paso 5:** Documentar en `docs/internal/v3.2-qa-findings.md`.

---

## Bloque F · Merge + tag (15 min)

### Tarea F.1 · PR

- [ ] **Paso 1:** Push y crear PR.

```bash
git push -u origin feature/v3.2-pwa-shell
gh pr create --base main --title "v3.2 · PWA shell instalable" --body "$(cat <<'EOF'
## Summary

- Manifest + icons + splash screens iOS.
- Service Worker unificado (Workbox) sustituye push-sw.js. Push handlers portados.
- Adapter pattern src/frontend/src/platform/* deja arquitectura Capacitor-ready.
- Install prompt UX: Android Chrome (beforeinstallprompt) + iOS instructions.
- UpdateAvailableToast + OfflineBanner.
- Lighthouse PWA score ≥ 90 (gate CI nuevo).
- usePWAInstall con engagement gate (2 sesiones + 7d dismissal).

Spec: docs/superpowers/specs/2026-05-12-mobile-pwa-strategy-design.md (Fase 2)
Plan: docs/superpowers/plans/2026-05-12-v3.2-pwa-shell.md

## Test plan

- [x] Lighthouse PWA ≥ 90 sobre preview CF Pages.
- [x] Playwright: manifest válido, SW registrado, install flow.
- [x] QA two-account físico iPhone + Android: install + push + update + offline.
- [x] Push subscriptions existentes siguen válidas (VAPID key sin cambios).
- [x] CI verde.

🤖 Generated with [claude-flow](https://github.com/ruvnet/claude-flow)
EOF
)"
```

### Tarea F.2 · Merge + tag

- [ ] **Paso 1:**

```bash
gh pr merge --merge --delete-branch
git checkout main && git pull --ff-only origin main
git tag -a v3.2 -m "v3.2 · PWA shell instalable"
git push origin v3.2
```

- [ ] **Paso 2:** Actualizar `docs/STATUS.md`.

---

## Criterios de aceptación (gate de cierre Fase 2)

- [ ] Lighthouse PWA ≥ 90 en deploy preview y prod.
- [ ] `manifest.webmanifest` válido, todos los icons existen.
- [ ] SW registrado en `navigator.serviceWorker.ready`.
- [ ] App instalable en Android Chrome → icono en home funciona.
- [ ] iOS Safari muestra instrucciones, instalación manual funciona, splash visible al abrir.
- [ ] Push notifications siguen llegando tras la migración del SW (verificado con 2 cuentas).
- [ ] Offline mode: app shell carga, GETs cacheados visibles, mutations muestran error claro.
- [ ] Update toast aparece tras deploy nuevo, recarga limpia.
- [ ] Tag `v3.2` aplicado, `docs/STATUS.md` actualizado.

---

## Rollback

**SW nuevo rompe push:**
- Deploy hotfix con SW que se auto-unregistra (ver código en push-sw.js deprecated comment).
- Los usuarios re-suscriben en próxima visita.

**Workbox cachea HTML viejo persistentemente:**
- `caches.delete('workbox-precache-v2')` en consola devtools (instrucción al usuario).
- O hotfix: subir versión que en `activate` haga `caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))))`.

**Manifest válido pero install prompt no aparece:**
- Verificar HTTPS, manifest linkeado, icons disponibles, SW activo. Lighthouse PWA reporta exactamente qué falta.

---

## Siguiente: Fase 0b (deep infra migration)

Plan: `docs/superpowers/plans/2026-05-12-v3.0.1-deep-infra-migration.md`.
