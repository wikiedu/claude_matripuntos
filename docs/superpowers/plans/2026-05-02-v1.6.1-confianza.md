# v1.6.1 · Confianza — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir Matripuntos en app pública seria con privacy legal (GDPR intermedio), telemetría real (PostHog opt-out anónimo), onboarding completo del invitado, QA automatizado E2E (Playwright), contract testing back↔front (workspace `packages/shared`), rate-limiting granular y account deletion / leave couple con anonimización del histórico.

**Architecture:** Monorepo gana workspace `packages/shared` con schemas zod compartidos + tipos derivados + catálogo de eventos de telemetría. Backend añade modelos `User.deletedAt/firstLoginAt`, `Couple.dissolvedAt`, servicios `accountDeletionService` / `coupleLifecycleService`, cron `dataRetentionJob`, wrappers `telemetry`, middleware `rateLimiter` con 5 buckets nombrados. Frontend añade páginas legales estáticas, `CookieConsentBanner` no bloqueante, wizards `DeleteAccountWizard` (3 pasos) y `LeaveCoupleWizard` (2 pasos), `ProfileCompletionBanner` (7 días), 2 pasos extra de onboarding del invitado con `AvatarPicker` (heredado de v1.6). E2E con Playwright corre 22 specs en chromium + webkit contra backend con SQLite efímero en CI.

**Tech Stack:** TypeScript · React 18 · Vite · Prisma · Express · Zod · React Query · `posthog-js` + `posthog-node` · `react-markdown` · `express-rate-limit` · `node-cron` · Playwright + `@playwright/test` · Vitest + RTL · Jest + supertest. Workspace: npm workspaces.

**Pre-flight requirement:** v1.6 (La Personalidad) debe estar en producción con tag `v1.6` y migración `20260427000000_v1_6_mood_log_and_mood_keys` aplicada limpiamente.

**Spec source:** `docs/superpowers/specs/2026-05-01-v1.6.1-confianza-design.md`

---

## File Structure

**Workspace nuevo:**
- `packages/shared/package.json` — `@matripuntos/shared`.
- `packages/shared/tsconfig.json`.
- `packages/shared/src/schemas/{auth,profile,events,tasks,moodHistory,account,couple,common}.ts`.
- `packages/shared/src/types/index.ts`.
- `packages/shared/src/telemetry-events.ts`.
- `packages/shared/src/index.ts`.

**Backend nuevos:**
- `src/backend/src/services/accountDeletionService.ts` + test.
- `src/backend/src/services/coupleLifecycleService.ts` + test.
- `src/backend/src/services/telemetry.ts` + test.
- `src/backend/src/jobs/dataRetentionJob.ts` + test.
- `src/backend/src/jobs/scheduler.ts`.
- `src/backend/src/middleware/rateLimiter.ts` + test.
- `src/backend/src/routes/account.ts` + test.
- `src/backend/src/routes/couple.ts` + test.
- `src/backend/src/routes/history.ts` + test.
- `src/backend/src/routes/profileCompletion.ts` + test.
- `src/backend/prisma/migrations/20260502000000_v1_6_1_confianza/migration.sql`.
- `src/backend/tests/contracts/{auth,profile,events,tasks,notifications,points,account,couple}.contract.test.ts`.
- `src/backend/tests/rateLimit.test.ts`.

**Backend modificados:**
- `src/backend/prisma/schema.prisma` — `User.deletedAt`, `User.firstLoginAt`, `Couple.dissolvedAt`.
- `src/backend/src/server.ts` — montar nuevas rutas + middlewares rate-limit + scheduler.
- `src/backend/src/routes/profile.ts` — usar schemas de shared.
- `src/backend/src/routes/authRoutes.ts` — set `firstLoginAt` en primer login + telemetry events.
- `src/backend/jest.config.ci.js` — añadir nuevos contract tests al subset hermético.

**Frontend nuevos:**
- `src/frontend/src/pages/legal/LegalPage.tsx`.
- `src/frontend/src/pages/legal/{Privacy,Terms,Cookies}.tsx`.
- `src/frontend/src/components/Footer.tsx` + test.
- `src/frontend/src/components/CookieConsentBanner.tsx` + test.
- `src/frontend/src/components/v2/wizards/DeleteAccountWizard.tsx` + test.
- `src/frontend/src/components/v2/wizards/LeaveCoupleWizard.tsx` + test.
- `src/frontend/src/components/v2/dashboard/ProfileCompletionBanner.tsx` + test.
- `src/frontend/src/services/consent.ts` + test.
- `src/frontend/src/services/telemetry.ts` + test.
- `src/frontend/src/hooks/useConsent.ts` + test.
- `src/frontend/src/utils/profileCompletion.ts` + test.
- `src/frontend/src/components/onboarding/StepInviteeAvatar.tsx`.
- `src/frontend/src/components/onboarding/StepInviteeWork.tsx`.
- `src/frontend/src/services/__tests__/contracts.test.ts`.

**Frontend modificados:**
- `src/frontend/src/main.tsx` — montar `CookieConsentBanner` + `Footer` + telemetry.init.
- `src/frontend/src/pages/Settings.tsx` — pestañas Privacidad y ampliación de Pareja.
- `src/frontend/src/components/onboarding/StepJoinAccount.tsx` — extender flujo a 3 pasos.
- `src/frontend/src/services/apiClient.ts` — funciones nuevas (deleteAccount, leaveCouple, getPastCouples, getProfileCompletion).
- `src/frontend/src/App.tsx` (router) — rutas `/privacy`, `/terms`, `/cookies`.

**E2E nuevos:**
- `e2e/playwright.config.ts`.
- `e2e/fixtures/backend.ts`.
- `e2e/fixtures/frontend.ts`.
- `e2e/helpers/{createCouple,signInUser,seedActivity}.ts`.
- `e2e/specs/{auth-signup,auth-signup-joincode,auth-login,auth-logout-refresh,onboarding-creator,onboarding-invitee,activity-create-accept,activity-counter-rounds,activity-force,activity-reject,task-log-verify,task-log-dispute,mood-set-see,mood-expire,mood-history,phrase-cascade,avatar-picker,settings-leave-couple,settings-delete-account,privacy-cookie-consent,smoke-bottom-nav,smoke-fab-actions}.spec.ts` — 22 archivos.

**Documentación:**
- `docs/legal/{privacy,terms,cookies,rat,dpa-checklist,arco-pol-procedure,retention,posthog-dashboard,scaling-notes}.md`.
- `docs/qa/v1.6.1-qa-checklist.md`.
- `.github/workflows/ci.yml` — jobs e2e-chromium, e2e-webkit.

---

## Task 0: Pre-flight + worktree + branch

- [ ] **0.1: Verificar v1.6 en producción**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
git fetch --tags && git tag | grep v1.6
curl -s https://<render-backend-url>/api/health | jq '.lastMigration'
```

Expected: tag `v1.6` existe; `lastMigration` contiene `20260427000000_v1_6_mood_log_and_mood_keys`. Si no, **parar y completar v1.6 primero**.

- [ ] **0.2: Crear worktree**

```bash
git worktree add ~/.config/superpowers/worktrees/Matripuntos/feature/v1.6.1-confianza -b feature/v1.6.1-confianza main
cd ~/.config/superpowers/worktrees/Matripuntos/feature/v1.6.1-confianza
npm install
```

- [ ] **0.3: Verificar baseline verde**

```bash
npm run test --workspaces
npm run type-check --workspaces
```

Expected: todos verdes. Si rojo, arreglar antes de empezar.

---

## Task 1: Workspace `packages/shared` skeleton

- [ ] **1.1: Crear estructura de archivos**

```bash
mkdir -p packages/shared/src/schemas packages/shared/src/types
```

- [ ] **1.2: Crear `packages/shared/package.json`**

```json
{
  "name": "@matripuntos/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "zod": "^3.22.0" },
  "devDependencies": { "typescript": "^5.0.0", "vitest": "^1.0.0" }
}
```

- [ ] **1.3: Crear `packages/shared/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"]
}
```

- [ ] **1.4: Crear `packages/shared/src/index.ts`**

```ts
export * from './schemas/auth'
export * from './schemas/profile'
export * from './schemas/events'
export * from './schemas/tasks'
export * from './schemas/moodHistory'
export * from './schemas/account'
export * from './schemas/couple'
export * from './schemas/common'
export * from './telemetry-events'
export * from './types'
```

- [ ] **1.5: Crear `packages/shared/src/types/index.ts` (placeholder, se llenará con cada schema)**

```ts
// Re-exports inferidos por cada módulo de schemas. Placeholder.
export {}
```

- [ ] **1.6: Modificar `package.json` raíz para incluir packages/* en workspaces**

```json
{
  "workspaces": ["src/backend", "src/frontend", "packages/*"]
}
```

- [ ] **1.7: Reinstalar deps + smoke**

```bash
npm install
npm run type-check --workspace=@matripuntos/shared
```

Expected: workspace reconocido, type-check verde.

- [ ] **1.8: Commit**

```bash
git add package.json package-lock.json packages/
git commit -m "feat(v1.6.1): scaffold workspace @matripuntos/shared"
```

---

## Task 2: Migrar schemas zod existentes a shared

- [ ] **2.1: Crear `packages/shared/src/schemas/common.ts`**

```ts
import { z } from 'zod'

export const idSchema = z.string().min(1)
export const emailSchema = z.string().email()
export const passwordSchema = z.string().min(6).max(100)
export const isoDateSchema = z.string().datetime()

export const errorResponseSchema = z.object({
  error: z.string(),
  details: z.array(z.any()).optional(),
})
export type ErrorResponse = z.infer<typeof errorResponseSchema>
```

- [ ] **2.2: Crear `packages/shared/src/schemas/auth.ts`** (migrar de `src/backend/src/schemas/authSchemas.ts`)

```ts
import { z } from 'zod'
import { emailSchema, passwordSchema } from './common'

export const loginSchema = z.object({
  email: emailSchema,
  password: z.string().min(1),
})
export type LoginInput = z.infer<typeof loginSchema>

export const registerSchema = z.object({
  email: emailSchema,
  password: passwordSchema,
  name: z.string().min(1).max(80),
  joinCode: z.string().optional(),
})
export type RegisterInput = z.infer<typeof registerSchema>

export const joinCodeRegisterSchema = z.object({
  password: passwordSchema,
  name: z.string().min(1).max(80),
  token: z.string().min(1),
})
export type JoinCodeRegisterInput = z.infer<typeof joinCodeRegisterSchema>

export const authResponseSchema = z.object({
  token: z.string(),
  user: z.object({
    id: z.string(),
    email: z.string().nullable(),
    name: z.string(),
    coupleId: z.string(),
    hasCompletedOnboarding: z.boolean(),
    avatarEmoji: z.string().nullable().optional(),
    avatarColor: z.string().nullable().optional(),
    currentMood: z.string().nullable().optional(),
    moodUpdatedAt: z.string().nullable().optional(),
  }),
  couple: z.object({ id: z.string(), secretKey: z.string() }),
})
export type AuthResponse = z.infer<typeof authResponseSchema>
```

- [ ] **2.3: Crear `packages/shared/src/schemas/profile.ts`** (incluye MOOD_KEYS de v1.6)

```ts
import { z } from 'zod'

export const MOOD_KEYS = [
  'feliz','enamorado','energico','carinoso',
  'tranquilo','pensativo',
  'cansado','enfermo',
  'estresado','bajon',
] as const
export type MoodKey = typeof MOOD_KEYS[number]

export const profileUpdateSchema = z.object({
  surname: z.string().max(80).optional(),
  profilePhotoUrl: z.string().url().nullable().optional(),
  weeklyWorkHours: z.number().int().min(0).max(80).optional(),
  workMode: z.enum(['presencial','remoto','hibrido']).optional(),
  taskPreferencesLoves: z.array(z.string()).optional(),
  taskPreferencesDislikes: z.array(z.string()).optional(),
  avatarEmoji: z.string().max(8).optional(),
  avatarColor: z.string().regex(/^#[0-9a-f]{6}$/i).optional(),
  currentMood: z.enum(MOOD_KEYS).nullable().optional(),
  hasCompletedOnboarding: z.boolean().optional(),
})
export type ProfileUpdateInput = z.infer<typeof profileUpdateSchema>

export const profileCompletionResponseSchema = z.object({
  percent: z.number().int().min(0).max(100),
  missing: z.array(z.string()),
})
export type ProfileCompletion = z.infer<typeof profileCompletionResponseSchema>
```

- [ ] **2.4: Crear `packages/shared/src/schemas/events.ts`**

```ts
import { z } from 'zod'

export const eventCreateSchema = z.object({
  type: z.string().min(1).max(80),
  dateStart: z.string().datetime(),
  dateEnd: z.string().datetime(),
  numChildren: z.number().int().min(0).max(20),
  pointsBase: z.number().min(0).max(1000),
  compensation: z.string().max(500).nullable().optional(),
  compensationDiscount: z.number().min(0).max(1).optional(),
})
export type EventCreateInput = z.infer<typeof eventCreateSchema>

export const eventCounterSchema = z.object({
  pointsProposed: z.number().min(0).max(1000),
  message: z.string().max(500).optional(),
})
export type EventCounterInput = z.infer<typeof eventCounterSchema>
```

- [ ] **2.5: Crear `packages/shared/src/schemas/tasks.ts`**

```ts
import { z } from 'zod'

export const TASK_CATEGORIES = [
  'cocina','baños','limpieza','compra','logistica','cuidado','mantenimiento','jardineria','mascotas',
] as const
export type TaskCategory = typeof TASK_CATEGORIES[number]

export const taskCreateSchema = z.object({
  name: z.string().min(1).max(120),
  category: z.enum(TASK_CATEGORIES),
  pointsBase: z.number().min(0).max(50),
})
export type TaskCreateInput = z.infer<typeof taskCreateSchema>

export const taskLogCreateSchema = z.object({
  taskId: z.string(),
  date: z.string().datetime(),
  pointsBase: z.number().min(0),
  pointsFinal: z.number().min(0),
})
export type TaskLogCreateInput = z.infer<typeof taskLogCreateSchema>

export const taskDisputeSchema = z.object({
  reason: z.string().min(1).max(500),
})
export type TaskDisputeInput = z.infer<typeof taskDisputeSchema>
```

- [ ] **2.6: Crear `packages/shared/src/schemas/moodHistory.ts`**

```ts
import { z } from 'zod'
import { MOOD_KEYS } from './profile'

export const moodHistoryQuerySchema = z.object({
  days: z.coerce.number().int().min(1).max(30).default(7),
  tz: z.string().default('Europe/Madrid'),
})
export type MoodHistoryQuery = z.infer<typeof moodHistoryQuerySchema>

export const moodHistoryEntrySchema = z.object({
  date: z.string(),
  moodKey: z.enum(MOOD_KEYS).nullable(),
  emoji: z.string().optional(),
  label: z.string().optional(),
})
export type MoodHistoryEntry = z.infer<typeof moodHistoryEntrySchema>

export const moodHistoryResponseSchema = z.object({
  tz: z.string(),
  days: z.number().int(),
  history: z.array(moodHistoryEntrySchema),
})
export type MoodHistoryResponse = z.infer<typeof moodHistoryResponseSchema>
```

- [ ] **2.7: Crear `packages/shared/src/schemas/account.ts`**

```ts
import { z } from 'zod'

export const accountDeleteRequestSchema = z.object({
  password: z.string().min(1),
})
export type AccountDeleteRequestInput = z.infer<typeof accountDeleteRequestSchema>

export const accountDeleteRequestResponseSchema = z.object({
  ok: z.literal(true),
  codeViaConsole: z.boolean().optional(),
})
export type AccountDeleteRequestResponse = z.infer<typeof accountDeleteRequestResponseSchema>

export const accountDeleteSchema = z.object({
  password: z.string().min(1),
  code: z.string().length(6),
})
export type AccountDeleteInput = z.infer<typeof accountDeleteSchema>
```

- [ ] **2.8: Crear `packages/shared/src/schemas/couple.ts`**

```ts
import { z } from 'zod'

export const coupleLeaveSchema = z.object({
  password: z.string().min(1),
})
export type CoupleLeaveInput = z.infer<typeof coupleLeaveSchema>

export const pastCoupleSchema = z.object({
  id: z.string(),
  partnerName: z.string(),
  dissolvedAt: z.string(),
  createdAt: z.string(),
  finalBalance: z.number(),
  totalEvents: z.number().int(),
  totalTasks: z.number().int(),
})
export type PastCouple = z.infer<typeof pastCoupleSchema>

export const pastCouplesResponseSchema = z.object({
  pastCouples: z.array(pastCoupleSchema),
})
export type PastCouplesResponse = z.infer<typeof pastCouplesResponseSchema>
```

- [ ] **2.9: Crear placeholder vacío `packages/shared/src/telemetry-events.ts` (se llena en Task 13)**

```ts
export const TELEMETRY_EVENTS_PLACEHOLDER = true
```

- [ ] **2.10: Backend usa `@matripuntos/shared` — añadir dependencia**

En `src/backend/package.json` añadir a `dependencies`:
```json
"@matripuntos/shared": "*"
```

- [ ] **2.11: Frontend usa `@matripuntos/shared` — añadir dependencia**

En `src/frontend/package.json` añadir a `dependencies`:
```json
"@matripuntos/shared": "*"
```

- [ ] **2.12: Re-instalar y verificar resolución**

```bash
npm install
node -e "require.resolve('@matripuntos/shared', { paths: ['./src/backend'] })"
node -e "require.resolve('@matripuntos/shared', { paths: ['./src/frontend'] })"
```

Expected: ambos resuelven al path del workspace.

- [ ] **2.13: Refactor backend authRoutes.ts para usar schemas de shared**

Reemplazar imports locales por:
```ts
import { loginSchema, registerSchema, joinCodeRegisterSchema } from '@matripuntos/shared'
```

Borrar `src/backend/src/schemas/authSchemas.ts` si queda vacío. Verificar tests verdes:
```bash
npm run test --workspace=src/backend
```

- [ ] **2.14: Refactor backend profile.ts (PUT /me) para usar `profileUpdateSchema` de shared**

Reemplazar el zod schema inline por import. Mismo procedimiento.

- [ ] **2.15: Verde + commit**

```bash
npm run test --workspaces
npm run type-check --workspaces
git add packages src/backend src/frontend package.json package-lock.json
git commit -m "feat(v1.6.1): migrar schemas zod a @matripuntos/shared"
```

---

## Task 3: Schema Prisma + migración v1.6.1

- [ ] **3.1: Editar `src/backend/prisma/schema.prisma`**

En el modelo `User` añadir:
```prisma
deletedAt    DateTime?
firstLoginAt DateTime?
```

En el modelo `Couple` añadir:
```prisma
dissolvedAt DateTime?
```

- [ ] **3.2: Generar migración**

```bash
cd src/backend
npx prisma migrate dev --name v1_6_1_confianza --create-only
```

Renombrar timestamp si Prisma genera otro a `20260502000000`.

- [ ] **3.3: Editar el SQL para añadir backfill de firstLoginAt**

Append al `migration.sql` generado:
```sql
UPDATE "User" SET "firstLoginAt" = "createdAt" WHERE "firstLoginAt" IS NULL;
```

- [ ] **3.4: Aplicar y regenerar client**

```bash
npx prisma migrate dev
npx prisma generate
```

- [ ] **3.5: Smoke test**

```bash
node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.user.findFirst({select:{id:true,deletedAt:true,firstLoginAt:true}}).then(r=>{console.log('OK',JSON.stringify(r));process.exit(0)})"
```

Expected: imprime user con campos nuevos accesibles.

- [ ] **3.6: Commit**

```bash
git add src/backend/prisma/schema.prisma src/backend/prisma/migrations/20260502000000_v1_6_1_confianza/
git commit -m "feat(v1.6.1): schema deletedAt + firstLoginAt + dissolvedAt + migración"
```

---

## Task 4: `accountDeletionService` + tests

- [ ] **4.1: Test failing primero**

```ts
// src/backend/src/services/accountDeletionService.test.ts
import { deleteAccount } from './accountDeletionService'
import prisma from '../lib/prisma'

describe('accountDeletionService', () => {
  let coupleId: string, userId: string, partnerId: string

  beforeEach(async () => {
    const couple = await prisma.couple.create({ data: { secretKey: 'ad-' + Date.now() } })
    coupleId = couple.id
    const u1 = await prisma.user.create({ data: { coupleId, email: `a${Date.now()}@x.com`, passwordHash: 'h', name: 'A' } })
    const u2 = await prisma.user.create({ data: { coupleId, email: `b${Date.now()}@x.com`, passwordHash: 'h', name: 'B' } })
    userId = u1.id; partnerId = u2.id
    await prisma.userProfile.create({ data: { userId } })
    await prisma.userProfile.create({ data: { userId: partnerId } })
    // crear datos relacionados
    await prisma.pointsTransaction.create({ data: { coupleId, userId, type: 'event_accepted', amount: 10 } })
    await prisma.moodLog.create({ data: { userId, coupleId, moodKey: 'feliz' } })
    await prisma.notification.create({ data: { userId, coupleId, type: 'task_pending', title: 'X', message: 'y' } })
  })

  afterEach(async () => {
    await prisma.pointsTransaction.deleteMany({ where: { coupleId } })
    await prisma.moodLog.deleteMany({ where: { coupleId } })
    await prisma.notification.deleteMany({ where: { coupleId } })
    await prisma.userProfile.deleteMany({ where: { user: { coupleId } } })
    await prisma.user.deleteMany({ where: { coupleId } })
    await prisma.couple.deleteMany({ where: { id: coupleId } })
  })

  it('marca user.deletedAt y limpia PII', async () => {
    await deleteAccount(userId)
    const u = await prisma.user.findUnique({ where: { id: userId } })
    expect(u?.deletedAt).not.toBeNull()
    expect(u?.email).toBeNull()
    expect(u?.passwordHash).toBe('')
    expect(u?.name).toBe('Usuario eliminado')
  })

  it('crea ghost user único por couple y reasigna PointsTransaction', async () => {
    await deleteAccount(userId)
    const ghosts = await prisma.user.findMany({ where: { coupleId, name: 'Usuario eliminado' } })
    expect(ghosts).toHaveLength(1)
    const tx = await prisma.pointsTransaction.findFirst({ where: { coupleId } })
    expect(tx?.userId).toBe(ghosts[0].id)
  })

  it('borra MoodLog y Notification del user', async () => {
    await deleteAccount(userId)
    expect(await prisma.moodLog.count({ where: { userId } })).toBe(0)
    expect(await prisma.notification.count({ where: { userId } })).toBe(0)
  })

  it('si era el último activo, marca couple.dissolvedAt', async () => {
    await deleteAccount(userId)  // primer borrado, partner sigue
    let c = await prisma.couple.findUnique({ where: { id: coupleId } })
    expect(c?.dissolvedAt).toBeNull()
    await deleteAccount(partnerId)
    c = await prisma.couple.findUnique({ where: { id: coupleId } })
    expect(c?.dissolvedAt).not.toBeNull()
  })

  it('idempotente: segundo delete del mismo user no rompe', async () => {
    await deleteAccount(userId)
    await expect(deleteAccount(userId)).resolves.not.toThrow()
  })
})
```

- [ ] **4.2: Run + verify fail**

```bash
cd src/backend && npx jest accountDeletionService.test.ts
```

Expected: FAIL (módulo no existe).

- [ ] **4.3: Implementar `accountDeletionService.ts`**

```ts
// src/backend/src/services/accountDeletionService.ts
import prisma from '../lib/prisma.js'

export async function deleteAccount(userId: string): Promise<void> {
  const user = await prisma.user.findUnique({ where: { id: userId } })
  if (!user) throw new Error('User not found')
  if (user.deletedAt) return  // idempotente

  const coupleId = user.coupleId

  await prisma.$transaction(async (tx) => {
    // 1. Crear o reutilizar ghost
    let ghost = await tx.user.findFirst({
      where: { coupleId, name: 'Usuario eliminado', deletedAt: { not: null } },
    })
    if (!ghost) {
      ghost = await tx.user.create({
        data: {
          coupleId,
          email: null as any,
          passwordHash: '',
          name: 'Usuario eliminado',
          deletedAt: new Date(),
          hasCompletedOnboarding: true,
        },
      })
    }

    // 2. Reasignar referencias
    await tx.pointsTransaction.updateMany({ where: { userId }, data: { userId: ghost.id } })
    await tx.taskLog.updateMany({ where: { completedBy: userId }, data: { completedBy: ghost.id } })
    await tx.taskLog.updateMany({ where: { verifiedBy: userId }, data: { verifiedBy: ghost.id } })
    await tx.event.updateMany({ where: { createdBy: userId }, data: { createdBy: ghost.id } })
    await tx.negotiation.updateMany({ where: { proposedBy: userId }, data: { proposedBy: ghost.id } })
    await tx.negotiation.updateMany({ where: { respondedBy: userId }, data: { respondedBy: ghost.id } })

    // 3. Borrar datos personales
    await tx.moodLog.deleteMany({ where: { userId } })
    await tx.notification.deleteMany({ where: { userId } })
    await tx.userProfile.deleteMany({ where: { userId } })

    // 4. Soft delete del user (no DELETE para preservar fk integrity hasta cron de purga)
    await tx.user.update({
      where: { id: userId },
      data: {
        deletedAt: new Date(),
        email: null,
        passwordHash: '',
        name: 'Usuario eliminado',
      },
    })

    // 5. Si era el último user activo del couple, marcar dissolved
    const activeUsers = await tx.user.count({
      where: { coupleId, deletedAt: null },
    })
    if (activeUsers === 0) {
      await tx.couple.update({ where: { id: coupleId }, data: { dissolvedAt: new Date() } })
    }
  })
}
```

- [ ] **4.4: Verde + commit**

```bash
npx jest accountDeletionService.test.ts
git add src/backend/src/services/accountDeletionService.ts src/backend/src/services/accountDeletionService.test.ts
git commit -m "feat(v1.6.1): accountDeletionService con ghost user + tests"
```

---

## Task 5: Routes `/api/account/*` + tests + frontend wizard

- [ ] **5.1: Test de routes failing**

```ts
// src/backend/tests/contracts/account.contract.test.ts
import request from 'supertest'
import app from '../../src/server'
import prisma from '../../src/lib/prisma'
import bcrypt from 'bcrypt'
import { signTestJwt } from '../helpers'

describe('POST /api/account/delete-request + delete', () => {
  let userId: string, coupleId: string, token: string
  const password = 'pwd1234'

  beforeEach(async () => {
    const couple = await prisma.couple.create({ data: { secretKey: 'd-' + Date.now() } })
    coupleId = couple.id
    const hash = await bcrypt.hash(password, 10)
    const u = await prisma.user.create({ data: { coupleId, email: `d${Date.now()}@x.com`, passwordHash: hash, name: 'D' } })
    userId = u.id
    token = signTestJwt({ userId, coupleId })
  })

  afterEach(async () => {
    await prisma.user.deleteMany({ where: { coupleId } })
    await prisma.couple.deleteMany({ where: { id: coupleId } })
  })

  it('delete-request con password correcto → 200 + code (consola en dev)', async () => {
    const res = await request(app).post('/api/account/delete-request')
      .set('Authorization', `Bearer ${token}`)
      .send({ password })
    expect(res.status).toBe(200)
    expect(res.body.ok).toBe(true)
  })

  it('delete-request con password incorrecto → 401', async () => {
    const res = await request(app).post('/api/account/delete-request')
      .set('Authorization', `Bearer ${token}`)
      .send({ password: 'wrong' })
    expect(res.status).toBe(401)
  })

  it('delete con código válido → 200 + user marcado deletedAt', async () => {
    // simular code emitido (en dev devuelve el code en el response o en consola)
    const r1 = await request(app).post('/api/account/delete-request')
      .set('Authorization', `Bearer ${token}`).send({ password })
    const code = r1.body.code  // dev only
    const res = await request(app).post('/api/account/delete')
      .set('Authorization', `Bearer ${token}`)
      .send({ password, code })
    expect(res.status).toBe(200)
    const u = await prisma.user.findUnique({ where: { id: userId } })
    expect(u?.deletedAt).not.toBeNull()
  })

  it('delete con código inválido → 401', async () => {
    const res = await request(app).post('/api/account/delete')
      .set('Authorization', `Bearer ${token}`)
      .send({ password, code: '000000' })
    expect(res.status).toBe(401)
  })
})
```

- [ ] **5.2: Implementar `src/backend/src/routes/account.ts`**

```ts
// src/backend/src/routes/account.ts
import { Router } from 'express'
import bcrypt from 'bcrypt'
import { authenticateToken } from '../middleware/auth.js'
import { criticalBucket } from '../middleware/rateLimiter.js'  // creado en Task 19
import prisma from '../lib/prisma.js'
import { accountDeleteRequestSchema, accountDeleteSchema } from '@matripuntos/shared'
import { deleteAccount } from '../services/accountDeletionService.js'

const router = Router()
router.use(authenticateToken)

// in-memory store de codes (TTL 15min, single-use)
const deleteCodes = new Map<string, { code: string; expiresAt: number }>()

router.post('/delete-request', criticalBucket, async (req, res) => {
  const userId = (req as any).user.id
  const parsed = accountDeleteRequestSchema.safeParse(req.body)
  if (!parsed.success) return res.status(400).json({ error: 'Datos inválidos' })

  const user = await prisma.user.findUnique({ where: { id: userId } })
  if (!user) return res.status(404).json({ error: 'Usuario no encontrado' })

  const ok = await bcrypt.compare(parsed.data.password, user.passwordHash)
  if (!ok) return res.status(401).json({ error: 'Contraseña incorrecta' })

  const code = String(Math.floor(100000 + Math.random() * 900000))
  deleteCodes.set(userId, { code, expiresAt: Date.now() + 15 * 60 * 1000 })

  const isDev = process.env.NODE_ENV !== 'production'
  if (isDev) {
    console.log(`[DELETE-CODE] user=${userId} code=${code}`)
    return res.json({ ok: true, codeViaConsole: true, code })  // code expuesto solo en dev
  }

  // En prod: enviar email vía SMTP. Si no configurado → 503.
  if (!process.env.SMTP_HOST) {
    return res.status(503).json({ error: 'Servicio de email no disponible. Contacta soporte.' })
  }
  // TODO post-merge: integrar SMTP real (Resend / SendGrid). Por ahora docs/legal/scaling-notes.md cubre el plan.
  return res.json({ ok: true })
})

router.post('/delete', criticalBucket, async (req, res) => {
  const userId = (req as any).user.id
  const parsed = accountDeleteSchema.safeParse(req.body)
  if (!parsed.success) return res.status(400).json({ error: 'Datos inválidos' })

  const user = await prisma.user.findUnique({ where: { id: userId } })
  if (!user) return res.status(404).json({ error: 'Usuario no encontrado' })

  const ok = await bcrypt.compare(parsed.data.password, user.passwordHash)
  if (!ok) return res.status(401).json({ error: 'Contraseña incorrecta' })

  const stored = deleteCodes.get(userId)
  if (!stored || stored.code !== parsed.data.code || stored.expiresAt < Date.now()) {
    return res.status(401).json({ error: 'Código inválido o expirado' })
  }
  deleteCodes.delete(userId)

  await deleteAccount(userId)
  res.json({ ok: true })
})

export default router
```

- [ ] **5.3: Registrar router en `server.ts`**

```ts
import accountRoutes from './routes/account.js'
// ...
app.use('/api/account', accountRoutes)
```

- [ ] **5.4: Run tests verdes**

```bash
npx jest tests/contracts/account.contract.test.ts
```

- [ ] **5.5: Crear `src/frontend/src/components/v2/wizards/DeleteAccountWizard.tsx`**

```tsx
import { useState } from 'react'
import apiClient from '../../../services/apiClient'

interface Props { isOpen: boolean; onClose: () => void; onDeleted: () => void }

export function DeleteAccountWizard({ isOpen, onClose, onDeleted }: Props) {
  const [step, setStep] = useState<1|2|3>(1)
  const [password, setPassword] = useState('')
  const [code, setCode] = useState('')
  const [confirm, setConfirm] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  if (!isOpen) return null

  const requestCode = async () => {
    setLoading(true); setError(null)
    try {
      const { data } = await apiClient.post('/account/delete-request', { password })
      if (data.codeViaConsole) console.log('Código:', data.code)  // dev fallback
      setStep(3)
    } catch (e: any) {
      setError(e.response?.data?.error ?? 'Error desconocido')
    }
    setLoading(false)
  }

  const confirmDelete = async () => {
    if (confirm !== 'ELIMINAR') { setError('Debes escribir ELIMINAR'); return }
    setLoading(true); setError(null)
    try {
      await apiClient.post('/account/delete', { password, code })
      onDeleted()
    } catch (e: any) {
      setError(e.response?.data?.error ?? 'Error desconocido')
      setLoading(false)
    }
  }

  return (
    <div data-testid="delete-account-wizard" className="fixed inset-0 z-50 flex items-center justify-center bg-black/70">
      <div className="bg-[#1a1035] rounded-2xl p-6 max-w-md w-full mx-4">
        {step === 1 && (
          <>
            <h2 className="text-xl font-bold text-white mb-3">Eliminar cuenta</h2>
            <p className="text-sm text-white/80 mb-4">
              Esto eliminará tus datos personales. Tu pareja conservará el histórico de la etapa
              compartida marcado como "Usuario eliminado". Es irreversible.
            </p>
            <div className="flex gap-2 justify-end">
              <button onClick={onClose} className="px-4 py-2 rounded-lg bg-white/10 text-white">Cancelar</button>
              <button data-testid="btn-step1-continue" onClick={() => setStep(2)} className="px-4 py-2 rounded-lg bg-red-500 text-white">Continuar</button>
            </div>
          </>
        )}
        {step === 2 && (
          <>
            <h2 className="text-xl font-bold text-white mb-3">Verifica tu identidad</h2>
            <input type="password" placeholder="Contraseña" value={password} onChange={e => setPassword(e.target.value)}
              className="w-full px-3 py-2 rounded-lg bg-white/10 text-white mb-3" data-testid="input-password" />
            {error && <p className="text-red-400 text-sm mb-2">{error}</p>}
            <div className="flex gap-2 justify-end">
              <button onClick={() => setStep(1)} className="px-4 py-2 rounded-lg bg-white/10 text-white">Atrás</button>
              <button disabled={loading} data-testid="btn-step2-send-code" onClick={requestCode} className="px-4 py-2 rounded-lg bg-red-500 text-white">Enviar código</button>
            </div>
          </>
        )}
        {step === 3 && (
          <>
            <h2 className="text-xl font-bold text-white mb-3">Confirmación final</h2>
            <p className="text-xs text-white/70 mb-2">Te hemos enviado un código por email (revisa también la consola en dev).</p>
            <input placeholder="Código de 6 dígitos" maxLength={6} value={code} onChange={e => setCode(e.target.value)}
              className="w-full px-3 py-2 rounded-lg bg-white/10 text-white mb-2" data-testid="input-code" />
            <input placeholder="Escribe ELIMINAR" value={confirm} onChange={e => setConfirm(e.target.value)}
              className="w-full px-3 py-2 rounded-lg bg-white/10 text-white mb-3" data-testid="input-confirm" />
            {error && <p className="text-red-400 text-sm mb-2">{error}</p>}
            <div className="flex gap-2 justify-end">
              <button onClick={() => setStep(2)} className="px-4 py-2 rounded-lg bg-white/10 text-white">Atrás</button>
              <button disabled={loading} data-testid="btn-step3-confirm" onClick={confirmDelete} className="px-4 py-2 rounded-lg bg-red-600 text-white">Eliminar definitivamente</button>
            </div>
          </>
        )}
      </div>
    </div>
  )
}
```

- [ ] **5.6: Test del wizard**

```tsx
// src/frontend/src/components/v2/wizards/DeleteAccountWizard.test.tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { DeleteAccountWizard } from './DeleteAccountWizard'

describe('DeleteAccountWizard', () => {
  it('renderiza paso 1 al abrir', () => {
    render(<DeleteAccountWizard isOpen onClose={() => {}} onDeleted={() => {}} />)
    expect(screen.getByText(/Eliminar cuenta/)).toBeInTheDocument()
    expect(screen.getByTestId('btn-step1-continue')).toBeInTheDocument()
  })

  it('no renderiza si !isOpen', () => {
    render(<DeleteAccountWizard isOpen={false} onClose={() => {}} onDeleted={() => {}} />)
    expect(screen.queryByTestId('delete-account-wizard')).toBeNull()
  })

  it('avanza de paso 1 a paso 2 al pulsar Continuar', () => {
    render(<DeleteAccountWizard isOpen onClose={() => {}} onDeleted={() => {}} />)
    fireEvent.click(screen.getByTestId('btn-step1-continue'))
    expect(screen.getByTestId('input-password')).toBeInTheDocument()
  })
})
```

- [ ] **5.7: Verde + commit**

```bash
npx vitest run src/components/v2/wizards/DeleteAccountWizard.test.tsx
git add src/backend/src/routes/account.ts src/backend/tests/contracts/account.contract.test.ts src/frontend/src/components/v2/wizards/DeleteAccountWizard.tsx src/frontend/src/components/v2/wizards/DeleteAccountWizard.test.tsx src/backend/src/server.ts
git commit -m "feat(v1.6.1): account delete-request + delete + wizard 3 pasos"
```

---

## Task 6: `coupleLifecycleService` + tests

- [ ] **6.1: Test failing**

```ts
// src/backend/src/services/coupleLifecycleService.test.ts
import { dissolveCouple } from './coupleLifecycleService'
import prisma from '../lib/prisma'

describe('coupleLifecycleService', () => {
  it('dissolveCouple marca dissolvedAt y reasigna users a couples nuevos', async () => {
    const c = await prisma.couple.create({ data: { secretKey: 'cl-' + Date.now() } })
    const u1 = await prisma.user.create({ data: { coupleId: c.id, email: `u1${Date.now()}@x.com`, passwordHash: 'h', name: 'U1' } })
    const u2 = await prisma.user.create({ data: { coupleId: c.id, email: `u2${Date.now()}@x.com`, passwordHash: 'h', name: 'U2' } })

    await dissolveCouple(c.id)

    const oldCouple = await prisma.couple.findUnique({ where: { id: c.id } })
    expect(oldCouple?.dissolvedAt).not.toBeNull()

    const u1After = await prisma.user.findUnique({ where: { id: u1.id } })
    const u2After = await prisma.user.findUnique({ where: { id: u2.id } })
    expect(u1After?.coupleId).not.toBe(c.id)
    expect(u2After?.coupleId).not.toBe(c.id)
    expect(u1After?.coupleId).not.toBe(u2After?.coupleId)  // couples distintos

    // limpieza
    await prisma.user.deleteMany({ where: { id: { in: [u1.id, u2.id] } } })
    await prisma.couple.deleteMany({ where: { id: { in: [c.id, u1After!.coupleId, u2After!.coupleId] } } })
  })
})
```

- [ ] **6.2: Implementar `coupleLifecycleService.ts`**

```ts
// src/backend/src/services/coupleLifecycleService.ts
import crypto from 'crypto'
import prisma from '../lib/prisma.js'

export async function dissolveCouple(coupleId: string): Promise<void> {
  const couple = await prisma.couple.findUnique({
    where: { id: coupleId },
    include: { users: { where: { deletedAt: null } } },
  })
  if (!couple) throw new Error('Couple not found')
  if (couple.dissolvedAt) return  // idempotente

  await prisma.$transaction(async (tx) => {
    await tx.couple.update({ where: { id: coupleId }, data: { dissolvedAt: new Date() } })

    for (const user of couple.users) {
      const newCouple = await tx.couple.create({
        data: {
          secretKey: crypto.randomBytes(16).toString('hex'),
          numChildren: 0,
          language: couple.language ?? 'es',
        },
      })
      await tx.user.update({ where: { id: user.id }, data: { coupleId: newCouple.id } })
    }
  })
}
```

- [ ] **6.3: Verde + commit**

```bash
npx jest coupleLifecycleService.test.ts
git add src/backend/src/services/coupleLifecycleService.ts src/backend/src/services/coupleLifecycleService.test.ts
git commit -m "feat(v1.6.1): coupleLifecycleService.dissolveCouple"
```

---

## Task 7: Routes `/api/couple/leave` + `/api/history/past-couples` + wizard

- [ ] **7.1: Test contract failing**

```ts
// src/backend/tests/contracts/couple.contract.test.ts
import request from 'supertest'
import app from '../../src/server'
import prisma from '../../src/lib/prisma'
import bcrypt from 'bcrypt'
import { signTestJwt } from '../helpers'

describe('couple leave + past-couples', () => {
  let coupleId: string, userId: string, partnerId: string, token: string
  const password = 'pwd1234'

  beforeEach(async () => {
    const couple = await prisma.couple.create({ data: { secretKey: 'cl-' + Date.now() } })
    coupleId = couple.id
    const h = await bcrypt.hash(password, 10)
    const u = await prisma.user.create({ data: { coupleId, email: `cu${Date.now()}@x.com`, passwordHash: h, name: 'U' } })
    const p = await prisma.user.create({ data: { coupleId, email: `cp${Date.now()}@x.com`, passwordHash: h, name: 'P' } })
    userId = u.id; partnerId = p.id
    token = signTestJwt({ userId, coupleId })
  })

  afterEach(async () => {
    await prisma.user.deleteMany({ where: { OR: [{ id: { in: [userId, partnerId] } }, { coupleId: { not: coupleId } }] } })
    await prisma.couple.deleteMany({ where: { id: coupleId } })
  })

  it('POST /couple/leave con password correcto → 200', async () => {
    const res = await request(app).post('/api/couple/leave')
      .set('Authorization', `Bearer ${token}`).send({ password })
    expect(res.status).toBe(200)
  })

  it('POST /couple/leave password incorrecto → 401', async () => {
    const res = await request(app).post('/api/couple/leave')
      .set('Authorization', `Bearer ${token}`).send({ password: 'wrong' })
    expect(res.status).toBe(401)
  })

  it('GET /history/past-couples devuelve lista vacía si no hay disueltos', async () => {
    const res = await request(app).get('/api/history/past-couples')
      .set('Authorization', `Bearer ${token}`)
    expect(res.status).toBe(200)
    expect(res.body.pastCouples).toEqual([])
  })

  it('tras leave, GET past-couples incluye el couple disuelto', async () => {
    await request(app).post('/api/couple/leave').set('Authorization', `Bearer ${token}`).send({ password })
    // re-firmar token con nuevo coupleId del user
    const u = await prisma.user.findUnique({ where: { id: userId } })
    const newToken = signTestJwt({ userId, coupleId: u!.coupleId })
    const res = await request(app).get('/api/history/past-couples')
      .set('Authorization', `Bearer ${newToken}`)
    expect(res.body.pastCouples).toHaveLength(1)
    expect(res.body.pastCouples[0].partnerName).toBe('P')
  })
})
```

- [ ] **7.2: Implementar `src/backend/src/routes/couple.ts`**

```ts
// src/backend/src/routes/couple.ts
import { Router } from 'express'
import bcrypt from 'bcrypt'
import { authenticateToken } from '../middleware/auth.js'
import { criticalBucket } from '../middleware/rateLimiter.js'
import prisma from '../lib/prisma.js'
import { coupleLeaveSchema } from '@matripuntos/shared'
import { dissolveCouple } from '../services/coupleLifecycleService.js'

const router = Router()
router.use(authenticateToken)

router.post('/leave', criticalBucket, async (req, res) => {
  const userId = (req as any).user.id
  const coupleId = (req as any).user.coupleId

  const parsed = coupleLeaveSchema.safeParse(req.body)
  if (!parsed.success) return res.status(400).json({ error: 'Datos inválidos' })

  const user = await prisma.user.findUnique({ where: { id: userId } })
  if (!user) return res.status(404).json({ error: 'Usuario no encontrado' })

  const ok = await bcrypt.compare(parsed.data.password, user.passwordHash)
  if (!ok) return res.status(401).json({ error: 'Contraseña incorrecta' })

  await dissolveCouple(coupleId)

  // Devolver el nuevo coupleId del user para que el frontend renueve token
  const updated = await prisma.user.findUnique({ where: { id: userId } })
  res.json({ ok: true, newCoupleId: updated?.coupleId })
})

export default router
```

- [ ] **7.3: Implementar `src/backend/src/routes/history.ts`**

```ts
// src/backend/src/routes/history.ts
import { Router } from 'express'
import { authenticateToken } from '../middleware/auth.js'
import prisma from '../lib/prisma.js'

const router = Router()
router.use(authenticateToken)

router.get('/past-couples', async (req, res) => {
  const userId = (req as any).user.id
  // past-couples = couples con dissolvedAt donde el user (o el ghost del user) tuvo PointsTransaction
  const txs = await prisma.pointsTransaction.findMany({
    where: { userId, couple: { dissolvedAt: { not: null } } },
    select: { coupleId: true },
    distinct: ['coupleId'],
  })
  const coupleIds = txs.map(t => t.coupleId)
  const couples = await prisma.couple.findMany({
    where: { id: { in: coupleIds } },
    include: { users: true },
  })
  const result = couples.map(c => {
    const partner = c.users.find(u => u.id !== userId)
    const txCount = c.users.length  // simplificación
    return {
      id: c.id,
      partnerName: partner?.name ?? 'Desconocido',
      dissolvedAt: c.dissolvedAt!.toISOString(),
      createdAt: c.createdAt.toISOString(),
      finalBalance: 0,  // TODO: calcular si se requiere; placeholder no-blocking
      totalEvents: 0,
      totalTasks: 0,
    }
  })
  res.json({ pastCouples: result })
})

router.get('/past-couples/:id', async (req, res) => {
  const userId = (req as any).user.id
  const couple = await prisma.couple.findUnique({
    where: { id: req.params.id },
    include: {
      users: true,
      events: { orderBy: { dateStart: 'desc' }, take: 100 },
      tasks: true,
    },
  })
  if (!couple || !couple.dissolvedAt) return res.status(404).json({ error: 'No encontrado' })
  // Verificar que el user perteneció a este couple
  const hadAccess = await prisma.pointsTransaction.findFirst({ where: { userId, coupleId: couple.id } })
  if (!hadAccess) return res.status(403).json({ error: 'Sin acceso' })
  res.json({ couple })
})

export default router
```

- [ ] **7.4: Registrar routers en `server.ts`**

```ts
import coupleRoutes from './routes/couple.js'
import historyRoutes from './routes/history.js'
app.use('/api/couple', coupleRoutes)
app.use('/api/history', historyRoutes)
```

- [ ] **7.5: Implementar `LeaveCoupleWizard.tsx` (similar al DeleteAccountWizard, 2 pasos)**

```tsx
// src/frontend/src/components/v2/wizards/LeaveCoupleWizard.tsx
import { useState } from 'react'
import apiClient from '../../../services/apiClient'

interface Props { isOpen: boolean; onClose: () => void; onLeft: () => void }

export function LeaveCoupleWizard({ isOpen, onClose, onLeft }: Props) {
  const [step, setStep] = useState<1|2>(1)
  const [password, setPassword] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  if (!isOpen) return null

  const submit = async () => {
    setLoading(true); setError(null)
    try {
      await apiClient.post('/couple/leave', { password })
      onLeft()
    } catch (e: any) {
      setError(e.response?.data?.error ?? 'Error desconocido')
      setLoading(false)
    }
  }

  return (
    <div data-testid="leave-couple-wizard" className="fixed inset-0 z-50 flex items-center justify-center bg-black/70">
      <div className="bg-[#1a1035] rounded-2xl p-6 max-w-md w-full mx-4">
        {step === 1 && (
          <>
            <h2 className="text-xl font-bold text-white mb-3">Salir de la pareja</h2>
            <p className="text-sm text-white/80 mb-4">
              Ambos quedaréis sin pareja activa. Podréis ver "Mi etapa con [nombre]" como histórico
              read-only en Settings y emparejaros con otra persona después.
            </p>
            <div className="flex gap-2 justify-end">
              <button onClick={onClose} className="px-4 py-2 rounded-lg bg-white/10 text-white">Cancelar</button>
              <button data-testid="btn-step1-continue" onClick={() => setStep(2)} className="px-4 py-2 rounded-lg bg-amber-500 text-white">Continuar</button>
            </div>
          </>
        )}
        {step === 2 && (
          <>
            <h2 className="text-xl font-bold text-white mb-3">Confirma tu identidad</h2>
            <input type="password" placeholder="Contraseña" value={password} onChange={e => setPassword(e.target.value)}
              className="w-full px-3 py-2 rounded-lg bg-white/10 text-white mb-3" data-testid="input-password" />
            {error && <p className="text-red-400 text-sm mb-2">{error}</p>}
            <div className="flex gap-2 justify-end">
              <button onClick={() => setStep(1)} className="px-4 py-2 rounded-lg bg-white/10 text-white">Atrás</button>
              <button disabled={loading} data-testid="btn-step2-confirm" onClick={submit} className="px-4 py-2 rounded-lg bg-amber-600 text-white">Confirmar</button>
            </div>
          </>
        )}
      </div>
    </div>
  )
}
```

- [ ] **7.6: Test del wizard (similar al de DeleteAccountWizard)**

```tsx
// src/frontend/src/components/v2/wizards/LeaveCoupleWizard.test.tsx
import { describe, it, expect } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { LeaveCoupleWizard } from './LeaveCoupleWizard'

describe('LeaveCoupleWizard', () => {
  it('avanza de paso 1 a paso 2', () => {
    render(<LeaveCoupleWizard isOpen onClose={() => {}} onLeft={() => {}} />)
    fireEvent.click(screen.getByTestId('btn-step1-continue'))
    expect(screen.getByTestId('input-password')).toBeInTheDocument()
  })
})
```

- [ ] **7.7: Verde + commit**

```bash
npx jest tests/contracts/couple.contract.test.ts
npx vitest run src/components/v2/wizards/LeaveCoupleWizard.test.tsx
git add src/backend/src/routes/couple.ts src/backend/src/routes/history.ts src/backend/tests/contracts/couple.contract.test.ts src/frontend/src/components/v2/wizards/LeaveCoupleWizard.tsx src/frontend/src/components/v2/wizards/LeaveCoupleWizard.test.tsx src/backend/src/server.ts
git commit -m "feat(v1.6.1): couple leave + past-couples routes + wizard"
```

---

## Task 8: Cron `dataRetentionJob` + scheduler + tests

- [ ] **8.1: Test failing**

```ts
// src/backend/src/jobs/dataRetentionJob.test.ts
import { runRetention } from './dataRetentionJob'
import prisma from '../lib/prisma'

describe('dataRetentionJob', () => {
  it('borra MoodLog >90 días', async () => {
    const couple = await prisma.couple.create({ data: { secretKey: 'r-' + Date.now() } })
    const user = await prisma.user.create({ data: { coupleId: couple.id, email: `r${Date.now()}@x.com`, passwordHash: 'h', name: 'R' } })
    const old = new Date(Date.now() - 91 * 86400000)
    await prisma.moodLog.create({ data: { userId: user.id, coupleId: couple.id, moodKey: 'feliz', createdAt: old } })
    const recent = await prisma.moodLog.create({ data: { userId: user.id, coupleId: couple.id, moodKey: 'cansado' } })

    await runRetention({ dryRun: false })

    expect(await prisma.moodLog.count({ where: { userId: user.id } })).toBe(1)
    const remaining = await prisma.moodLog.findFirst({ where: { userId: user.id } })
    expect(remaining?.id).toBe(recent.id)

    await prisma.user.delete({ where: { id: user.id } })
    await prisma.couple.delete({ where: { id: couple.id } })
  })

  it('en dry-run no borra, solo loguea', async () => {
    const couple = await prisma.couple.create({ data: { secretKey: 'rd-' + Date.now() } })
    const user = await prisma.user.create({ data: { coupleId: couple.id, email: `rd${Date.now()}@x.com`, passwordHash: 'h', name: 'R' } })
    const old = new Date(Date.now() - 91 * 86400000)
    await prisma.moodLog.create({ data: { userId: user.id, coupleId: couple.id, moodKey: 'feliz', createdAt: old } })

    const result = await runRetention({ dryRun: true })

    expect(await prisma.moodLog.count({ where: { userId: user.id } })).toBe(1)
    expect(result.moodLog).toBe(1)  // hubieran sido borrados

    await prisma.moodLog.deleteMany({ where: { userId: user.id } })
    await prisma.user.delete({ where: { id: user.id } })
    await prisma.couple.delete({ where: { id: couple.id } })
  })
})
```

- [ ] **8.2: Implementar `dataRetentionJob.ts`**

```ts
// src/backend/src/jobs/dataRetentionJob.ts
import prisma from '../lib/prisma.js'

export interface RetentionResult {
  moodLog: number
  notification: number
  invitation: number
  userPurged: number
}

export async function runRetention(opts: { dryRun: boolean } = { dryRun: false }): Promise<RetentionResult> {
  const now = Date.now()
  const moodCutoff = new Date(now - 90 * 86400000)
  const notifCutoff = new Date(now - 60 * 86400000)
  const inviteCutoff = new Date(now - 14 * 86400000)
  const userPurgeCutoff = new Date(now - 30 * 86400000)

  if (opts.dryRun) {
    const [moodLog, notification, invitation, userPurged] = await Promise.all([
      prisma.moodLog.count({ where: { createdAt: { lt: moodCutoff } } }),
      prisma.notification.count({ where: { createdAt: { lt: notifCutoff } } }),
      prisma.invitation.count({ where: { status: 'pending', expiresAt: { lt: inviteCutoff } } }),
      prisma.user.count({ where: { deletedAt: { lt: userPurgeCutoff } } }),
    ])
    console.log('[retention DRY-RUN]', { moodLog, notification, invitation, userPurged })
    return { moodLog, notification, invitation, userPurged }
  }

  const moodLog = (await prisma.moodLog.deleteMany({ where: { createdAt: { lt: moodCutoff } } })).count
  const notification = (await prisma.notification.deleteMany({ where: { createdAt: { lt: notifCutoff } } })).count
  const invitation = (await prisma.invitation.deleteMany({ where: { status: 'pending', expiresAt: { lt: inviteCutoff } } })).count
  const userPurged = (await prisma.user.deleteMany({ where: { deletedAt: { lt: userPurgeCutoff } } })).count

  console.log('[retention]', { moodLog, notification, invitation, userPurged })
  return { moodLog, notification, invitation, userPurged }
}
```

- [ ] **8.3: Implementar `scheduler.ts`**

```ts
// src/backend/src/jobs/scheduler.ts
import cron from 'node-cron'
import { runRetention } from './dataRetentionJob.js'

let retentionRuns = 0

export function startScheduler() {
  // Diario a las 04:00 UTC
  cron.schedule('0 4 * * *', async () => {
    const dryRun = retentionRuns < 2 && process.env.NODE_ENV === 'production'
    try {
      await runRetention({ dryRun })
      retentionRuns++
    } catch (e) {
      console.error('Retention job failed', e)
    }
  })
}
```

- [ ] **8.4: Añadir `node-cron` a deps backend + script `npm run job:retention`**

```bash
cd src/backend && npm install node-cron @types/node-cron
```

En `src/backend/package.json` scripts:
```json
"job:retention": "ts-node src/jobs/runRetention.ts"
```

Crear `src/backend/src/jobs/runRetention.ts`:
```ts
import { runRetention } from './dataRetentionJob.js'
runRetention({ dryRun: process.argv.includes('--dry-run') })
  .then(r => { console.log(r); process.exit(0) })
  .catch(e => { console.error(e); process.exit(1) })
```

- [ ] **8.5: Wire scheduler en `server.ts` solo en producción**

```ts
import { startScheduler } from './jobs/scheduler.js'
if (process.env.NODE_ENV === 'production') startScheduler()
```

- [ ] **8.6: Verde + commit**

```bash
npx jest dataRetentionJob.test.ts
git add src/backend/src/jobs/ src/backend/src/server.ts src/backend/package.json src/backend/package-lock.json
git commit -m "feat(v1.6.1): cron de retención de datos + scheduler"
```

---

## Task 9: Páginas legales + LegalPage + Footer + rutas

- [ ] **9.1: Crear markdowns en `docs/legal/`**

Crear `docs/legal/privacy.md`, `docs/legal/terms.md`, `docs/legal/cookies.md` con plantillas redactadas (versión inicial; el copy final lo aporta el usuario o un legal en pasada posterior, pero la estructura completa entra ya).

`docs/legal/privacy.md`:
```markdown
# Política de Privacidad

Última actualización: 2026-05-02

## Quién es el responsable de tus datos
Matripuntos (eduhand92@gmail.com).

## Qué datos recogemos
- Identificativos: email, nombre, contraseña hasheada.
- Perfil: avatar, mood, preferencias de tareas, horas de trabajo.
- Familiares: nombres y datos de hijos y mascotas (si los registras).
- Uso: actividades, tareas, balance de puntos, frases vistas.
- Telemetría: eventos anónimos de uso (si aceptas).

## Para qué los usamos
- Funcionar la app (base legal: ejecución de contrato).
- Mejorar el producto (base legal: interés legítimo, telemetría anónima).
- Análisis identificado (base legal: consentimiento explícito).

## Sub-procesadores
| Servicio | Finalidad | Ubicación |
|---|---|---|
| Render | Hosting backend | UE |
| Supabase | Base de datos | UE |
| PostHog | Analytics opcional | UE (Frankfurt) |
| Sentry | Errores | UE |

## Retención
Ver `docs/legal/retention.md`. Los datos se conservan mientras tu cuenta esté activa y según los plazos específicos por tipo de dato.

## Tus derechos
Acceso, rectificación, supresión, oposición, portabilidad, limitación. Contacto: eduhand92@gmail.com. Plazo de respuesta: 30 días.

## Borrar tu cuenta
Settings > Privacidad > Eliminar mi cuenta. Tras 30 días en papelera el borrado es definitivo.

## Cambios
Si cambia esta política te avisaremos en la app.
```

`docs/legal/terms.md`: estructura similar — uso aceptable, suspensión, limitación, ley aplicable España.

`docs/legal/cookies.md`: tabla de cookies (esenciales JWT + idioma; analítica PostHog).

- [ ] **9.2: Implementar `LegalPage.tsx`**

```tsx
// src/frontend/src/pages/legal/LegalPage.tsx
import { useEffect, useState } from 'react'
import ReactMarkdown from 'react-markdown'

interface Props { slug: 'privacy' | 'terms' | 'cookies' }

export function LegalPage({ slug }: Props) {
  const [content, setContent] = useState<string>('Cargando...')

  useEffect(() => {
    import(`../../../../docs/legal/${slug}.md?raw`)
      .then(m => setContent(m.default))
      .catch(() => setContent('No se pudo cargar el documento.'))
  }, [slug])

  return (
    <div className="min-h-screen bg-[#0f0a1e] text-white px-4 py-8">
      <div className="max-w-2xl mx-auto prose prose-invert">
        <ReactMarkdown>{content}</ReactMarkdown>
      </div>
    </div>
  )
}
```

> Nota Vite: configurar `vite.config.ts` para resolver imports `?raw` desde `docs/legal/` con `assetsInclude: ['**/*.md']` o ajustar paths. Verificar con un primer build.

- [ ] **9.3: Crear pages wrappers**

`src/frontend/src/pages/legal/Privacy.tsx`:
```tsx
import { LegalPage } from './LegalPage'
export default function Privacy() { return <LegalPage slug="privacy" /> }
```

Idem `Terms.tsx`, `Cookies.tsx`.

- [ ] **9.4: Añadir rutas en App.tsx (router)**

```tsx
import Privacy from './pages/legal/Privacy'
import Terms from './pages/legal/Terms'
import Cookies from './pages/legal/Cookies'
// dentro del Routes:
<Route path="/privacy" element={<Privacy />} />
<Route path="/terms" element={<Terms />} />
<Route path="/cookies" element={<Cookies />} />
```

- [ ] **9.5: Implementar `Footer.tsx`**

```tsx
// src/frontend/src/components/Footer.tsx
import { Link } from 'react-router-dom'

export function Footer() {
  return (
    <footer className="text-center text-xs text-white/40 py-4 space-x-3" data-testid="global-footer">
      <Link to="/privacy" className="hover:text-white/70">Privacidad</Link>
      <span>·</span>
      <Link to="/terms" className="hover:text-white/70">Términos</Link>
      <span>·</span>
      <Link to="/cookies" className="hover:text-white/70">Cookies</Link>
    </footer>
  )
}
```

- [ ] **9.6: Test del footer**

```tsx
// src/frontend/src/components/Footer.test.tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { Footer } from './Footer'

describe('Footer', () => {
  it('renders 3 legal links', () => {
    render(<MemoryRouter><Footer /></MemoryRouter>)
    expect(screen.getByText('Privacidad')).toBeInTheDocument()
    expect(screen.getByText('Términos')).toBeInTheDocument()
    expect(screen.getByText('Cookies')).toBeInTheDocument()
  })
})
```

- [ ] **9.7: Montar Footer en main layout** (Dashboard / pages autenticadas + login)

Editar `src/frontend/src/App.tsx` o el componente layout principal y añadir `<Footer />` al final.

- [ ] **9.8: Instalar react-markdown si no está**

```bash
cd src/frontend && npm install react-markdown
```

- [ ] **9.9: Smoke + commit**

```bash
npm run build --workspace=src/frontend
npm run dev  # manual: visitar /privacy, /terms, /cookies
git add docs/legal/ src/frontend/src/pages/legal/ src/frontend/src/components/Footer.tsx src/frontend/src/components/Footer.test.tsx src/frontend/src/App.tsx src/frontend/package.json src/frontend/package-lock.json
git commit -m "feat(v1.6.1): páginas legales + Footer + rutas"
```

---

## Task 10: Documentación legal completa (RAT + DPAs + ARCO-POL + retención)

- [ ] **10.1: `docs/legal/rat.md` (Registro de Actividades de Tratamiento)**

Crear con plantilla AEPD adaptada: 7 secciones (responsable, fines, base legal, categorías de interesados, categorías de datos, destinatarios, plazos, transferencias internacionales, medidas técnicas/organizativas).

- [ ] **10.2: `docs/legal/dpa-checklist.md`**

```markdown
# Checklist DPAs sub-procesadores

| Servicio | DPA firmado | Fecha | Link |
|---|---|---|---|
| Render | ⏳ Pendiente | — | https://render.com/legal/dpa |
| Supabase | ⏳ Pendiente | — | https://supabase.com/dpa |
| PostHog | ⏳ Pendiente | — | https://posthog.com/dpa |
| Sentry | ⏳ Pendiente | — | https://sentry.io/legal/dpa/ |
| FTP host (keepitup.io) | ⏳ Revisar contrato | — | — |

**Acción usuario:** firmar los 4 DPAs auto-servicio + revisar contrato FTP.
```

- [ ] **10.3: `docs/legal/arco-pol-procedure.md`**

Procedimiento de respuesta a derechos en <30 días: email dedicado, template, log.

- [ ] **10.4: `docs/legal/retention.md`** (tabla del §4.6 del spec)

- [ ] **10.5: `docs/legal/posthog-dashboard.md`** (link al dashboard, lista de KPIs)

- [ ] **10.6: `docs/legal/scaling-notes.md`** (notas para futuro: Redis multi-instancia, SMTP transaccional)

- [ ] **10.7: Commit**

```bash
git add docs/legal/
git commit -m "docs(v1.6.1): RAT + DPAs + ARCO-POL + retención + dashboard + scaling notes"
```

---

## Task 11: Servicio `consent` + hook + tests

- [ ] **11.1: Test failing**

```ts
// src/frontend/src/services/consent.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import { readConsent, writeConsent, CONSENT_COOKIE_NAME } from './consent'

beforeEach(() => { document.cookie = `${CONSENT_COOKIE_NAME}=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/` })

describe('consent service', () => {
  it('readConsent returns null cuando no hay cookie', () => {
    expect(readConsent()).toBeNull()
  })

  it('writeConsent + readConsent roundtrip', () => {
    writeConsent({ analytics: true })
    const r = readConsent()
    expect(r?.analytics).toBe(true)
    expect(r?.version).toBe(1)
    expect(r?.acceptedAt).toBeTruthy()
  })

  it('writeConsent analytics:false persiste', () => {
    writeConsent({ analytics: false })
    expect(readConsent()?.analytics).toBe(false)
  })
})
```

- [ ] **11.2: Implementar `consent.ts`**

```ts
// src/frontend/src/services/consent.ts
export const CONSENT_COOKIE_NAME = 'mp_consent_v1'
export const CONSENT_TTL_DAYS = 365

export interface Consent { analytics: boolean; version: 1; acceptedAt: string }

export function readConsent(): Consent | null {
  const m = document.cookie.match(new RegExp(`${CONSENT_COOKIE_NAME}=([^;]+)`))
  if (!m) return null
  try {
    const parsed = JSON.parse(decodeURIComponent(m[1]))
    if (parsed.version === 1) return parsed
  } catch {}
  return null
}

export function writeConsent(partial: { analytics: boolean }): void {
  const data: Consent = { analytics: partial.analytics, version: 1, acceptedAt: new Date().toISOString() }
  const exp = new Date(Date.now() + CONSENT_TTL_DAYS * 86400000).toUTCString()
  document.cookie = `${CONSENT_COOKIE_NAME}=${encodeURIComponent(JSON.stringify(data))}; expires=${exp}; path=/; SameSite=Lax`
}

export function clearConsent(): void {
  document.cookie = `${CONSENT_COOKIE_NAME}=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/`
}
```

- [ ] **11.3: Hook `useConsent`**

```ts
// src/frontend/src/hooks/useConsent.ts
import { useEffect, useState } from 'react'
import { readConsent, writeConsent, type Consent } from '../services/consent'

export function useConsent() {
  const [consent, setConsentState] = useState<Consent | null>(() => readConsent())

  const setConsent = (partial: { analytics: boolean }) => {
    writeConsent(partial)
    setConsentState(readConsent())
  }

  useEffect(() => {
    const onStorage = () => setConsentState(readConsent())
    window.addEventListener('storage', onStorage)
    return () => window.removeEventListener('storage', onStorage)
  }, [])

  return { consent, setConsent, hasResponded: consent !== null }
}
```

- [ ] **11.4: Verde + commit**

```bash
npx vitest run src/services/consent.test.ts
git add src/frontend/src/services/consent.ts src/frontend/src/services/consent.test.ts src/frontend/src/hooks/useConsent.ts
git commit -m "feat(v1.6.1): consent service + useConsent hook"
```

---

## Task 12: `CookieConsentBanner` + tests

- [ ] **12.1: Test failing**

```tsx
// src/frontend/src/components/CookieConsentBanner.test.tsx
import { describe, it, expect, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { CookieConsentBanner } from './CookieConsentBanner'
import { CONSENT_COOKIE_NAME } from '../services/consent'

beforeEach(() => { document.cookie = `${CONSENT_COOKIE_NAME}=; expires=Thu, 01 Jan 1970 00:00:00 GMT; path=/` })

describe('CookieConsentBanner', () => {
  it('aparece si no hay consent', () => {
    render(<CookieConsentBanner />)
    expect(screen.getByTestId('cookie-banner')).toBeInTheDocument()
  })

  it('no aparece tras Aceptar todo', () => {
    const { rerender } = render(<CookieConsentBanner />)
    fireEvent.click(screen.getByTestId('btn-accept-all'))
    rerender(<CookieConsentBanner />)
    expect(screen.queryByTestId('cookie-banner')).toBeNull()
  })

  it('Solo esenciales persiste analytics:false', () => {
    render(<CookieConsentBanner />)
    fireEvent.click(screen.getByTestId('btn-only-essentials'))
    expect(document.cookie).toMatch(/"analytics":false/)
  })

  it('Personalizar abre modal con toggles', () => {
    render(<CookieConsentBanner />)
    fireEvent.click(screen.getByTestId('btn-customize'))
    expect(screen.getByTestId('toggle-analytics')).toBeInTheDocument()
  })
})
```

- [ ] **12.2: Implementar `CookieConsentBanner.tsx`**

```tsx
// src/frontend/src/components/CookieConsentBanner.tsx
import { useState } from 'react'
import { useConsent } from '../hooks/useConsent'

export function CookieConsentBanner() {
  const { consent, setConsent } = useConsent()
  const [showCustom, setShowCustom] = useState(false)
  const [analytics, setAnalytics] = useState(true)

  if (consent) return null

  const accept = (a: boolean) => setConsent({ analytics: a })

  return (
    <div data-testid="cookie-banner" className="fixed bottom-0 left-0 right-0 z-40 bg-[#1a1035] border-t border-white/10 p-4 shadow-2xl">
      {!showCustom && (
        <div className="max-w-3xl mx-auto flex flex-col md:flex-row items-center gap-3">
          <p className="text-sm text-white/90 flex-1">
            Usamos cookies esenciales para que la app funcione. Las analíticas son opcionales y nos ayudan a mejorarla.{' '}
            <a href="/cookies" className="underline">Más info</a>
          </p>
          <div className="flex gap-2 flex-wrap justify-end">
            <button data-testid="btn-only-essentials" onClick={() => accept(false)} className="px-3 py-1.5 rounded-lg bg-white/10 text-white text-sm">Solo esenciales</button>
            <button data-testid="btn-customize" onClick={() => setShowCustom(true)} className="px-3 py-1.5 rounded-lg bg-white/10 text-white text-sm">Personalizar</button>
            <button data-testid="btn-accept-all" onClick={() => accept(true)} className="px-3 py-1.5 rounded-lg bg-amber-500 text-black text-sm font-medium">Aceptar todo</button>
          </div>
        </div>
      )}
      {showCustom && (
        <div className="max-w-md mx-auto">
          <h3 className="text-base font-semibold text-white mb-3">Personalizar cookies</h3>
          <div className="space-y-2 mb-4">
            <label className="flex items-center justify-between gap-3 text-sm">
              <span className="text-white/90">Esenciales <span className="text-white/40 text-xs">(necesarias)</span></span>
              <input type="checkbox" checked disabled />
            </label>
            <label className="flex items-center justify-between gap-3 text-sm">
              <span className="text-white/90">Analíticas (PostHog)</span>
              <input data-testid="toggle-analytics" type="checkbox" checked={analytics} onChange={e => setAnalytics(e.target.checked)} />
            </label>
          </div>
          <div className="flex gap-2 justify-end">
            <button onClick={() => setShowCustom(false)} className="px-3 py-1.5 rounded-lg bg-white/10 text-white text-sm">Volver</button>
            <button onClick={() => accept(analytics)} className="px-3 py-1.5 rounded-lg bg-amber-500 text-black text-sm font-medium">Guardar</button>
          </div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **12.3: Montar en `main.tsx`**

```tsx
import { CookieConsentBanner } from './components/CookieConsentBanner'
// dentro del root render, junto al RouterProvider:
<>
  <RouterProvider router={router} />
  <CookieConsentBanner />
</>
```

- [ ] **12.4: Verde + commit**

```bash
npx vitest run src/components/CookieConsentBanner.test.tsx
git add src/frontend/src/components/CookieConsentBanner.tsx src/frontend/src/components/CookieConsentBanner.test.tsx src/frontend/src/main.tsx
git commit -m "feat(v1.6.1): CookieConsentBanner con 3 acciones + modal personalizar"
```

---

## Task 13: Catálogo de eventos de telemetría en shared

- [ ] **13.1: Implementar `packages/shared/src/telemetry-events.ts`**

```ts
import { z } from 'zod'
import { MOOD_KEYS } from './schemas/profile'

const phraseCategoryEnum = z.enum([
  'reconciliacion','animo','celebrar','agradecer','calma','animo-suave','hito','neutra-positivo',
])

export const TELEMETRY_EVENT_SCHEMAS = {
  'auth.signup_completed':           z.object({ method: z.enum(['email','joinCode']), timeToCompleteMs: z.number() }),
  'auth.login_success':              z.object({}),
  'auth.logout':                     z.object({}),
  'onboarding.step_completed':       z.object({ step: z.number().int().min(1).max(6), role: z.enum(['creator','invitee']) }),
  'onboarding.invitee_skipped_step': z.object({ step: z.number().int() }),
  'onboarding.invitee_banner_dismissed': z.object({}),
  'mood.set':                        z.object({ moodKey: z.enum(MOOD_KEYS), source: z.enum(['header','card','nudge','settings']) }),
  'mood.cleared':                    z.object({}),
  'mood.expired_seen':               z.object({}),
  'phrase.daily_seen':               z.object({ category: phraseCategoryEnum }),
  'avatar.changed':                  z.object({ from: z.string(), to: z.string(), colorChanged: z.boolean() }),
  'activity.created':                z.object({ type: z.string(), hasCompensation: z.boolean(), pointsBase: z.number() }),
  'activity.responded':              z.object({ response: z.enum(['accepted','rejected','counter','forced']), round: z.number().int() }),
  'activity.force_paid':             z.object({ amount: z.number() }),
  'task.log_created':                z.object({ category: z.string() }),
  'task.log_disputed':               z.object({}),
  'task.log_auto_verified':          z.object({}),
  'couple.left':                     z.object({}),
  'account.deleted':                 z.object({}),
  'consent.changed':                 z.object({ analytics: z.boolean() }),
  'ratelimit.hit':                   z.object({ endpoint: z.string(), bucket: z.string() }),
} as const

export type TelemetryEventName = keyof typeof TELEMETRY_EVENT_SCHEMAS
export type TelemetryEventProps<E extends TelemetryEventName> = z.infer<typeof TELEMETRY_EVENT_SCHEMAS[E]>

export function validateTelemetryEvent<E extends TelemetryEventName>(name: E, props: unknown):
  { ok: true; data: TelemetryEventProps<E> } | { ok: false; error: z.ZodError } {
  const schema = TELEMETRY_EVENT_SCHEMAS[name]
  const r = schema.safeParse(props)
  if (r.success) return { ok: true, data: r.data as any }
  return { ok: false, error: r.error }
}
```

- [ ] **13.2: Test del catálogo**

```ts
// packages/shared/src/telemetry-events.test.ts
import { describe, it, expect } from 'vitest'
import { TELEMETRY_EVENT_SCHEMAS, validateTelemetryEvent } from './telemetry-events'

describe('telemetry events catalog', () => {
  it('contains expected event names', () => {
    expect(Object.keys(TELEMETRY_EVENT_SCHEMAS)).toContain('mood.set')
    expect(Object.keys(TELEMETRY_EVENT_SCHEMAS)).toContain('account.deleted')
  })

  it('validateTelemetryEvent rejects bad mood.set props', () => {
    const r = validateTelemetryEvent('mood.set', { moodKey: 'nonexistent', source: 'header' })
    expect(r.ok).toBe(false)
  })

  it('validateTelemetryEvent accepts valid mood.set', () => {
    const r = validateTelemetryEvent('mood.set', { moodKey: 'feliz', source: 'header' })
    expect(r.ok).toBe(true)
  })
})
```

- [ ] **13.3: Verde + commit**

```bash
cd packages/shared && npx vitest run src/telemetry-events.test.ts
git add packages/shared/src/telemetry-events.ts packages/shared/src/telemetry-events.test.ts
git commit -m "feat(v1.6.1): catálogo cerrado de eventos de telemetría con zod"
```

---

## Task 14: Frontend telemetry wrapper + tests + integración

- [ ] **14.1: Test del wrapper**

```ts
// src/frontend/src/services/telemetry.test.ts
import { describe, it, expect, vi } from 'vitest'
import { telemetry, sanitizeProps } from './telemetry'

describe('telemetry sanitize', () => {
  it('strip blacklisted keys', () => {
    const r = sanitizeProps({ moodKey: 'feliz', email: 'me@x.com', password: 'p', name: 'X' })
    expect(r.moodKey).toBe('feliz')
    expect(r.email).toBeUndefined()
    expect(r.password).toBeUndefined()
    expect(r.name).toBeUndefined()
  })
})
```

- [ ] **14.2: Implementar `telemetry.ts`**

```ts
// src/frontend/src/services/telemetry.ts
import posthog from 'posthog-js'
import { validateTelemetryEvent, type TelemetryEventName, type TelemetryEventProps } from '@matripuntos/shared'

const PII_BLACKLIST = ['email','password','passwordHash','name','surname','secretKey','joinCode','text','message','notes']

export function sanitizeProps(props?: Record<string, any>): Record<string, any> {
  if (!props) return {}
  const out: Record<string, any> = {}
  for (const [k, v] of Object.entries(props)) {
    if (PII_BLACKLIST.includes(k)) continue
    out[k] = v
  }
  return out
}

export const telemetry = {
  init(consent: boolean) {
    const key = import.meta.env.VITE_POSTHOG_KEY
    const host = import.meta.env.VITE_POSTHOG_HOST ?? 'https://eu.posthog.com'
    if (!key) return
    posthog.init(key, {
      api_host: host,
      opt_out_capturing_by_default: !consent,
      persistence: 'localStorage+cookie',
      autocapture: false,
      capture_pageview: false,
    })
  },
  identify(userId: string, traits?: Record<string, any>) {
    posthog.identify(userId, sanitizeProps(traits))
  },
  track<E extends TelemetryEventName>(event: E, props?: TelemetryEventProps<E>) {
    const r = validateTelemetryEvent(event, props ?? {})
    if (!r.ok) {
      if (import.meta.env.DEV) console.warn(`[telemetry] event ${event} schema fail`, r.error.issues)
      return
    }
    posthog.capture(event, sanitizeProps(r.data as any))
  },
  optIn() { posthog.opt_in_capturing() },
  optOut() { posthog.opt_out_capturing() },
  reset() { posthog.reset() },
}
```

- [ ] **14.3: Instalar `posthog-js`**

```bash
cd src/frontend && npm install posthog-js
```

- [ ] **14.4: Wire en `main.tsx`**

```tsx
import { telemetry } from './services/telemetry'
import { readConsent } from './services/consent'

const c = readConsent()
telemetry.init(c?.analytics ?? false)
```

- [ ] **14.5: Verde + commit**

```bash
npx vitest run src/services/telemetry.test.ts
git add src/frontend/src/services/telemetry.ts src/frontend/src/services/telemetry.test.ts src/frontend/src/main.tsx src/frontend/package.json src/frontend/package-lock.json
git commit -m "feat(v1.6.1): frontend telemetry wrapper + sanitize + init"
```

---

## Task 15: Backend telemetry wrapper + tests + integración

- [ ] **15.1: Implementar `src/backend/src/services/telemetry.ts`**

```ts
import { PostHog } from 'posthog-node'
import { validateTelemetryEvent, type TelemetryEventName, type TelemetryEventProps } from '@matripuntos/shared'

const PII_BLACKLIST = ['email','password','passwordHash','name','surname','secretKey','joinCode','text','message','notes']

function sanitize(props?: Record<string, any>): Record<string, any> {
  if (!props) return {}
  const out: Record<string, any> = {}
  for (const [k, v] of Object.entries(props)) if (!PII_BLACKLIST.includes(k)) out[k] = v
  return out
}

const key = process.env.POSTHOG_KEY
const host = process.env.POSTHOG_HOST ?? 'https://eu.posthog.com'
const client = key ? new PostHog(key, { host }) : null

export const telemetryBackend = {
  track<E extends TelemetryEventName>(distinctId: string, event: E, props?: TelemetryEventProps<E>) {
    if (!client) return
    const r = validateTelemetryEvent(event, props ?? {})
    if (!r.ok) {
      console.warn(`[telemetry-backend] ${event} schema fail`, r.error.issues)
      return
    }
    client.capture({ distinctId, event, properties: sanitize(r.data as any) })
  },
  async shutdown() { await client?.shutdownAsync() },
}
```

- [ ] **15.2: Instalar `posthog-node`**

```bash
cd src/backend && npm install posthog-node
```

- [ ] **15.3: Disparar shutdown en server shutdown signal**

En `server.ts`:
```ts
process.on('SIGTERM', async () => { await telemetryBackend.shutdown(); process.exit(0) })
```

- [ ] **15.4: Commit**

```bash
git add src/backend/src/services/telemetry.ts src/backend/src/server.ts src/backend/package.json src/backend/package-lock.json
git commit -m "feat(v1.6.1): backend telemetry wrapper"
```

---

## Task 16: Integrar telemetría en routes/components clave

- [ ] **16.1: Backend — disparar eventos clave**

En `authRoutes.ts`:
```ts
import { telemetryBackend } from '../services/telemetry.js'
// tras crear user en register:
telemetryBackend.track(user.id, 'auth.signup_completed', { method: hasJoinCode ? 'joinCode' : 'email', timeToCompleteMs: 0 })
// tras login OK:
telemetryBackend.track(user.id, 'auth.login_success', {})
// también: set firstLoginAt si null
if (!user.firstLoginAt) await prisma.user.update({ where: { id: user.id }, data: { firstLoginAt: new Date() } })
```

En `account.ts` tras `deleteAccount(userId)`:
```ts
telemetryBackend.track(userId, 'account.deleted', {})
```

En `couple.ts` tras `dissolveCouple`:
```ts
telemetryBackend.track(userId, 'couple.left', {})
```

- [ ] **16.2: Frontend — disparar eventos clave**

En `MoodSelectorSheet`:
```ts
telemetry.track('mood.set', { moodKey: key, source: 'sheet' })
```

En `DailyPhrase`:
```ts
useEffect(() => { telemetry.track('phrase.daily_seen', { category: phrase.category }) }, [phrase.id])
```

En `AvatarPicker.onChange`:
```ts
telemetry.track('avatar.changed', { from: oldEmoji, to: newEmoji, colorChanged: oldColor !== newColor })
```

En `CookieConsentBanner` accept:
```ts
telemetry.track('consent.changed', { analytics: a })
```

- [ ] **16.3: Smoke + commit**

```bash
npm run build --workspaces
git add src/backend/src/routes/ src/frontend/src/components/
git commit -m "feat(v1.6.1): integrar telemetría en routes y componentes clave"
```

---

## Task 17: Onboarding del invitado — 2 pasos extra

- [ ] **17.1: Implementar `StepInviteeAvatar.tsx`**

```tsx
// src/frontend/src/components/onboarding/StepInviteeAvatar.tsx
import { useState } from 'react'
import { AvatarPicker } from '../v2/primitives/AvatarPicker'

interface Props { onContinue: (data: { emoji: string; color: string }) => void }

export function StepInviteeAvatar({ onContinue }: Props) {
  const [emoji, setEmoji] = useState('🦊')
  const [color, setColor] = useState('#7c3aed')

  return (
    <div className="space-y-6 max-w-md mx-auto p-4">
      <h2 className="text-2xl font-bold text-white">Tu avatar</h2>
      <AvatarPicker emoji={emoji} color={color} onChange={({ emoji: e, color: c }) => { setEmoji(e); setColor(c) }} />
      <button data-testid="btn-invitee-avatar-continue" onClick={() => onContinue({ emoji, color })}
        className="w-full py-3 rounded-xl bg-amber-500 text-black font-medium">
        Continuar
      </button>
    </div>
  )
}
```

- [ ] **17.2: Implementar `StepInviteeWork.tsx`**

```tsx
// src/frontend/src/components/onboarding/StepInviteeWork.tsx
import { useState } from 'react'

interface Props { onContinue: (data: { weeklyWorkHours: number; workMode: string }) => void; onSkip: () => void }

export function StepInviteeWork({ onContinue, onSkip }: Props) {
  const [hours, setHours] = useState(40)
  const [mode, setMode] = useState<'presencial'|'remoto'|'hibrido'>('presencial')

  return (
    <div className="space-y-6 max-w-md mx-auto p-4">
      <h2 className="text-2xl font-bold text-white">¿Cómo trabajas?</h2>
      <label className="block">
        <span className="text-sm text-white/70">Horas semanales: {hours}h</span>
        <input type="range" min={0} max={80} value={hours} onChange={e => setHours(Number(e.target.value))} className="w-full" />
      </label>
      <div className="grid grid-cols-3 gap-2">
        {(['presencial','remoto','hibrido'] as const).map(m => (
          <button key={m} onClick={() => setMode(m)}
            className={`py-2 rounded-lg ${m === mode ? 'bg-amber-500 text-black' : 'bg-white/10 text-white'}`}>
            {m}
          </button>
        ))}
      </div>
      <div className="flex gap-2">
        <button onClick={onSkip} className="flex-1 py-3 rounded-xl bg-white/10 text-white">Saltar</button>
        <button data-testid="btn-invitee-work-continue" onClick={() => onContinue({ weeklyWorkHours: hours, workMode: mode })}
          className="flex-1 py-3 rounded-xl bg-amber-500 text-black font-medium">
          Continuar
        </button>
      </div>
    </div>
  )
}
```

- [ ] **17.3: Encadenar tras `StepJoinAccount`**

Modificar el flujo del invitado: tras `StepJoinAccount` exitoso, **NO** marcar `hasCompletedOnboarding = true` aún. En lugar, navegar a `/onboarding/invitee/avatar`. Tras avatar → `/onboarding/invitee/work`. Tras work (o saltar) → `PUT /api/profile/me { hasCompletedOnboarding: true, avatarEmoji, avatarColor, weeklyWorkHours, workMode }` y `navigate('/dashboard')`.

Concretamente:
- Crear `OnboardingInviteeFlow.tsx` que orquesta los 2 pasos en estado interno.
- Rutas en App.tsx: `/onboarding/invitee/*`.

- [ ] **17.4: Test del flow invitee**

```tsx
// src/frontend/src/components/onboarding/StepInviteeAvatar.test.tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { StepInviteeAvatar } from './StepInviteeAvatar'

describe('StepInviteeAvatar', () => {
  it('llama onContinue con emoji y color', () => {
    const onContinue = vi.fn()
    render(<StepInviteeAvatar onContinue={onContinue} />)
    fireEvent.click(screen.getByTestId('btn-invitee-avatar-continue'))
    expect(onContinue).toHaveBeenCalledWith(expect.objectContaining({ emoji: expect.any(String), color: expect.any(String) }))
  })
})
```

- [ ] **17.5: Verde + commit**

```bash
npx vitest run src/components/onboarding/StepInviteeAvatar.test.tsx
git add src/frontend/src/components/onboarding/ src/frontend/src/App.tsx
git commit -m "feat(v1.6.1): onboarding invitee 2 pasos extra (avatar + work)"
```

---

## Task 18: ProfileCompletionBanner + utility + endpoint

- [ ] **18.1: Test util**

```ts
// src/frontend/src/utils/profileCompletion.test.ts
import { describe, it, expect } from 'vitest'
import { calcCompletion } from './profileCompletion'

describe('calcCompletion', () => {
  it('completo = 100', () => {
    expect(calcCompletion({
      avatarEmoji: '🦊', avatarColor: '#fff', surname: 'Doe', workMode: 'remoto',
      weeklyWorkHours: 40, taskPreferencesLoves: ['cocina'], taskPreferencesDislikes: ['baño'],
      profilePhotoUrl: 'http://x',
    })).toBe(100)
  })

  it('vacío = 0', () => {
    expect(calcCompletion({})).toBe(0)
  })

  it('avatar+surname = 20', () => {
    expect(calcCompletion({ avatarEmoji: '🦊', avatarColor: '#fff', surname: 'Doe' })).toBe(20)
  })
})
```

- [ ] **18.2: Implementar `profileCompletion.ts`**

```ts
// src/frontend/src/utils/profileCompletion.ts
export interface ProfileFields {
  avatarEmoji?: string | null
  avatarColor?: string | null
  surname?: string | null
  workMode?: string | null
  weeklyWorkHours?: number | null
  taskPreferencesLoves?: string[] | null
  taskPreferencesDislikes?: string[] | null
  profilePhotoUrl?: string | null
}

export function calcCompletion(p: ProfileFields): number {
  let pct = 0
  if (p.avatarEmoji && p.avatarColor) pct += 10
  if (p.surname && p.surname.length > 0) pct += 10
  if (p.workMode) pct += 15
  if (p.weeklyWorkHours !== null && p.weeklyWorkHours !== undefined) pct += 15
  if (p.taskPreferencesLoves && p.taskPreferencesLoves.length > 0) pct += 20
  if (p.taskPreferencesDislikes && p.taskPreferencesDislikes.length > 0) pct += 20
  if (p.profilePhotoUrl) pct += 10
  return pct
}

export function listMissing(p: ProfileFields): string[] {
  const m: string[] = []
  if (!p.avatarEmoji || !p.avatarColor) m.push('avatar')
  if (!p.surname) m.push('surname')
  if (!p.workMode) m.push('workMode')
  if (p.weeklyWorkHours === null || p.weeklyWorkHours === undefined) m.push('weeklyWorkHours')
  if (!p.taskPreferencesLoves || p.taskPreferencesLoves.length === 0) m.push('taskPreferencesLoves')
  if (!p.taskPreferencesDislikes || p.taskPreferencesDislikes.length === 0) m.push('taskPreferencesDislikes')
  if (!p.profilePhotoUrl) m.push('profilePhotoUrl')
  return m
}
```

- [ ] **18.3: Endpoint backend `GET /api/profile/completion`**

Crear `src/backend/src/routes/profileCompletion.ts`:
```ts
import { Router } from 'express'
import { authenticateToken } from '../middleware/auth.js'
import prisma from '../lib/prisma.js'

const router = Router()
router.use(authenticateToken)

router.get('/completion', async (req, res) => {
  const userId = (req as any).user.id
  const user = await prisma.user.findUnique({ where: { id: userId }, include: { profile: true } })
  if (!user) return res.status(404).json({ error: 'No encontrado' })

  const profile = user.profile
  const fields = {
    avatarEmoji: user.avatarEmoji, avatarColor: user.avatarColor,
    surname: profile?.surname, workMode: profile?.workMode,
    weeklyWorkHours: profile?.weeklyWorkHours,
    taskPreferencesLoves: profile?.taskPreferencesLoves ? JSON.parse(profile.taskPreferencesLoves) : [],
    taskPreferencesDislikes: profile?.taskPreferencesDislikes ? JSON.parse(profile.taskPreferencesDislikes) : [],
    profilePhotoUrl: profile?.profilePhotoUrl,
  }

  const calc = await import('../utils/profileCompletion.js').then(m => m.calcCompletion(fields))  // si lo movemos a backend; alternativa duplicar
  const missing = await import('../utils/profileCompletion.js').then(m => m.listMissing(fields))
  res.json({ percent: calc, missing })
})

export default router
```

> Nota: la utilidad `profileCompletion.ts` puede vivir también en `packages/shared/src/utils/` para no duplicar entre frontend y backend. Si optas por shared, mover allí y ajustar imports.

- [ ] **18.4: Implementar `ProfileCompletionBanner.tsx`**

```tsx
// src/frontend/src/components/v2/dashboard/ProfileCompletionBanner.tsx
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import apiClient from '../../../services/apiClient'

interface Props { firstLoginAt: string | null | undefined }

export function ProfileCompletionBanner({ firstLoginAt }: Props) {
  const [data, setData] = useState<{ percent: number; missing: string[] } | null>(null)
  const [dismissed, setDismissed] = useState(() => !!localStorage.getItem('matripuntos_profile_banner_dismissed_at'))
  const navigate = useNavigate()

  useEffect(() => {
    apiClient.get('/profile/completion').then(r => setData(r.data)).catch(() => {})
  }, [])

  if (dismissed || !data || data.percent >= 80) return null
  if (!firstLoginAt) return null
  const ageDays = (Date.now() - new Date(firstLoginAt).getTime()) / 86400000
  if (ageDays > 7) return null

  const handleDismiss = () => {
    localStorage.setItem('matripuntos_profile_banner_dismissed_at', new Date().toISOString())
    setDismissed(true)
  }

  return (
    <div data-testid="profile-completion-banner" className="rounded-xl bg-amber-500/15 border border-amber-500/30 p-3 flex items-center gap-3">
      <span className="text-amber-100 text-sm flex-1">
        Tu perfil está al {data.percent}% — completar mejora la app para ti.
      </span>
      <button onClick={() => navigate(`/settings#${data.missing[0] ?? ''}`)} className="text-sm text-amber-100 underline">
        Completar →
      </button>
      <button data-testid="btn-banner-dismiss" onClick={handleDismiss} className="text-amber-100/60 hover:text-amber-100 text-xl px-2">×</button>
    </div>
  )
}
```

- [ ] **18.5: Tests + commit**

```bash
npx vitest run src/utils/profileCompletion.test.ts
git add src/frontend/src/utils/profileCompletion.ts src/frontend/src/utils/profileCompletion.test.ts src/frontend/src/components/v2/dashboard/ProfileCompletionBanner.tsx src/backend/src/routes/profileCompletion.ts src/backend/src/server.ts
git commit -m "feat(v1.6.1): profile completion endpoint + util + banner"
```

---

## Task 19: Rate-limiter middleware

- [ ] **19.1: Test failing**

```ts
// src/backend/tests/rateLimit.test.ts
import request from 'supertest'
import app from '../src/server'

describe('rate limiting', () => {
  it('11º login en <1min → 429', async () => {
    for (let i = 0; i < 10; i++) {
      await request(app).post('/api/auth/login').send({ email: 'x@y.z', password: 'wrong' })
    }
    const res = await request(app).post('/api/auth/login').send({ email: 'x@y.z', password: 'wrong' })
    expect(res.status).toBe(429)
  })
})
```

- [ ] **19.2: Implementar `rateLimiter.ts`**

```ts
// src/backend/src/middleware/rateLimiter.ts
import rateLimit from 'express-rate-limit'
import type { Request } from 'express'
import { telemetryBackend } from '../services/telemetry.js'

const keyByUserOrIp = (req: Request) => (req as any).user?.id ?? req.ip

const trackHit = (bucket: string) => (req: Request, res: any) => {
  telemetryBackend.track((req as any).user?.id ?? 'anon', 'ratelimit.hit', { endpoint: req.path, bucket })
  res.status(429).json({ error: bucket === 'auth' ? 'Demasiados intentos, prueba en un minuto' : 'Demasiadas peticiones, prueba en un minuto' })
}

export const authBucket = rateLimit({
  windowMs: 60_000, max: 10, keyGenerator: req => req.ip,
  handler: trackHit('auth'),
  standardHeaders: true, legacyHeaders: false,
})

export const writeBucket = rateLimit({
  windowMs: 60_000, max: 60, keyGenerator: keyByUserOrIp,
  handler: trackHit('write'),
})

export const readBucket = rateLimit({
  windowMs: 60_000, max: 200, keyGenerator: keyByUserOrIp,
  handler: trackHit('read'),
})

export const profileMutationBucket = rateLimit({
  windowMs: 60_000, max: 30, keyGenerator: keyByUserOrIp,
  handler: trackHit('profile'),
})

export const criticalBucket = rateLimit({
  windowMs: 3_600_000, max: 3, keyGenerator: keyByUserOrIp,
  handler: trackHit('critical'),
})
```

- [ ] **19.3: Aplicar a routes**

En `authRoutes.ts`:
```ts
import { authBucket } from '../middleware/rateLimiter.js'
router.post('/login', authBucket, async (req, res) => { ... })
router.post('/register', authBucket, ...)
router.post('/join-couple', authBucket, ...)
```

En `profile.ts`:
```ts
import { profileMutationBucket } from '../middleware/rateLimiter.js'
router.put('/me', profileMutationBucket, ...)
```

En `eventRoutes.ts`, `taskRoutes.ts`:
```ts
import { writeBucket, readBucket } from '../middleware/rateLimiter.js'
router.use(writeBucket)  // o por endpoint específico
```

- [ ] **19.4: Verde + commit**

```bash
npx jest tests/rateLimit.test.ts
git add src/backend/src/middleware/rateLimiter.ts src/backend/src/routes/ src/backend/tests/rateLimit.test.ts
git commit -m "feat(v1.6.1): rate-limit granular por endpoint + user"
```

---

## Task 20: Contract tests por endpoint

- [ ] **20.1: Crear los 6 archivos restantes** (account y couple ya están en Task 5/7):
- `src/backend/tests/contracts/auth.contract.test.ts`
- `src/backend/tests/contracts/profile.contract.test.ts`
- `src/backend/tests/contracts/events.contract.test.ts`
- `src/backend/tests/contracts/tasks.contract.test.ts`
- `src/backend/tests/contracts/notifications.contract.test.ts`
- `src/backend/tests/contracts/points.contract.test.ts`

Cada uno sigue el patrón:
```ts
import request from 'supertest'
import app from '../../src/server'
import { loginSchema, authResponseSchema } from '@matripuntos/shared'

describe('contract /api/auth/login', () => {
  it('payload válido + response valida contra authResponseSchema', async () => {
    // setup user...
    const res = await request(app).post('/api/auth/login').send({ email: 'x@y.z', password: 'pwd' })
    expect(res.status).toBe(200)
    expect(authResponseSchema.safeParse(res.body).success).toBe(true)
  })
  it('payload inválido → 400 con error shape', async () => {
    const res = await request(app).post('/api/auth/login').send({ email: 'no-email' })
    expect(res.status).toBe(400)
    expect(res.body).toHaveProperty('error')
  })
})
```

Repetir para cada endpoint priorizado en el spec §8.3. Un test happy + un test invalid por endpoint.

- [ ] **20.2: Frontend espejo**

```ts
// src/frontend/src/services/__tests__/contracts.test.ts
import { describe, it, expect } from 'vitest'
import { loginSchema, authResponseSchema } from '@matripuntos/shared'

describe('frontend mocks pasan schemas shared', () => {
  it('mock login payload', () => {
    const mock = { email: 'me@x.com', password: 'pwd1234' }
    expect(loginSchema.safeParse(mock).success).toBe(true)
  })
  it('mock login response', () => {
    const mock = { token: 't', user: { id: 'u', email: 'a@b.com', name: 'X', coupleId: 'c', hasCompletedOnboarding: true }, couple: { id: 'c', secretKey: 's' } }
    expect(authResponseSchema.safeParse(mock).success).toBe(true)
  })
})
```

- [ ] **20.3: Verde + commit**

```bash
npx jest tests/contracts/
npx vitest run src/services/__tests__/contracts.test.ts
git add src/backend/tests/contracts/ src/frontend/src/services/__tests__/contracts.test.ts
git commit -m "test(v1.6.1): contract tests back↔front (8 endpoints)"
```

---

## Task 21: Playwright setup + helpers

- [ ] **21.1: Instalar Playwright**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos" || cd ~/.config/superpowers/worktrees/Matripuntos/feature/v1.6.1-confianza
npm install -D @playwright/test
npx playwright install chromium webkit
```

- [ ] **21.2: Crear `e2e/playwright.config.ts`**

```ts
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './specs',
  timeout: 30_000,
  fullyParallel: true,
  workers: 4,
  reporter: [['html'], ['github']],
  use: {
    baseURL: 'http://localhost:5174',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'webkit', use: { ...devices['Desktop Safari'] } },
  ],
  webServer: [
    {
      command: 'cd ../src/backend && DATABASE_URL=file:./e2e-test.db PORT=3001 NODE_ENV=test npm run dev',
      port: 3001, reuseExistingServer: !process.env.CI,
    },
    {
      command: 'cd ../src/frontend && VITE_API_BASE=http://localhost:3001 npm run dev -- --port 5174',
      port: 5174, reuseExistingServer: !process.env.CI,
    },
  ],
})
```

- [ ] **21.3: Helper `createCouple`**

```ts
// e2e/helpers/createCouple.ts
import type { APIRequestContext } from '@playwright/test'

export async function createCouple(api: APIRequestContext, opts?: { user1Email?: string }) {
  const ts = Date.now()
  const e1 = opts?.user1Email ?? `e2e-u1-${ts}@x.com`
  const e2 = `e2e-u2-${ts}@x.com`
  const password = 'pwd12345'

  const r1 = await api.post('/api/auth/register', { data: { email: e1, password, name: 'E2E1' } })
  const j1 = await r1.json()
  const r2 = await api.post('/api/auth/register', { data: { email: e2, password, name: 'E2E2', joinCode: j1.couple.secretKey } })
  const j2 = await r2.json()
  return {
    user1: { id: j1.user.id, email: e1, password, token: j1.token },
    user2: { id: j2.user.id, email: e2, password, token: j2.token },
    coupleId: j1.user.coupleId,
  }
}
```

- [ ] **21.4: Helper de seed activity y signin**

`e2e/helpers/signInUser.ts`:
```ts
import type { Page } from '@playwright/test'

export async function signInUser(page: Page, email: string, password: string) {
  await page.goto('/login')
  await page.getByLabel(/email/i).fill(email)
  await page.getByLabel(/contraseña/i).fill(password)
  await page.getByRole('button', { name: /entrar/i }).click()
  await page.waitForURL('**/dashboard')
}
```

- [ ] **21.5: Verificar setup**

```bash
cd e2e && npx playwright test --list
```

Expected: lista vacía (sin specs aún) sin errores de config.

- [ ] **21.6: Commit**

```bash
git add e2e/playwright.config.ts e2e/helpers/ package.json package-lock.json
git commit -m "feat(v1.6.1): Playwright setup + fixtures + helpers"
```

---

## Task 22: E2E specs — Auth (4 archivos)

Se escriben 4 archivos en `e2e/specs/`. Cada uno sigue el patrón:

```ts
// e2e/specs/auth-signup.spec.ts
import { test, expect } from '@playwright/test'

test('signup nuevo creador → dashboard', async ({ page }) => {
  await page.goto('/signup')
  await page.getByLabel(/email/i).fill(`e2e-${Date.now()}@x.com`)
  await page.getByLabel(/contraseña/i).fill('pwd12345')
  await page.getByLabel(/nombre/i).fill('E2E')
  await page.getByRole('button', { name: /crear cuenta/i }).click()
  await page.waitForURL(/\/onboarding/)
  await expect(page.getByText(/onboarding/i)).toBeVisible()
})

test('signup con password 6-7 chars OK', async ({ page }) => {
  await page.goto('/signup')
  await page.getByLabel(/email/i).fill(`e2e-${Date.now()}@x.com`)
  await page.getByLabel(/contraseña/i).fill('pwd123')
  await page.getByLabel(/nombre/i).fill('E2E')
  await page.getByRole('button', { name: /crear cuenta/i }).click()
  await expect(page.getByText(/contraseña/i)).not.toContainText(/inválida/i)
})

test('signup con email malformado → error visible', async ({ page }) => {
  await page.goto('/signup')
  await page.getByLabel(/email/i).fill('no-email')
  await page.getByLabel(/contraseña/i).fill('pwd12345')
  await page.getByRole('button', { name: /crear cuenta/i }).click()
  await expect(page.getByText(/email/i)).toBeVisible()
})
```

- [ ] **22.1: `e2e/specs/auth-signup.spec.ts`** (3 tests)
- [ ] **22.2: `e2e/specs/auth-signup-joincode.spec.ts`** (invitee con joinCode → 3 pasos onboarding nuevo)
- [ ] **22.3: `e2e/specs/auth-login.spec.ts`** (login OK + login error)
- [ ] **22.4: `e2e/specs/auth-logout-refresh.spec.ts`** (logout limpia store, refresh con token expirado redirige)

- [ ] **22.5: Run + commit**

```bash
cd e2e && npx playwright test specs/auth-*
git add e2e/specs/auth-*.spec.ts
git commit -m "test(v1.6.1): E2E specs auth (signup/joincode/login/logout-refresh)"
```

---

## Task 23: E2E specs — Onboarding + Activity + Task (8 archivos)

- [ ] **23.1: `onboarding-creator.spec.ts`** (6 pasos creador)
- [ ] **23.2: `onboarding-invitee.spec.ts`** (3 pasos invitado, banner aparece, dismiss persiste)
- [ ] **23.3: `activity-create-accept.spec.ts`**
- [ ] **23.4: `activity-counter-rounds.spec.ts`**
- [ ] **23.5: `activity-force.spec.ts`**
- [ ] **23.6: `activity-reject.spec.ts`**
- [ ] **23.7: `task-log-verify.spec.ts`**
- [ ] **23.8: `task-log-dispute.spec.ts`**

Cada spec usa `createCouple` helper + `signInUser` para tener 2 sesiones (2 contextos browser distintos). Patrón ejemplo para activity-create-accept:

```ts
import { test, expect } from '@playwright/test'
import { createCouple } from '../helpers/createCouple'

test('A crea actividad → B la ve en banner → B acepta → balance correcto', async ({ browser, request }) => {
  const { user1, user2 } = await createCouple(request)

  const ctxA = await browser.newContext()
  const pageA = await ctxA.newPage()
  // login user1
  await pageA.goto('/login')
  await pageA.getByLabel(/email/i).fill(user1.email)
  await pageA.getByLabel(/contraseña/i).fill(user1.password)
  await pageA.getByRole('button', { name: /entrar/i }).click()

  // crear actividad
  await pageA.getByTestId('fab-button').click()
  await pageA.getByText(/actividad/i).click()
  // ... rellenar wizard, submit

  const ctxB = await browser.newContext()
  const pageB = await ctxB.newPage()
  // login user2 + verificar banner muestra activity de A + aceptar

  // verificar balance en ambos
})
```

- [ ] **23.9: Run + commit**

```bash
cd e2e && npx playwright test specs/onboarding-* specs/activity-* specs/task-*
git add e2e/specs/
git commit -m "test(v1.6.1): E2E specs onboarding + activity + task"
```

---

## Task 24: E2E specs — Mood + Phrase + Avatar (5 archivos)

- [ ] **24.1: `mood-set-see.spec.ts`** (A fija mood → B ve)
- [ ] **24.2: `mood-expire.spec.ts`** (mock Date 24h, expira)
- [ ] **24.3: `mood-history.spec.ts`** (MyMoodWeek, sin fuga partner)
- [ ] **24.4: `phrase-cascade.spec.ts`** (disputa abierta → categoría reconciliacion)
- [ ] **24.5: `avatar-picker.spec.ts`** (cambio Settings persiste)

- [ ] **24.6: Run + commit**

```bash
cd e2e && npx playwright test specs/mood-* specs/phrase-* specs/avatar-*
git add e2e/specs/
git commit -m "test(v1.6.1): E2E specs mood + phrase + avatar"
```

---

## Task 25: E2E specs — Settings + Privacy + Smoke (5 archivos)

- [ ] **25.1: `settings-leave-couple.spec.ts`** (A sale, ambos sin couple, A ve histórico)
- [ ] **25.2: `settings-delete-account.spec.ts`** (A borra, B ve "Usuario eliminado")
- [ ] **25.3: `privacy-cookie-consent.spec.ts`** (banner, accept/reject, PostHog request)
- [ ] **25.4: `smoke-bottom-nav.spec.ts`** (4 tabs sin error consola)
- [ ] **25.5: `smoke-fab-actions.spec.ts`** (FAB sheet 3 opciones)

- [ ] **25.6: Run completo + commit**

```bash
cd e2e && npx playwright test
git add e2e/specs/
git commit -m "test(v1.6.1): E2E specs settings + privacy + smoke"
```

---

## Task 26: CI workflow updates

- [ ] **26.1: Editar `.github/workflows/ci.yml`**

Añadir jobs:
```yaml
  e2e-chromium:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
      - run: npx playwright install chromium --with-deps
      - run: cd e2e && npx playwright test --project=chromium
      - if: failure()
        uses: actions/upload-artifact@v4
        with: { name: e2e-chromium-report, path: e2e/playwright-report }

  e2e-webkit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps: [...]  # idéntico con --project=webkit
```

Ampliar el job de jest hermético existente para incluir nuevos contract tests.

- [ ] **26.2: Probar localmente con `act` o push a feature branch**

```bash
git push origin feature/v1.6.1-confianza
# Verificar en GitHub Actions que e2e-chromium y e2e-webkit pasan
```

- [ ] **26.3: Commit**

```bash
git add .github/workflows/ci.yml src/backend/jest.config.ci.js
git commit -m "ci(v1.6.1): jobs e2e-chromium + e2e-webkit + extender contract tests"
```

---

## Task 27: Settings — pestañas Privacidad y Pareja ampliada

- [ ] **27.1: Editar `Settings.tsx`** — añadir pestaña Privacidad

```tsx
// dentro de Settings, sección nueva:
<section className="rounded-2xl bg-white/5 p-5 space-y-4">
  <h2 className="text-lg font-semibold text-white">Privacidad</h2>

  <label className="flex items-center justify-between">
    <span className="text-sm text-white">Análisis de uso anónimo</span>
    <input type="checkbox" checked={consent?.analytics ?? false} onChange={e => setConsent({ analytics: e.target.checked })} />
  </label>

  <button
    data-testid="btn-open-delete-wizard"
    onClick={() => setDeleteOpen(true)}
    className="w-full py-3 rounded-xl bg-red-500/20 border border-red-500/40 text-red-200"
  >
    Eliminar mi cuenta
  </button>
</section>

<DeleteAccountWizard isOpen={deleteOpen} onClose={() => setDeleteOpen(false)} onDeleted={() => { logout(); navigate('/') }} />
```

- [ ] **27.2: Pestaña Pareja — sección Etapas anteriores + botón Salir**

```tsx
import { LeaveCoupleWizard } from '../components/v2/wizards/LeaveCoupleWizard'

const pastCouplesQuery = useQuery({ queryKey: ['past-couples'], queryFn: () => apiClient.get('/history/past-couples').then(r => r.data) })

// JSX:
{pastCouplesQuery.data?.pastCouples?.length > 0 && (
  <section>
    <h3 className="text-base text-white mb-2">Etapas anteriores</h3>
    {pastCouplesQuery.data.pastCouples.map((pc: any) => (
      <details key={pc.id} className="bg-white/5 rounded-lg p-3 mb-2">
        <summary>Mi etapa con {pc.partnerName}</summary>
        <div className="text-xs text-white/70 mt-2">
          <p>Inicio: {pc.createdAt} · Fin: {pc.dissolvedAt}</p>
          <p>Eventos: {pc.totalEvents} · Tareas: {pc.totalTasks}</p>
        </div>
      </details>
    ))}
  </section>
)}
<button data-testid="btn-open-leave-wizard" onClick={() => setLeaveOpen(true)} className="w-full py-3 rounded-xl bg-amber-500/20 border border-amber-500/40 text-amber-200">
  Salir de la pareja
</button>
<LeaveCoupleWizard isOpen={leaveOpen} onClose={() => setLeaveOpen(false)} onLeft={() => { /* refresh user store + navigate dashboard */ }} />
```

- [ ] **27.3: Smoke + commit**

```bash
git add src/frontend/src/pages/Settings.tsx
git commit -m "feat(v1.6.1): Settings con pestañas Privacidad + Pareja ampliada"
```

---

## Task 28: ROADMAP + DECISIONS update + tag preparación

- [ ] **28.1: Actualizar `docs/ROADMAP.md`**

Cambiar fila `v1.6.1` a `✅ Producción YYYY-MM-DD` con tag `v1.6.1`.

- [ ] **28.2: Actualizar `docs/DECISIONS.md`**

Añadir bloque `## YYYY-MM-DD · v1.6.1 · Confianza` con las 10 decisiones del §3 del spec, formato consistente con entradas anteriores.

- [ ] **28.3: Commit**

```bash
git add docs/ROADMAP.md docs/DECISIONS.md
git commit -m "docs(v1.6.1): tag v1.6.1 producción + 10 decisiones"
```

---

## Task 29: QA dos cuentas — checklist + ejecución

- [ ] **29.1: Crear `docs/qa/v1.6.1-qa-checklist.md`** con los 18 ítems del §13 del spec.

- [ ] **29.2: Ejecutar QA real con dos navegadores**

Recorrer cada ítem. Documentar hallazgos. Si algo rojo → abrir hotfix tasks dentro del mismo branch.

- [ ] **29.3: Commit checklist rellenado**

```bash
git add docs/qa/v1.6.1-qa-checklist.md
git commit -m "docs(v1.6.1): QA checklist dos cuentas ejecutado"
```

---

## Task 30: Merge a main + tag + deploy

- [ ] **30.1: CI verde**

```bash
git push origin feature/v1.6.1-confianza
# verificar todos los jobs verdes en GitHub
```

- [ ] **30.2: Merge ff-only**

```bash
cd "/Users/edu/Web development/Claude/Matripuntos"
git fetch origin
git checkout main
git merge --ff-only feature/v1.6.1-confianza
git push origin main
```

- [ ] **30.3: Tag**

```bash
git tag -a v1.6.1 -m "v1.6.1 · Confianza — privacy + telemetría + E2E + contract tests + onboarding invitee + rate-limit"
git push origin v1.6.1
```

- [ ] **30.4: Deploy**

- Backend: Render auto-deploy desde push a `main`. Verificar `/api/health` muestra commit nuevo + nueva migración aplicada (`20260502000000_v1_6_1_confianza`).
- Frontend: build + FTP upload (script existente).
- Configurar env vars en producción: `POSTHOG_KEY`, `POSTHOG_HOST`, `VITE_POSTHOG_KEY`, `VITE_POSTHOG_HOST`.
- Verificar: cookie banner aparece, /privacy /terms /cookies cargan, account deletion funciona en producción con cuenta de prueba.

- [ ] **30.5: Limpiar worktree**

```bash
git worktree remove ~/.config/superpowers/worktrees/Matripuntos/feature/v1.6.1-confianza
git branch -d feature/v1.6.1-confianza
```

---

## Self-review

**Spec coverage check:**

| Spec § | Implementado en task |
|---|---|
| §4 Privacy stack | T9 (legal pages) + T10 (RAT/DPAs/ARCO-POL) + T11 (consent service) + T12 (banner) |
| §4.3 Account deletion | T4 (service) + T5 (routes + wizard) |
| §4.4 Salir de pareja | T6 (service) + T7 (routes + wizard) |
| §4.6 Retention cron | T8 |
| §5 Telemetría | T13 (catálogo) + T14 (frontend wrapper) + T15 (backend wrapper) + T16 (integración) |
| §6 Onboarding invitado | T17 (2 pasos) + T18 (banner + util + endpoint) |
| §7 E2E Playwright | T21 (setup) + T22-25 (22 specs) + T26 (CI) |
| §8 Contract testing | T1 (workspace) + T2 (migración schemas) + T20 (8 contract tests) |
| §9 Rate-limit | T19 |
| §10 Backend changes | T3 (schema) + T5/T7 (rutas) + T8 (cron) |
| §11 Frontend changes | T9 (legal pages) + T17 (onboarding) + T18 (banner) + T27 (Settings) |
| §13 QA dos cuentas | T29 |
| §14 Plan release 16 hitos | T1-T30 cubren los 16 hitos en orden |

**Placeholder scan:**
- Las "actions usuario" en T10.2 (DPA checklist con "⏳ Pendiente") son explícitas: el user firma los DPAs, no es un TBD del plan.
- T9.1 "el copy final lo aporta el usuario" — versión inicial entra completa, pulir lengaje no bloquea release. Justificado.
- T18.3 endpoint `profileCompletion` tiene una nota sobre mover util a shared — opcional, no bloqueante.

**Type consistency:**
- `MOOD_KEYS` exportado desde `packages/shared/src/schemas/profile.ts` y consumido en backend (Task 9 v1.6) + telemetry-events (T13) + frontend (heredado v1.6).
- `accountDeleteSchema` definido T2.7, usado en T5.2.
- `coupleLeaveSchema` definido T2.8, usado en T7.2.
- `validateTelemetryEvent` definido T13.1, usado en T14.2 y T15.1.
- `criticalBucket`/`authBucket` etc. definidos T19.2, usados en T5.2 + T7.2 + T19.3.

**Riesgos cubiertos:**
- Migración Supabase (v1.5.1 prerequisito) en Task 0.
- Workspace shared rompe builds → T2 hace migración mecánica con tests pre/post + T2.12 verifica resolución de paths.
- E2E lento → T26 paraleliza chromium/webkit y caché Playwright.
- Cron borra por error → T8 dry-run las 2 primeras ejecuciones en producción.
- SMTP no listo → T5.2 fallback a `codeViaConsole` en dev + 503 explícito en prod.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-02-v1.6.1-confianza.md`. Two execution options:

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration. Especially adecuado para v1.6.1 dada la cantidad de tasks.
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
